# Item 2a — (i-a) producer, readers and gate, plus bound (2-CAP)

> **INDEPENDENT DESIGN FILE: BINDING, AND UNREVIEWED.** This file is part of the requirements of
> `docs/plans/2026-08-27-pipeline-final-plan.md` (the parent). The parent cites it at § *Item 2a*, the (i-a) producer, readers and gate, plus bound (2-CAP), and does
> not restate it. It is reviewed on its own: `init-design-review.sh docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-i-a-producer-readers-gate-and-2-cap.md`, then
> `run-design-review-loop.sh`. Its review verdict covers only this file's spec hash. A verdict on the parent
> does not cover this file, and a verdict on this file does not cover the parent. It is not history.
>
> **Context for a reviewer who reads only this file.** The body below was moved verbatim from the parent on
> 2026-09-23 (Chris-approved split), and this header was added the same day; the body is unchanged. Item
> numbers, node names such as `2a-env`, `baseline captured` and `protocol-approved`, and bound labels such as
> (i-a), (i-b), (2-ORD) and (2-CAP) are defined in the parent's execution-order table (§ *Execution order
> and acceptance*) and § *Still needs Chris*, and, since the parent's SECOND MOVE (2026-09-24), in the
> children: the dependency graph, *Standing fallback*, *Recommended next actions* and § *Item 2* in the 2p
> file; § *Item 2i* and § *Item 8* in the item 8 file; § *Item 2a* in the (i-a) file; § *Item 7* in the
> item 7 file; § *#840 proposed revisions* in the 2p file. The parent keeps only one-line pointers there (2026-09-26; UNREVIEWED). A lens reviewing this file receives this file and is not assumed to open the parent or the checkout.
> Line anchors `:NNN` into the parent point at the parent as it stood before the 2026-09-23 splits. Anchors
> into other files name their own tree or checkpoint.
>
> **RELABELLED 2026-09-23 (native run `6e0afdc0` HIGH, arbiter-confirmed; Photon correction decision; plan-only, UNREVIEWED).** This file was first named and titled as bound (i-c). Its body is the (i-a)
> producer, readers and gate plus the whole (2-CAP) contract. The only (i-c) file is
> `docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-i-c-scanner-counters.md`. The body is unchanged.

**PRODUCER, READERS AND GATE, named here because a requirement without them is the "named deliverable owned by no half" defect this document repairs elsewhere.** **PRODUCER:** the short-circuit mint at `run-review-loop.sh:3115` writes a form that is not byte-identical to `:3585`'s. **(i-a) REQUIRES ONLY THAT A READER CAN DISTINGUISH THE TWO MINTS FROM THE ARTIFACT ALONE; the concrete form is `2a-env`'s to choose under ONE constraint, so the honouring set migrates once rather than twice: item 2i(B) Stage 2's writers mint this same executed-review form and add no FORM token, and Stage 2 does not tell a pre-Stage-2 executed-review mint apart by form (parent § *Item 2i*, (B) Stage 2), so the short-circuit versus executed-review distinction this bound requires is kept unchanged through Stage 2 (aligned with the parent 2026-09-24; plan-only, UNREVIEWED); and `2a-env` is NOT the convergence-policy half's envelope. The completeness/cap transport envelope is a DIFFERENT artifact from this marker envelope — its constraints are the item-2a overflow row's and it is scheduled by bound (2-CAP) below (re-pointed 2026-09-13; UNREVIEWED) — and (i-a) does not wait on it.** **(2-CAP) THE REVIEWER-CONTRACT DELTA THE BASELINE MEASURES — PER-RESPONSE COMPLETENESS PLUS COMPLETE-RESULT COLLECTION — LANDS IN `2a-env`. DEFINED ONCE HERE; every other surface cites it. RESTATED 2026-09-13 under Chris's `go` on the measurement target (`840-new-baseline-direction-go-20260913.md`; UNREVIEWED).** *History, not instruction:* as first written the same day, this bound moved only the per-response statement and kept continuation behind `baseline captured`; native b5bddf50 (HIGH) showed that complete-only eligibility beside the live 3-new drip (`init-review-loop.sh:466`/`:556`, `skills/litmus/SKILL.md:641`) admits only low-yield reviews, and a retained fixed findings threshold was rejected for the same reason. **MEASUREMENT TARGET:** `baseline captured` measures the reviewer version that carries this delta. Observations from before it are the **HISTORICAL COHORT** — retained, labelled with their contract version, never pooled with the baseline, never relabelled, never counted toward N. The contract version is a required cohort dimension (item 2p's prompt/policy/harness/route revision, cited). **THE DELTA, EXHAUSTIVE:** **(a)** each response states item 2a's `completeness` enum, added to EVERY producer contract that lists response fields — the `<output_contract>` field list at `init-review-loop.sh:450-456` and the JSON format at `skills/litmus/SKILL.md:352`; `:354`'s PASS definition ("No issues or only low-severity") stays a findings-based status rule, unchanged; `:478` stays cited to (c-1b) — `complete` means the producer states it withheld nothing it found, `truncated` that it states more remain; **(b)** the 3-new drip clause is deleted from EVERY copy — both live prompt blocks (`init-review-loop.sh:466`, `:556`) and both skill-body copies (`skills/litmus/SKILL.md:623` "Convergence cap - Max 3 new issues per iteration" and `:641-642`), the list being the count — while the PER-RESPONSE limits (10 issues per response, the existing timeout) are UNCHANGED — they are resource limits, not a cap on accepted findings; **(c) CONTINUATION CHAIN:** when a page states `truncated`, further pages are collected within the same reviewed scope and iteration, bound to the same `reviewed_diff_hash`, under the item-2a OVERFLOW row's transport constraints (1)–(5), cited not restated — self-identifying pages that accumulate rather than replace, the hard page cap, dedup across pages after accumulation, `cap_exhausted`, and the single OVERALL deadline; the envelope itself — page request, framing, cross-response accumulation, the one shared remaining budget and the fallback bound — is specified once at that row's "THE ENVELOPE" and cited, not restated (R2, 2026-09-13); the chain's findings are the accumulated pages; **(d)** every page attempt is recorded with run identity, slot, chain id, page ordinal and attempt ordinal, plus its stated `completeness` and `withheld` manifest so the outstanding set is derivable from the records, and the backend that actually served it — its SERVING BACKEND, none for a failed page (T2), defined at ATTEMPT ATTRIBUTION below (CORRECTED 2026-09-16 post-15, native round-15 issues[4]; UNREVIEWED) — so the assembled `model` is derivable too, in a MINIMAL PER-PAGE ATTEMPT RECORD that `2a-env` OWNS and writes at its continuation driver; item 2-A later EXTENDS this record with its dispatch-boundary fields (CAPTURE GRANULARITY, cited) and never replaces it. **SERVING-BACKEND IDENTITY CHANNEL — the narrow producer of this REQUIRED field, owned by `2a-env` (MECHANISM MEASURED 2026-09-15 in isolated stubs on this host's bash 3.2.57; RUNTIME IMPLEMENTATION PENDING; production dispatcher and reviewers NOT exercised; the private-directory nonce receipt proposed earlier the same day is WITHDRAWN; native FINAL13 issues[6] MEDIUM; source-traced at HEAD 34887cb7; UNREVIEWED).** WHY A SEAM IS NEEDED: the loop dispatches through a command substitution, `REVIEW_OUTPUT=$(execute_review …)` (`run-review-loop.sh:3256`), so only stdout and the exit status come back; `execute_review` (`resolve-cli.sh:4378`) sends Codex to `_execute_codex` (`:4427`), whose Codex-success emit (`:4032-4035`, exit status `_ECX_EXIT_CODE`) and droid-escalation-success emit (`:4000-4005`) BOTH return 0 with review text on stdout, so today's exit status cannot name the backend. No existing plumbing can: the `codex-droid-fallback` event (`:3996`) is appended to a shared `bypass-log.jsonl` with no run, page or attempt identity (a stale/cross-attempt source); stdout is reviewer-authored text; and the requested CLI (`RESOLVED_CLI`) is not the serving one on the Codex arm. None of these is ever read as provenance. WHY THE WITHDRAWN RECEIPT DOES NOT SERVE: it trusted the first writer of a named file carrying a nonce, so any same-UID process able to write that directory and learn the nonce could forge it, and its equivalence to the #803 lib-pin trust was asserted, never demonstrated; nothing of it is carried forward. TRACED AND MEASURED BOUNDARIES (source-read at HEAD 34887cb7; bash behaviour MEASURED 2026-09-15 on this host's `/bin/bash` 3.2.57 with isolated stubs — a verbatim copy of `_bd_exit_as`, stand-in reviewer children and dispatcher shapes; no reviewer, network, library or gate state touched; scripts, outputs and hashes archived under `~/.hermes/reports/840-probe-20260915/`): (i) the substitution at `run-review-loop.sh:3256` forks, so a variable the dispatcher sets (`_bd803_droid_ok`, `:3972-3973`) dies with it (measured); `_bd_exit_as` (`resolve-cli.sh:1366-1375`) only sets the status and does not exit, so an identity assignment belongs inside the emit branch, before its `_bd_exit_as`; (ii) every reviewer exec on this path goes through `_portable_timeout` — `_execute_codex`'s companion, direct-codex and droid-escalation launches (`:3812`, `:3822`, `:3956`, `:3963`), `_run_review_with_retries` (`:3465-3481`, used by agy `:4507`/`:4513` and grok `:4603`) and direct droid (`:4536`) — whose eight launch commands (`:2425`, `:2437`, `:2456`, `:2489`, `:2525`, `:2527`, `:2537`, `:2560`) clear the environment with `/usr/bin/env -i` or not at all and run `timeout -k 5` or a perl fork/`exec`, none of which closes an inherited descriptor; (iii) the driver runs `#!/bin/bash -p`, bash 3.2.57 on this host (measured `--version`), which has neither `coproc` nor `{fd}` allocation, and the repo has measured that `builtin exec N< f` does not keep the descriptor (`resolve-cli.sh:252-256`), so a held descriptor is a pinned number under group redirection (`:281`) and any descriptor, FIFO or file read-back meets filesystem first-opener trust, which this plan does not accept FOR THE IDENTITY — (ii) and (iii) are why the identity below travels on no descriptor, file or FIFO, and are no longer load-bearing for it; the reviewer's stdout, which is reviewer-authored text and never provenance, is the one thing the CARRIER below moves to a driver-created regular file, the posture the #823 PR backstop already takes (`_bs_out` created by `mktemp` at `run-review-loop.sh:1354`, written at `:1445`, removed by the `:536` EXIT trap); (iv) calling `execute_review` in-process WITHOUT `$(…)` is MEASURED INFEASIBLE: the `exit` statements and EXIT/TERM/INT traps once cited at `resolve-cli.sh:4958-5028` belong to the separate `--execute-opencode-review` process (`:4922`), not to `execute_review`'s in-shell call graph, whose only trap install (`:329`) is skipped when the caller owns EXIT — but a dispatcher nounset fault under `set -euo pipefail` (`run-review-loop.sh:193`) kills the driver (status 127, even inside `set +e`), and inside an `if` or `||` condition exits it with status 0 WITHOUT its EXIT trap, where today's substitution returns 1 and the loop continues; a dispatcher `exit 5` ends the driver with 5; `grok_sandbox_preflight`'s plain `LD_*`/`DYLD_*` assignments (`resolve-cli.sh:4332-4340`, called in-shell at `:4594`) scrub the driver's own environment. Dropping output that a lingering descendant writes after the dispatcher returns is NOT one of these failures: waiting for it is the unbounded capture #823 removed (`skills/litmus/SKILL.md:243`; `scripts/ci/run-shell-tests.sh:316-321`), and the CARRIER below adopts the file capture (CORRECTED 2026-09-15 post-14, native round-14 issues[2] MEDIUM; UNREVIEWED). The earlier B1 statement that no in-process result crosses the substitution was wrong: the substitution's own exit status does. REQUIRED CHANNEL — the capturing subshell's own exit status: WRITERS are exactly the branches that emit accepted stdout with status 0 — `codex` at `_execute_codex`'s Codex-success emit (`:4032-4035`, the `else` of the non-zero test at `:3905`), `droid` at its droid-ok emit (`:4000-4005`), the arm's own name at `_run_review_with_retries`'s emit (`:3526-3527`) when it returns 0 for agy or grok (after the empty/bare-notice normalization at `:3523-3525`, guarded on the final status being 0), and `droid` at direct droid (`:4536`), a bare pipeline with no emit branch today, which therefore needs one; each assigns ONE name from the closed set {`codex`, `droid`, `agy`, `grok`} to one dispatcher-shell variable before its `_bd_exit_as`, and nothing is assigned on 124, `BUILTIN_FALLBACK` exit 3, 1 or a refusal. CARRIER — A SUBSHELL WITH A REGULAR-FILE CAPTURE, NOT A COMMAND SUBSTITUTION (CORRECTED 2026-09-15 post-14, native round-14 issues[2] MEDIUM; Photon correction decision; UNREVIEWED): the driver's existing substitution `REVIEW_OUTPUT=$(execute_review …)` becomes `( <clear the variable>; execute_review …; <map> ) >"$review_out"`, still inside `set +e`/`set -e`, where `$review_out` is created by the driver with `mktemp` immediately before and joins the `:536` EXIT-trap removal list as `_bs_out` does; the map runs in that same subshell after the call — status 0 exits 200, 201, 202 or 203 for `codex`, `droid`, `agy` or `grok` and 204 for no name; a raw non-zero 200–204 exits 1; any other status exits unchanged. DRIVER: straight after `REVIEW_EXIT=$?` it maps 200–203 to `REVIEW_EXIT=0` plus that backend and 204 to `REVIEW_EXIT=0` with the backend unrecorded (bound (2-CAP) T2); a raw 0 means the map did not run and is likewise unrecorded (T2); the backend is never inferred and never marked served; only then is `REVIEW_OUTPUT` read from `$review_out`, once. WHY NOT A SUBSTITUTION: it waits for EOF from every writer of its pipe, and `_portable_timeout` stops supervising once the leader is reaped, so a surviving descendant holding stdout holds the driver with no clock (#823, `skills/litmus/SKILL.md:243`; `scripts/ci/run-shell-tests.sh:316-321`); a regular file has no EOF to wait for, so the attempt ends when the subshell does — which holds only once the subshell itself can end, and inner captures deny that today (CAPTURE OWNERSHIP below; CORRECTED 2026-09-16 post-15, native round-15 issues[0] HIGH; UNREVIEWED). The variable is cleared at the start of every subshell, so an inherited or earlier value is never read and each attempt's subshell is fresh; the map uses `[[`/`if`/`elif` and `builtin exit` (the #803 no-shadowable-builtin idiom — that spelling not measured), never `case … ;;`: the `if`/`elif` form is the one measured, and `case … ;;` was measured mis-parsed by this bash inside a command substitution; no descriptor, FIFO, environment variable, argument or prompt carries the identity, and the capture file carries stdout only. The wrapper is the litmus loop's only (`run-review-loop.sh:3256`); `run-design-review-loop.sh`'s `execute_review` calls (`:320`, `:926`, `:998`, `:1072`, `:1238`) are not the loop path and gain no CARRIER — the library-level CAPTURE OWNERSHIP below does reach them (CORRECTED 2026-09-16 post-15; UNREVIEWED) — and the opencode arm is outside the writer set. The emit-branch edits change `resolve-cli.sh`'s bytes, so its #803 pin is re-taken with them. MEASURED PRESERVATION, IN TWO STUB SETS THAT ARE NOT ONE MEASUREMENT: (1) the command-substitution form this carrier replaces (`~/.hermes/reports/840-probe-20260915/`; 296 combinations of streaming, captured-and-re-emitted and Codex→droid-escalation shapes × statuses 0, 1, 3, 124, 199, 200, 204, 205, 255 × eight payloads including trailing newlines, none, CR, UTF-8, NUL and a token-shaped body): captured stdout hash identical to today's substitution; the backend recorded only on status 0, and `droid` on escalation; a forging child that exports the variable, writes a name to descriptors 3–9 and prints a token-shaped body line changes nothing; an exported inherited value is cleared; nounset (status 1, loop continues, also inside `if`), `exit 5` and environment stay today's; (2) the subshell-plus-regular-file form adopted above (2026-09-15 post-14 isolated fixture, three executions, the last `run.tSauOK` 30 ok / 0 FAIL; script archived at `~/.hermes/reports/840-post14-correction-prechange-archive-20260915/fixture/fixture.sh`, sha256 `99f17096…`): with a stub dispatcher leaving a detached descendant holding stdout, today's substitution blocked 5122–5171 ms on the descendant while the file form returned in 13–14 ms with the descendant still alive; `codex` and `droid` were recorded on status 0 and nothing on 124; a reviewer's raw 202 read as 1 with no backend; an exported inherited identity was cleared and an unknown writer gave 204 with no backend; a dispatcher nounset fault gave status 1 to a driver that continued; the identity variable never reached the driver shell; the captured text was the review alone. NOT MEASURED for the file form: the 296-combination payload set, a dispatcher LEADER that itself outlives its window (bounded only by the existing per-launch `_portable_timeout`), and any cleanup of the descendant — returning early does NOT reap it, and nothing here claims it is reaped. THE TWO DELTAS: (a) `REVIEW_EXIT` is identical except a raw 200–204. Today only `_run_review_with_retries` passes a reviewer's own exit code through (`:3527`, agy and grok; `_ECX_RC` and `_ER_RC` at `:3596`, `:3692`, `:4412` carry preflight 1s), so an agy or grok reviewer exiting 200–204 reaches `REVIEW_EXIT` verbatim and takes the non-zero branch at `:3377`; under the wrapper it arrives as 1 and takes the same branch, and the only observable is the number printed at `:3378`. Nothing else emits that range: no `exit`/`_bd_exit_as` of 200–299 exists in `resolve-cli.sh`, `_portable_timeout` emits 124 (`:2470`, `:2503`, `:2551`, `:2574`), and signal deaths are 128+n. (b) Bytes a descendant writes to stdout after the subshell exits are not part of `REVIEW_OUTPUT`, which is read once, immediately; bytes written before the subshell exits are captured as a run on the same library without the CARRIER captures them, and whether a racing late write lands before that read is not claimed deterministic. **CAPTURE OWNERSHIP — THE CARRIER IS NECESSARY, NOT SUFFICIENT: EVERY INNER ATTEMPT CAPTURE AND ITS LAUNCHER ARE BOUNDED WITH IT — ADDED 2026-09-16 post-15 (native round-15 issues[0] HIGH and issues[4] MEDIUM; Photon correction decision `840-post15-plan-correction-decision.md`; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED).** OWNER: `2a-env`, beside the CARRIER it completes; it changes how the capture sites capture and adds no per-attempt record (those stay item 2-A's, gated on item 1), so `2a-env` still does not wait on item 1. WHY: the CARRIER's attempt ends when its subshell ends, and inside `execute_review` every reviewer launch still captures through `$(…)`, while `_portable_timeout` (`resolve-cli.sh:2010`) returns once its leader is reaped — its perl arms reap the child group only in the ALRM handler (`:2461-2471`, `:2494-2504`) and return straight after `waitpid` on a normal exit (`:2505-2509`) with no TERM/INT/HUP handler, and its coreutils arms run `timeout -k 5` in the foreground, which gives the child its own group (group creation measured by #823, `run-review-loop.sh:1425`) and leaves that group alone on a normal exit — so a descendant still holding an inner capture's pipe holds the subshell, and the driver, with no clock. The post-14 13–14 ms carrier fixture used a stub dispatcher with no inner substitution and does not measure this case. **THE INNER CAPTURE SITES AT HEAD (the list is the count):** `_run_review_with_retries` none-mode `:3465` (review arm) and `:3467`/`:3470` (non-review arm); its pipe-mode `:3476` (review) and `:3478`/`:3481` (non-review — grok lands here, its label being outside `:3475`'s review set); `_execute_codex`'s companion launch `:3812` and direct CLI `:3822`; the droid escalation `:3956` (separate stderr file) and `:3963`. **THE SCOPE IS A RULE, AND THE LIST IS DERIVED FROM IT — CORRECTED 2026-09-16 post-16 (native run `5e995822` issues HIGH direct-droid; Photon manual-resume brief `manual-802-838-840-847-20260916.md` #840 section, on the `840-post16-readonly-diagnosis-result.md` roots; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED; the earlier sentence enumerated STDOUT-CAPTURE sites only and so excluded two launches that take their PROMPT through a pipe, which Δ3 had already made in-scope one phase earlier).** IN SCOPE is EVERY review launch that either receives its prompt through a pipe or captures its stdout through `$(…)` — both hazards, one rule. The site list is DERIVED FROM THAT RULE ON THE IMPLEMENTATION START TREE, never carried forward from an earlier revision: at the start tree, every `printf '%s' … |` feeding `_portable_timeout` or an `env -i` review child, and every `$(… _portable_timeout`, is a site, and a launch matching the rule that is absent from the list makes the inventory INCOMPLETE. **DERIVED AT HEAD `34887cb7`, ADDED BY THIS CORRECTION:** direct droid (`:4536`) and the opencode witness (`:4867-4889`, `printf '%s' "$2" | … /usr/bin/env -i … --execute-opencode-review`) are PROMPT-OVER-PIPE launches and are IN SCOPE for C2's prompt-file transport and Δ3, though neither is a stdout capture; the arbiter reproduced the stdin hang on the `:4536` shape, and `:4867` has the same shape and was NOT reproduced. **RE-DERIVATION OWED:** `origin/main` PR #855 adds an agy stream-json stdin rung that this branch does not carry, so the list is re-derived at the start tree and this HEAD list is not the implementation's list. `builtin` (`:4899`) launches nothing and stays out; the agy version probes (`:4163`, `:4167`) share the launcher. **C1 — THE LAUNCHER OWNS ITS GROUP ON EVERY EXIT PATH, REUSING #823's HELPERS:** the four perl bodies (`:2456-2477`, `:2489-2510`, `:2537-2558`, `:2560-2581`) become ONE library string with the #823 backstop's `_BS_PERL_TO` semantics (`run-review-loop.sh:1225-1272`: `setpgrp` child; TERM/INT/HUP handlers that reap the child group; the ALRM reap; a normal-exit group TERM → 2 s → KILL with the status snapshotted first), which the driver then uses instead of its own copy; the coreutils arms (`:2425`, `:2437`, `:2525`, `:2527`) launch `timeout -k 5` in the background with stdin passed explicitly, `wait` for it and then reap its group, with a TERM/INT/HUP trap around the `wait` that reaps and exits 128+signo; and `_bs_reap_group` (`run-review-loop.sh:490-529`, `_BS_REAP_GRACE_S=8`) moves into `resolve-cli.sh` as ONE implementation both files call, rewritten to the library's no-shadowable-builtin posture (`resolve-cli.sh:2011`, `:3380`, `:4842-4844`; the `builtin` spelling of `:329`) with its probe order and grace unchanged. These edits change `resolve-cli.sh`'s bytes, so its #803 pin is re-taken with them. **C2 — EVERY INNER SITE CAPTURES TO A PRIVATE REGULAR FILE:** a `mktemp` output file; in pipe mode the prompt goes to a private `mktemp` file and is streamed to the child by a WRITER THE LAUNCHER OWNS — spawned INSIDE the process group C1 already creates and reaps, never by the caller — so **THE CHILD'S fd 0 REMAINS A PIPE AT EVERY PIPE-MODE SITE and no site changes the fd-0 type any child sees** (**CORRECTED 2026-09-16 — DRAFT, UNREVIEWED, NOT IMPLEMENTED: HEAD still uses `printf '%s' … |` at every site this rule names, and nothing below claims otherwise**). **A REGULAR-FILE fd 0 IS NOT MANDATED, and the earlier "passed as stdin instead of `printf |`" wording that did mandate one is WITHDRAWN** — it collided with three constraints the tree already states: `dispatch.sh:2713-2715` ("Pipe (not fd0-redirect) is required for bare droid to read its prompt without bailing"), `:1437-1439` ("Without a flag, droid bails on first read (fatal under stdin redirection)"), and the read-only posture at `resolve-cli.sh:4517-4522`, which forbids buying a redirect with `--auto`; the mandate would have put all three bare-`droid exec` sites (`:3956`, `:3963`, `:4536`) into exactly the bare-plus-regular-file combination those comments reject, and imposed an unevidenced fd-0 change on the opencode witness (`:4867-4889`) where the stdin hang was NOT reproduced. **THE SCOPE RULE AND ITS SITE LIST ARE UNCHANGED — this corrects the transport FORM the rule prescribes, uniformly, and narrows the rule's reach not at all.** **IMPLEMENTATION OBLIGATION, stated so it cannot be lost in the rewrite:** the writer is a MEMBER of the launcher's group, so C1's existing "the launcher owns its group on every exit path" obligation and the existing `_bs_reap_group` TERM → grace → KILL already reach it — **no new mechanism, no new bound, no new acceptance owner**; its lifetime stays inside the teardown bound Δ4 already subtracts, and the prompt file is one of the two files this bound already requires to be removed on every exit path. **A CALLER-SIDE WRITER IS REFUSED**: it sits in the CALLER's group, which no reap of the launcher's group can reach. Δ3's two gains are unchanged and are the reason for the form — the writer is not a member of the caller's pipeline, so no writer status enters the capture, and the launcher never waits on it. **EVIDENCE, WITH ITS LIMITS:** an offline shell prototype of this shape (`~/.hermes/reports/840-closure-checks-run3/`, one run, 22/22, exit 0, log `logs-d-run3.txt`) measured pipe fd 0, byte-exact delivery above the pipe buffer, writer and descendant membership in the launcher's group, their collapse by that group's reap, both faces of Δ3 on the HEAD form (141 under `pipefail`, silent loss without), and a NEGATIVE CONTROL in which a caller-side writer fails the membership assertion. **That is a MECHANISM PROTOTYPE ONLY: not this library, not `_portable_timeout`, not a native review PASS, and no evidence about the real droid binary's reaction to any fd-0 type** (the supporting host-semantics measurement is at `840-pgid-probe/`; the earlier run-2 FAIL remains a historical FAIL at `840-closure-checks-run2/`). After the launch the output is read ONCE as a snapshot bounded by the file's size at that moment, and an output larger than one cap constant reusing `_BS_MAX_ENVELOPE`'s value is treated as EMPTY, never truncated (#823's snapshot/oversize/empty rules, `run-review-loop.sh:1500-1540`), which the existing ladder already classifies as a failed attempt (`resolve-cli.sh:3510`, `:3523-3525`; `:3829`, `:3898-3901`); stderr stays merged exactly where it is merged today and the droid escalation keeps its separate stderr file (`:3954-3958`); both files are removed on every exit path. The codex companion's persistent broker is spawned `detached` and `unref`'d by the installed codex plugin 1.0.5 (`scripts/lib/broker-lifecycle.mjs:61-67`, source-read), so it sits outside every attempt group, holds no capture and is not reached by a group reap. **BEHAVIOURAL DELTAS FROM HEAD — STATED, NOT HIDDEN.** The emit statements, the exit mapping, the classification ladder, retry counts, droid eligibility and the 200–204 map are TEXTUALLY unchanged; what they RECEIVE differs from HEAD in exactly these cases, on every caller below, with or without the CARRIER — so THE TWO DELTAS above remain the CARRIER's only deltas against a run on the same corrected library: **(Δ1)** a same-group descendant that writes to stdout after its leader exits, or lingers — today the capture waits for it (a finite writer's bytes are included; an endless holder hangs until the harness kill, with no terminal status); now its group is reaped within the grace, bytes it writes during the grace still land in the file and bytes after the reap do not, so a HEAD hang becomes the leader's own verdict — a DEFINED outcome, which can be a PASS that HEAD never reached — and an output left empty or invalid is a failed attempt; **(Δ2)** a leader that itself writes more than the cap — today emitted in full; now a failed attempt; **(Δ3)** prompt transport — a reviewer that does not read all of its stdin no longer exposes a pipe-writer status (whether HEAD's SIGPIPE handling at `:3451-3459` ever changes an attempt's status is NOT established), and an escapee holding unread stdin with a prompt larger than the pipe buffer no longer blocks the writer; **(Δ4)** teardown budget — each attempt's window becomes its remaining budget minus its launcher's teardown bound, as the #823 backstop already subtracts (`run-review-loop.sh:437-442`), at `resolve-cli.sh:3417`/`:3420` and `:3729`/`:3731`, the first attempt keeping its always-runs rule; the bound is READ FROM LITERALS, NOT MEASURED — `-k 5` plus the 8 s reap grace on the coreutils arm, 5 s plus 2 s on the perl arm — and an attempt that HEAD completes within that final margin now ends 124, CAPTURE GRANULARITY (4)'s existing truncated-budget 124 outcome. **RESIDUAL, NOT CLAIMED FIXED:** a descendant that calls `setsid`/`setpgid` escapes every group signal (#823's residual, `run-review-loop.sh:1551-1557`); the file capture keeps it from holding any wait, but it may outlive the attempt and keep writing to an unlinked file. **THE 60 s PERSISTENCE RESERVE IS NOT PROVEN SUFFICIENT:** THE ENVELOPE's fixed reserve must now also absorb the final attempt's teardown before T4 is written; the literal bounds above fit inside 60 s by arithmetic, but nothing has measured that the teardown, the carrier map and T4 persistence together complete within it, and the acceptance below measures it rather than assuming it. **ATTEMPT ATTRIBUTION — ONE SOURCE, TWO VIEWS (native round-15 issues[4]; C6):** an attempt's LAUNCHED BACKEND is the literal label of the site that launched it — `codex` for `_execute_codex`'s loop attempts (`:3812`, `:3822`), `droid` for the escalation (`:3956`, `:3963`) and for direct droid (`:4536`), and `_run_review_with_retries`'s own label argument for its attempts (`agy`, `grok`, `opencode`) — stamped by the dispatcher on EVERY attempt, failed and superseded ones included, and never taken from reviewer output, the environment or `bypass-log.jsonl`; `builtin` (`:4899`) launches nothing and carries only a dispatch terminal record. A page's SERVING BACKEND is the LAUNCHED BACKEND of the attempt whose stdout is emitted with status 0, and the REQUIRED CHANNEL above is its ONLY carrier: the channel's value MUST equal that attempt's LAUNCHED BACKEND literal, so it carries the site literal across the capture boundary and is not a second source; a failed or superseded attempt carries its LAUNCHED BACKEND with its terminal classification (CAPTURE GRANULARITY (4)) and is never a SERVING BACKEND, and a failed dispatch still records no serving backend (T2). **CALLERS AND ENTRY PATHS REACHED — D5, Photon technical default: ONE launcher behaviour for every caller, per the #823 anti-drift header; CONSEQUENCE: Δ1–Δ4 apply on each of them:** the litmus driver (`run-review-loop.sh:3256`, under the CARRIER); the blueprint reviewer slots (`run-design-review-loop.sh:926`, `:998`, `:1072`, each `> raw 2>&1` inside `( … ) &` followed by an unbounded `wait` at `:1278-1280`, which C1 and C2 bound through the inner launches), the blueprint droid rescue (`:320`) and the Mechanism Witness (`:1238`, through `:5097`) — none of them gains the CARRIER; `skills/dispatch-cli/scripts/dispatch.sh` (`:1248`, `:1416-1432`, `:1458`, `:1670`, `:2331`, `:2593`, `:2717`; `_portable_timeout` when sourced, `:215-216`), `scripts/lib/ultra-oracle.sh` (`:143`, `:146`, `:1332`, `:1473`) and `scripts/ci/run-shell-tests.sh` (`:342`), each through C1 only; and THE ENVELOPE's scanner and markdown stages, which delegate to the same launcher. The blueprint harness budget text (`skills/blueprint-review/SKILL.md`) gains one teardown bound per INDEPENDENT budget (the droid rescue and the witness; inside a shared retry budget Δ4 already subtracts it) — an implementation target whose arithmetic is from literals, not measured. **ACCEPTANCE — RUNTIME, `2a-env`'s, OFFLINE (no reviewer, provider or registry call); each fixture states its HEAD outcome, and one HEAD already passes is labelled a regression guard:** the real `_portable_timeout` on both arms with a stub leader that exits 0 leaving a descendant ⇒ returns within the grace with the group gone (HEAD waits on the descendant); a TERM-ignoring leader ⇒ the group gone and THE STATUS ITS OWN ARM REPORTS — **124 on the perl arm and 137 on the coreutils arm**, which `timeout -k` returns when it has to KILL (`run-review-loop.sh:1217-1223` documents the divergence for the same event; `:1655-1662` treats both as terminal) — asserted PER ARM and NOT normalized (regression guard; CORRECTED 2026-09-16 post-16, native run `5e995822` MEDIUM, conf 0.95: the earlier text asserted 124 on both arms, which is false at HEAD and was never measured). **HEAD BEHAVIOUR, STATED NOT CHANGED:** the retry engines (`resolve-cli.sh:3497`, `:3853`, `:2649`) and the driver (`run-review-loop.sh:3368`) test only `-eq 124`, so a 137 takes their existing non-124 branches; this correction adds NO normalization, so there is no new Δ and no OOM-kill misclassification, and the per-arm status is an implementation-time runtime fixture; TERM to the launcher mid-wait ⇒ the group reaped and status 143 (HEAD leaves the group); the REAL `_run_review_with_retries` (agy on the review arm, grok on the non-review arm), `_execute_codex`'s direct path and the droid escalation, each with a stub reviewer that prints a valid review and leaves a `setsid` escapee holding stdout ⇒ returns within its window with exit 0 and output equal to the same stub without the escapee (HEAD blocks for the escapee's life); a stub writing cap + 1 bytes ⇒ a failed attempt, never success and never truncated; a stub whose same-group child writes after the leader exits and past the grace ⇒ those bytes absent (Δ1 pinned); the litmus driver with a TERM-ignoring reviewer that never answers, at a short `LITMUS_TIMEOUT` ⇒ T4 persisted before the cap, with the elapsed teardown-plus-persistence time recorded as the reserve measurement; the perl launcher text in the library and in the scanner stage's direct entries byte-equal; and, once the channel lands, the channel's value equals the emitted attempt's LAUNCHED BACKEND for every writer, while item 2-A's per-attempt fixtures (gated on item 1) show transient, transient, success ⇒ three `codex` attempts with only the last accepted, and Codex 124 then droid success ⇒ `codex` attempts plus one accepted `droid` escalation. `tests/test-pr-dual-voice.sh`'s perl-body extraction is retargeted to the library string. RUNTIME IMPLEMENTATION PENDING: none of C1, C2, Δ1–Δ4 or ATTEMPT ATTRIBUTION exists at HEAD. THREAT SCOPE (supported assumption, not a same-UID isolation claim): the emit branch runs in-shell in the driver-owned subshell and the status reaches the driver only through `wait`; a reviewer child controls its own exit code, environment, stdout and inherited descriptors, none of which sets a variable in its parent shell — the same process-memory boundary the #803 pin already relies on — and its exit code is folded into failure by the map. A same-UID process that attaches a debugger to the driver is a different effect, neither addressed nor claimed. The reopen-by-path obligation was introduced during this recovery, not by the original c050503d acceptance; with no descriptor it is moot and is removed. RUNTIME IMPLEMENTATION PENDING: the stubs are not the library; the four writers, the carrier and the driver map are unimplemented at HEAD, and `2a-env`'s acceptance owns their runtime proof. OWNERSHIP IS UNCHANGED: the channel is `2a-env`'s alone and needs no item 2-A capture site, so `2a-env` does not wait on item 1; item 2-A's per-attempt instrumentation (attempt outcomes, retries, run-identity propagation, sink) stays 2-A's, gated on item 1, and EXTENDS this record without adding a second SERVING-backend source (its per-attempt `backend` is the LAUNCHED BACKEND of ATTEMPT ATTRIBUTION above) — failed, superseded and non-completing pages are retained, and the chain value is derived from those records, never written over them. **TERMINATION IS BY THE OVERFLOW ROW'S EXPLICIT BOUNDS, WITH NO CLAIM THAT A REVIEWER'S FINDINGS ARE FINITE:** a chain ends at the first of **(T1)** a page states `complete` ⇒ chain `complete`; **(T2)** a page exhausts the EXISTING bounded retry ladder ⇒ chain `errored` — where a FAILED PAGE ATTEMPT on that ladder also includes a page stating any value other than `complete` or `truncated`, a page whose `completeness` is missing or invalid, a page whose extraction fails, a page refused by THE ENVELOPE's binding rule, and a page that THE ENVELOPE's page validator refuses under its withheld-key manifest (enumerated once there, cited not restated; 2026-09-13, Photon continuity-closure decision; UNREVIEWED); the continuation driver counts these itself, because the retry engine treats non-empty exit-0 output as success (`resolve-cli.sh:3829-3830` at `727d4652`), so T1–T4 are TOTAL over every response, and every such path keeps the earlier pages and never yields `complete` (CORRECTED 2026-09-13, native 2a9a6900 finding [6]; UNREVIEWED); **(T3)** the chain reaches the hard page cap P of constraint (2) with a page still stating `truncated`, or THE ENVELOPE's size bound shows earlier that its outstanding withheld keys exceed what the remaining pages can emit ⇒ chain `cap_exhausted` per constraint (4); **(T4)** the OVERALL deadline of constraint (5) — the chain's one shared remaining `LITMUS_TIMEOUT` budget, including a fallback that budget cannot fund — fires mid-continuation ⇒ the findings captured so far are RETAINED and the chain is `truncated`, or `cap_exhausted` per (4). No retry, page count or deadline is unbounded; P and the deadline are resource bounds whose values and versions the protocol artifact records; and a chain stopped by T2–T4 is NEVER `complete`. Within one page bound (ii)(d)'s last-accepted-attempt rule applies; across pages the findings union, and the slot's value is the chain's terminal value. **THREE QUANTITIES KEPT APART:** TRANSPORT completeness (did the chain end by T1) is what this bound delivers; DETECTION RECALL (whether a stochastic reviewer found every real defect) is NOT measured and NOT claimed — a `complete` chain certifies nothing about discovery; SAMPLING BIAS (whether eligible observations differ systematically from all observations) is NOT resolved by this bound. **UNRESOLVED, AND BLOCKING THE NO-BIAS CLAIM:** chains ending by T2–T4 stay ineligible and retained, and nothing here shows their rate is zero or independent of review yield — so — BY THE GRAPH NODE'S SINGLE DEFINITION, CITED, NEVER REDEFINED HERE (CORRECTED 2026-09-16 post-16) — `protocol-approved` may not claim item 2p's no-sampling-bias acceptance until the protocol artifact DECLARES, per contract version, the count of chains ending by T2–T4, each one's terminal value and the findings accepted before its stop — retained as a CENSORED stratum that never counts toward N — and the sampling rule that governs them; **`protocol-approved` is UNMET while that declaration is absent** (R2, 2026-09-13; UNREVIEWED). **The declaration is REPORTING plus an operator decision under the existing N / sampling-rule authority (*Still needs Chris* #15, cited): it is NEVER evidence that no sampling bias exists and NEVER an automatic waiver**, and the operator may reject it in favour of a different N or sampling rule. With one shared overall budget, T4 is structurally more likely for high-yield reviews, and no plan text removes that. The accepted direction proves no empirical milestone and no producer capability. **WHAT STAYS BEHIND `baseline captured` (the convergence-policy half):** the `cap_exhausted` SCOPE-REDUCTION recovery, the rest of the prompt rewrite, and every reader that REFUSES on the value (2i(B) Stage 2). The aggregate FOLD and its total precedence are defined once at 2i(B) (c-4) and cited, not owned by either half. **IT ADDS NO EDGE:** `2a-env` is already a graph prerequisite of `baseline captured`, already a list entry in that milestone's ONE authoritative prerequisite list, and already a collection-time conjunct of the promotion rule, so the ordering holds on the graph as it stands and the existing ACYCLICITY CHECK covers it unchanged. **(2-ORD) IS SATISFIED, NOT RE-OPENED, AND THIS IS THE REASON THE SPLIT IS LEGITIMATE:** (2-ORD) forbids a producer emitting a token before the reader that treats it as non-authorizing, and here **no reader refuses on a stated `completeness` value before Stage 2** — the only pre-baseline consumers are 2-A's CAPTURE and the continuation driver of (c), which write records and re-dispatch but grant nothing; a chain that driver does not finish (T2–T4) ends its run through the loop's EXISTING non-authorizing `infra_failure` exit, that is the driver's IN-RUN CHAIN CONTROL — its own retry ladder (T2), its own page count against P (T3) and its own deadline (T4), over responses it requested in this run — which, like today's reviewer timeout (`run-review-loop.sh` exit 124 at `727d4652`), withholds authorization from an unfinished run and never grants on a value: a live response's `completeness` decides only whether the chain CONTINUES, never whether a RECORDED artifact is honoured, so the driver is not the first authorizing reader (defined once at item 2i(B) Stage 2; T3 wording resolved 2026-09-13 under Photon's remaining-contract decision; UNREVIEWED), and the findings-based PASS/FAIL may change because the drip is deleted and pages accumulate (CORRECTED 2026-09-13, native 2a9a6900 finding [0]; UNREVIEWED — this read "no authorization outcome changes at all", which the OVERFLOW fixtures bound in `2a-env` contradict), so there is no interval in which a value travels past a gate that ought to refuse it. **SCOPE BOUND, so the statement cannot grow back into the transport it was split from:** `2a-env` owns stating, collecting and recording the value (the delta above); any reader that REFUSES on it is the convergence-policy half's and stays behind `baseline captured`. (CYCLE BROKEN 2026-09-12, native finding [08] MEDIUM: the deleted clause deferred (i-a)'s concrete shape to a decision that is itself gated by `baseline captured`, whose graph prerequisite is `2a-env`, whose prerequisite is (i-a) — this cell's existing FORMAT-NEUTRAL / lands-first sentence is CITED here, not restated, and no new node, envelope type or gate is introduced; UNREVIEWED.) **BOTH PRODUCERS ARE IN (i-a)'s SCOPE — EXTENDED 2026-09-09 (`全部批准`, round-14 finding [5]; UNREVIEWED). WHY THIS IS ENTAILED RATHER THAN ADDED, stated so a reader can check it instead of taking it:** the approval directs a cutover rejection of legacy markers *lacking proof of genuine executed review*, and a marker can only be said to LACK that proof against a world in which genuine mints CARRY it — so `:3585` acquiring a proof is the approved sentence's own presupposition, not a new requirement. It is nonetheless recorded plainly that this **widens (i-a)'s enumerated producer set from one site to two**, which is the single place this correction phase touched the enumerated scope of a decided bound. It contradicts no separately decided policy: #11 = (A) decided that provenance MUST be distinguishable *in the artifact*, a symmetric requirement over the pair, and the narrower "the short-circuit mint gets its own form" was a restatement of that bound rather than an independent decision. **RULED 2026-09-10 — PHOTON TECHNICAL CONFIRMATION, NOT A NEW CHRIS APPROVAL.** Photon confirmed the two-producer enumeration as a **necessary producer consequence of the EXISTING requirement** — Chris #11 = (A) plus the approved legacy bare-marker refusal — on the reproduced source facts: `run-review-loop.sh:3115` and `:3585` both mint a bare 64-hex marker, and the reader at `pre-commit-gate.sh:955` accepts it. That is a technical confirmation of what the already-decided requirement entails, and it is **explicitly not** a fresh approval, not a widening of Chris's decision, and **not authority over the envelope's general design**, which stays open exactly as this cell already records. The ordering is unchanged and load-bearing: **(i-a) stays FORMAT-NEUTRAL and lands first; the refusal stays in (i-b) and lands after**; the refusal remains **read-side only**, deleting no live marker or token. `:3585`, the executed-review mint, must ALSO stop writing a bare 64-hex hash and must carry an affirmative **proof of executed review**, in the same commit as `:3115`'s change and the readers. Distinguishing `:3115` alone does not suffice for the cutover refusal that (i-b) now carries: if `:3585` kept minting bare, the bare form would name the legacy population **and** every post-cutover genuine mint at once, and no reader could refuse the one without refusing the other. With both producers changed, **`bare 64-hex` means exactly `pre-cutover legacy` — FOR `litmus-passed.local` AND THE EMBEDDED COMMIT/MERGE CLAIMS THAT CARRY ITS BYTES** — a predicate decided by FORMAT **within that scope**, needing no timestamp, no mtime and no clock. **SCOPE STATED 2026-09-10 (round-16 finding [10]; Photon technical decision, UNREVIEWED): it is NOT total over every bare-hex marker in the repo.** The PR marker written at `run-review-loop.sh:784` and read by `pre-pr-gate.sh`'s bare-hex arm is a separate population with its own producer, and this cutover does not migrate it. If PR markers are also meant to migrate at `2a-env`, that producer and those readers must be enumerated in this cell first; until they are, they are OUT OF SCOPE and the two producers named here are the whole scope. **READERS: the gate-side honouring set DEFINED ONCE in this cell** — no separate list is created here, and the membership rule ("every site that READS OR CLASSIFIES MARKER CONTENT, by whatever channel") already covers the arms that must learn the new form, including `dispatcher-commit-block.sh:946-948`, which reads the `litmus-passed.local` commit-marker artifact. **MEMBERSHIP IS NOT ACCEPTANCE — QUALIFIED HERE, AT THE OWNING NODE, 2026-09-12 (same finding [11]; UNREVIEWED):** for that one site "learn the new form" means CLASSIFY the `BUILTIN-<hash>-complete` envelope and **CONTINUE TO REFUSE IT** — pr-grind's external-CLI review-PASS requirement is untouched by this plan. The reason is given ONCE at (c-1b) and is deliberately not restated here; without this qualification the membership rule read alone would license a `2a-env` worker to "fix" `:947` into honouring the envelope, which is the opposite of what the cutover requires. **THE HONOURING SET IS PARTITIONED BY ARTIFACT, NOT BY MARKER SHAPE — CORRECTED 2026-09-12, native finding [10] MEDIUM; UNREVIEWED.** The commit-marker cutover owns the `litmus-passed.local` readers and ONLY those. `hooks/gate-scripts/pre-pr-gate.sh:434-448` and `:449-468` read the **PR-marker** population — `:434-448` the bare-hex marker written at `run-review-loop.sh:784`, `:449-468` the `PASS-FAST` marker written at `:3563` and the `PASS-EXCLUDED` marker written at `:2649` (attribution CORRECTED 2026-09-13, native b5bddf50 HIGH, verified at `727d4652`: `:784` writes the bare hash only) — all of it PR-marker population that the SCOPE sentence above puts OUT OF this commit-marker cutover; the PASS-FAST marker's Stage 2 completeness obligation is at its MINT, as the parent's authorization-artifact invariant states it: `PASS-FAST` is minted only when the Codex lead review's `completeness` is exactly `complete`, and Stage 2 refuses the marker at its reader by FORM, as it does every review-derived form other than (i-a)'s (the parent's #12), not for its missing `completeness` field (aligned with the parent 2026-09-24; UNREVIEWED) — so **adding them under round-17 finding [8] contradicted that sentence inside this one cell, and the addition is WITHDRAWN.** They stay on the PR-marker side until that migration is separately scoped and its producer and readers are enumerated here, which this correction does NOT do. The membership rule ("every site that READS OR CLASSIFIES MARKER CONTENT, by whatever channel") is unchanged and still governs *within* each artifact's population: omitting a gate-side reader of the commit-marker artifact would still be the fail-open this row names. The existing control is likewise unchanged and is NOT relaxed by the partition — a fresh commit-review proof MUST NOT authorize PR creation (`:476-477`, ADR 0006). **The producer and every reader change in the SAME commit**; a distinguishable form honoured by a subset is a fail-open, since an unrecognizing reader falls back to the bare-hash accept path. **GATE: this is the INTEGRITY half's concern and rides item 2a's envelope change** — it therefore lands with the marker-format work and NOT behind `baseline captured`, which gates the convergence-policy half only. **It lands on the `2a-env` node**, added to the dependency graph 2026-09-09 (round 11) for exactly this work and defined once there — its gate, its separability rule and its acceptance are cited from that node, never restated here. An earlier version of this sentence said the requirement "creates no new node on the dependency graph", which left (i-a) and (i-b) with no schedulable home: (i-b) changes an authorization outcome, and the only node this cell assigned them to forbids that.

---

## Moved from the parent 2026-09-24: § *Item 2a*

> **MOVED VERBATIM (2026-09-24; Chris-approved shrink; plan-only, UNREVIEWED).** The text below was the parent's § *Item 2a*, at parent spec `8bfd530e…`; the parent keeps a one-line pointer to this file at its place. It binds as part of this file and is reviewed with this file's spec hash; a verdict on the parent does not cover it. Inside it, "this file", "this document", "above", "below" and line anchors `:NNN` into the parent refer to the parent as it stood before this move.

**Litmus convergence contract** (new in #840, **verified and strengthened**): make one executable prompt source authoritative. **Three sources, and the conflict is narrower than earlier drafts of this row claimed.** The live prompts at `skills/litmus/scripts/init-review-loop.sh:464`/`:554` already say "Maximum 10 issues per review. Prioritize by severity, then confidence" — the *same* output budget as `skills/litmus/prompt_template.txt:61` — and then `:466`/`:556` immediately add "Maximum 3 new issues per iteration to ensure convergence". So the 10-cap is **not** in conflict; the only contradiction is the 3-new drip against the template's extra clause "Report ALL issues upfront — do not hold back findings for later iterations". A third source was missed entirely: `skills/litmus/SKILL.md:641` also states "Maximum 3 new issues per iteration". **The template still has zero code consumers** (nothing under `skills/`, `hooks/`, `scripts/`, `tests/` reads it; only `skills/litmus/references/script-reference.md:288` and `advanced-usage.md:108` describe it as live), so the defect is: drip-feeding in the live path, doc drift in two references, and a fourth statement of the drip rule in the skill body. **Disposition for the orphaned template itself — stated here because an earlier draft diagnosed the orphan and then left its removal unauthorized and unscoped.** `.upstream-sources.json:1745` records `"path": "skills/litmus/prompt_template.txt"`, so deleting the template — the obvious cleanup for a consumer-less file whose text actively contradicts the live prompt — trips `test-upstream-manifest.sh:104-105` exactly as item 13/M9 does. This is the **second instance of the same manifest collision**, so answer it once: **either delete the template and remove its manifest row in the same change** (per invariant 6's own remediation text and item 4's rule above), **or keep it and add a header marking it non-authoritative with a pointer to `init-review-loop.sh`.** Name this file in item 4's precondition so the manifest question is answered once for both items rather than discovered twice. **Boundary with item 8, stated because the two rows otherwise contradict:** "never an automatic PASS" here means the *reporting* contract — an incomplete or errored review must not be recorded as a clean one. It does **not** mean every unresolved finding blocks: `lib/merge-findings.py:67` already downgrades LLM `medium` to advisory from iteration 3, so a medium open at iteration 3 does not block today. 2a must preserve that behaviour exactly; changing it is item 8's decision, after item 2's data. Replace drip-feeding with all known actionable findings inside the output budget and an explicit incomplete-report state. **The drip deletion, the per-response incomplete-report statement and complete-result collection across responses land in `2a-env` ahead of the baseline under item 2a bound (2-CAP), where they are defined once (moved 2026-09-13 under Chris's `go` on the measurement target; UNREVIEWED); this row keeps the rest.** **That state is not a prompt change — it is a field with consumers.** Name it, fix its permitted values, and carry it through every stage that can drop it: the extractor, the schema validator, the merger, and the marker writer. An incomplete result must prevent marker creation **independently of severity** — otherwise a truncated review with zero parsed findings still reads as clean, which is the exact laundering `lib/merge-findings.py` cannot catch because it only ever sees the findings that survived extraction.

**Name the field, or it will be built twice.** An earlier draft required "a field with consumers" without naming it, its values, or its relationship to the machinery that already exists — which leads to either a second competing status channel or an "implementation" that overloads `run-review-loop.sh`'s `terminal_status=infra_failure`, a value that does **not** describe this defect (a well-formed `{status: PASS, issues: []}` produced by the 3-new drip cap at `init-review-loop.sh:466`/`:556`). Specify: **`completeness`, exactly one of `complete`, `truncated`, `errored`, `cap_exhausted` or `unstated`** on the reviewer JSON, explicitly **distinct from loop-level `terminal_status`**. (Written with commas: the first draft used the pipe form, and a literal pipe inside a table cell splits the row — this cell carried two of them.) Map the existing paths onto that enum, including `parse-narrative.py`'s current None-on-extraction-failure behaviour. **Two of the five values were added 2026-09-08, and they are declared HERE because this is where the enum lives, not in the rows that motivated them.** `cap_exhausted` is this row's own (see the cap-exhaustion outcome below). `unstated` is item 2i(B) stage 1's producer-did-not-state sentinel — an artifact whose producer predates the field, or a narrative parse that never had one to carry. **Neither is `complete`**, so stage 2's existing rule (missing or not `complete` fails closed) already covers both and no additional refusal rule is owed. **Marker rule, restated over the CLASS of authorization artifacts rather than one filename — the single-file form left the entire PR gate outside the contract.** Verified at the checkpoint: PR-mode authorization never reads `litmus-passed.local`. `write_codex_lead_verdict()` (`run-review-loop.sh:735-746`) emits `pr-codex-lead.local.json` as a `printf` of `{"status","model","diff_hash","ts"}` — **no `issues` array and nowhere to put completeness**; `verify_pr_artifact()` (`:710-722`) checks only status/diff_hash/ts freshness; and `--write-pr-marker` (`:748-785`) mints the PR marker from those two persisted artifacts. **Invariant: no review-derived authorization artifact — `litmus-passed.local`, `pr-codex-lead.local.json`, the PR backstop verdict, or `pr-review-passed.local` — may be minted or honoured unless every executed review carries `completeness == complete`**, regardless of `issues[]`. **One rule for both forms, the `PASS-FAST` form of `pr-review-passed.local` included:** that marker has no `completeness` field and is not the (i-a) form, so Stage 2 refuses it at its reader by FORM (the parent's #12); its MINT is gated as well. The `:3563` writer mints `PASS-FAST` only when the Codex lead review's `completeness` is exactly `complete`, as the `:3585` writer does for the executed-review marker (Stage 2 producer cutover; the mint exemption is withdrawn 2026-09-24; plan-only, UNREVIEWED).

**Two bounds on that invariant, both stated because the unqualified form collides with existing behaviour.** **(i) Scope — it binds only mints whose authority derives from an EXECUTED review, and the no-review mints are governed by a SEPARATE, STRICTER rule rather than simply exempted. Reconciled 2026-09-08; the earlier text said only "out of scope" and left this row asserting both that `:3115` must fail closed on `completeness` and that it need not.** Artifacts minted on a configured **no-review** path — `SKIPPED-NONE` (`:1852`), `PASS-EXCLUDED` (`:2649`/`:2745`), and the short-circuit synthetic PASS (`:3115`) — are outside the *completeness* invariant, because there is no review to be complete or truncated and demanding `completeness == complete` of them would either block a configured path or force a synthetic `complete` that means the opposite of what the enum says. **They are NOT thereby unconstrained.** Two rules replace the bare exemption.
**(i-c) SHORT-CIRCUIT PRECONDITIONS — THE SCANNER COUNTERS. FOLDED IN FROM DECISION [13] 2026-09-10 (round-17 finding [3]; Photon technical decision, UNREVIEWED).** Binding contract moved verbatim to `docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-i-c-scanner-counters.md` (2026-09-23 split; UNREVIEWED — the review loop does not read it). It remains part of this plan's requirements.
**(i-a) BINDING REQUIREMENT — IN SCOPE; *Still needs Chris* #11 REOPENED and ANSWERED (A) 2026-09-09 by Chris (`批准840重開11並納入計劃`), reversing the earlier (B) "out of scope" (retained as provenance in that row). Provenance MUST be distinguishable in the artifact.** Today it is not: `run-review-loop.sh:3115` (short-circuit) and `:3585` (executed review) both run `printf '%s\n' "$REVIEWED_DIFF_HASH"` into `litmus-passed.local` and are **byte-identical**, under a comment at `:3109` reading "Write commit marker (same format as normal PASS)". No reader can apply this bound to an artifact it cannot tell apart, so "say which path a mint came from" is a **deliverable**, not a description: the mints must become distinguishable from each other, and every reader enumerated above is changed in the same commit. **Which producers that touches is stated once, in the PRODUCER clause below, and is not restated here — NARROWED TO A POINTER 2026-09-09 (`全部批准`, round-14 finding [5]; UNREVIEWED)**, because this sentence read "the short-circuit mint gets its own distinguishable form", naming one side of a scope the same cell now defines as covering both.
**PRODUCER, READERS AND GATE, named here because a requirement without them is the "named deliverable owned by no half" defect this document repairs elsewhere.** Binding contract moved verbatim to `docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-i-a-producer-readers-gate-and-2-cap.md` (2026-09-23 split; UNREVIEWED — the review loop does not read it). It remains part of this plan's requirements.
**(i-b) BINDING REQUIREMENT — IN SCOPE; #11 REOPENED and ANSWERED (A) 2026-09-09 by Chris — a short-circuit PASS cannot ALONE authorize a commit, and never substitutes for full commit verification. COMPLETE-VERIFICATION REQUIREMENT: a commit whose only authorization is a short-circuit mint is NOT authorized; it requires a full executed review.** **CUTOVER REFUSAL OF LEGACY BARE MARKERS — CHRIS-APPROVED 2026-09-09 (`全部批准`, round-14 finding [5]); PLAN TEXT ONLY, UNREVIEWED. It is a REFUSAL, not a grace period: no expiry, no window, no clock, and nothing that ages out.** At (i-b)'s activation, every marker still in the legacy **bare 64-hex** form is **refused as authorization** — it carries no proof of a genuine executed review, and under (i-a) nothing minted after the cutover looks like it, so the shape identifies the legacy population exactly. **THE LEGACY POPULATION IS KNOWINGLY INDISTINGUISHABLE AND IS REFUSED WHOLESALE — stated plainly rather than papered over, because it is the whole difficulty of this rule.** Today `:3115` and `:3585` write byte-identical markers under a comment at `:3109` reading "same format as normal PASS", so a legacy mint produced by a real executed review and one produced by a short-circuit **cannot be told apart by any reader**, and no field exists in which to triage them. The refusal therefore discards genuine reviews together with the short-circuit ones; that cost is accepted deliberately, since the only alternative — honouring the class — is honouring every short-circuit mint inside it, which is precisely what (i-b) exists to stop. It refuses what cannot be proven, and does not assert that any particular marker was illegitimate. **THE REFUSAL REACHES PENDING MERGE CLAIMS — EXTENDED 2026-09-10 (round-15 finding [16]; Photon technical decision under delegation, UNREVIEWED, and NOT a fresh Chris approval).** A cutover that refuses the marker but honours a merge claim already carrying the same legacy bytes is not fail-closed; it is the same authorization arriving by a second door. So: **at (i-b)'s activation, any pending merge claim whose EMBEDDED marker content is in the legacy bare form is refused as authorization.** The refusal is **read-side only and destroys no evidence** — the claim file itself is left untouched and auditable, exactly as for the markers; nothing is unlinked, expired or rewritten. **FRESH-REVIEW CONTROL (the fixture that makes it falsifiable):** arm a claim carrying legacy bare content BEFORE the cutover, then assert that after activation its publication is REFUSED, that the claim file still exists and still reads its original bytes, and that a claim re-armed from a fresh executed review of the SAME diff — carrying (i-a)'s proof — publishes normally. The pair is required: refusal alone would pass against a gate that had simply stopped working. **REQUIRED REVALIDATION is the recovery, and it is ordinary:** re-run the review on the same diff, and a fresh executed-review mint carrying (i-a)'s proof authorizes normally. The cost is one re-review per affected diff — the same cost 2i(B) already accepts elsewhere. **THIS RULE IS READ-SIDE ONLY: no live marker, token or artifact is deleted, expired, rewritten or back-filled by it.** The honouring set declines to accept a shape; every existing file stays on disk and remains auditable. **ORDERING:** this is an authorization change, so it belongs to (i-b) and lands **after** (i-a), never before — which is also what closes the hazard this row already names, since the "unrecognizing reader falls back to the bare-hash accept path" fall-open is exactly the path the refusal converts into a fail-closed one. **THE RECOVERY PATH IS NAMED, ADDED 2026-09-09 (round 11):** re-running `/litmus` on the same eligible diff takes the same branch and mints another unusable marker — the short-circuit branch writes the marker, logs the event, clears iteration history, prints its next-steps text and exits 0 (checkpoint `:3096-3128`, HEAD `:3084-3119`). The mechanism that escapes it already exists and this plan had named it **zero** times: **`LITMUS_SHORTCIRCUIT_DISABLED=1`, read at the branch guard itself (HEAD `:2991`, checkpoint `:3001`)**, which forces the full executed review that (i-b) now requires. Naming it is what makes (i-b) a requirement a worker can satisfy rather than a dead end; the earlier reviewer claim that (i-b) created an unresolvable deadlock was WRONG on the mechanism and right only about this document's silence. The earlier "DECIDED 2026-09-08" was struck (round 9, finding [1]) because this same cell then stated that the distinguishable form was "NOT decided by this row and … NOT decided anywhere else in this plan", and both could not hold; the first answer to #11 resolved that by declaring (i-a)/(i-b) out of scope, and **that resolution is now itself superseded — the form IS decided here, so the two claims are reconciled in the other direction: the closing "not decided anywhere else" sentence is withdrawn below rather than the bounds being demoted.** This is a deliberate tightening of live behaviour: `scripts/dispatcher-commit-block.sh` rejects `SKIPPED-NONE*`, `DEGRADED*` and `BUILTIN-*` by prefix (`:946-948`) but, because the short-circuit mint is byte-identical to a real one, today accepts it at `:1076-1084` as a genuine external-CLI review PASS. Once (i-a) lands, that acceptance ends. **State the direction plainly so no one reads (i) as a loophole: the no-review paths are exempt from the completeness invariant and MORE constrained, not less, in what they authorize.** Apply the completeness invariant to the review-derived set only. **(ii) Aggregation — "every executed review" is per required reviewer SLOT, not per dispatch ATTEMPT.** Completeness is evaluated over the **final accepted attempt** for each required slot; a superseded attempt is retained for item 2's ledger but does not gate authorization; and a slot that exhausts its retry budget without producing a `completeness` value is **`errored`**, not absent — so it fails the invariant rather than silently dropping out of the aggregate. **THE BRIDGE TO THE FOLD WAS MISSING AND IS SUPPLIED 2026-09-12 (native 4efaea28 finding B5 `completeness`, conf 0.7; UNREVIEWED).** This bound speaks in SLOTS; the (B)(c-4) fold that implements it is defined purely over SUPPLIED inputs (`FOLD PARTICIPANTS are REVIEW-ENVELOPE inputs only`), and nothing said how one becomes the other. The interface makes the gap concrete rather than theoretical: the merger receives positional argv or concatenated stdin and `_ingest` carries **no slot identity at all**, so it cannot distinguish two envelopes from ONE slot from one envelope each from TWO. The all-absent case is already covered (`no participants ⇒ unstated`); the **PARTIAL** case is the hole — two `complete` envelopes standing in for three required slots aggregate to `complete` and AUTHORIZE. **Therefore: (a)** the expected-slot SET is supplied by the loop, which already resolves the required reviewer roles, and is passed to the merger alongside the envelopes, so the fold runs over **REQUIRED SLOTS, not over supplied inputs**; **(b)** each input carries its **slot id**, which is how an envelope binds to a slot; **(c)** a required slot with **zero** results is **`errored`** — the same doctrine this bound already applies to retry exhaustion, extended to "never dispatched" and "output never arrived", which are indistinguishable at the merger and must not differ in effect; **(d)** for a slot with **two** accepted results **for the same page dispatch**, **the LAST ACCEPTED ATTEMPT WINS; earlier attempts are retained for item 2's ledger only and never participate in the fold.** Pages of one continuation chain are not competing attempts: their findings union and the slot's value is the chain's terminal value (bound (2-CAP), cited; scoped 2026-09-13). **(d) NOW STATES ITS RULE INSTEAD OF DEFERRING IT, AND THE INTERFACE HAS AN OWNER — CORRECTED 2026-09-12 (native c94fb702 finding [8], grok issues[3] `completeness`, conf 0.85; UNREVIEWED).** As first written (d) read "BLOCKING until superseded-attempt selection is stated" — an unresolved deliverable left inside a correction presented as complete, which is the same defer-and-never-decide shape finding [0] punished. It is decided here on the doctrine this bound already applies: completeness is evaluated over the **final accepted attempt** per slot, so the last accepted attempt is precisely what the fold must see. **OWNER OF THE SLOT-IDENTITY INTERFACE: Stage 1 — BOTH halves, shipped together in one change.** That means the `_ingest` signature gains a slot channel (today it is `(parsed, sink)` with no slot identity — verified at `lib/merge-findings.py:91-96`, so the merger cannot distinguish two envelopes from one slot from one each from two) **and** the loop passes the expected-slot set, in the same commit. Neither half may land alone: the fold without slot identity leaves Stage 2 honouring two envelopes for three slots, and slot identity without the expected-slot set makes every merge `errored`/`unstated` — harmless only while no reader consults the field, and not harmless the moment Stage 2 lands. **This changes no fold PRECEDENCE and adds no verdict state** — it names the domain the existing rule is evaluated over. **Fixture:** two `complete` envelopes where three slots are required ⇒ **NON-authorizing**. **ONE SCANNER-TO-MARKER REGRESSION ALSO JOINS `2a-env`'s OWN ACCEPTANCE — ADDED 2026-09-12 (native c94fb702 finding [5], codex issues[2] `completeness`, conf 0.85; UNREVIEWED).** The (2-ORD) split moved the minimal non-authorizing reader into `2a-env` but left **every item 8 fixture with item 8, behind `baseline captured`** — and bound (i-c)'s own scanner fixtures assert only that the short-circuit does NOT fire. So nothing pre-baseline exercises the path that runs *afterwards*: a failed scanner can bypass the short-circuit, accompany a clean executed LLM review, and still mint an authorization artifact if merger-side rejection is defective, with every check that would catch it (M1–M2, O1, X1–X4) sitting behind the milestone. **Therefore `2a-env`'s acceptance carries, pre-baseline: one failed scanner plus one clean executed review ⇒ a diagnostic is emitted and NO authorization artifact is minted; and the equivalent all-clean run mints normally** — **on BOTH authorizing paths: the external-review path through the merger, AND the built-in path, where it is item 2i(B) (A-iv)'s fixtures 7 and 8, cited and not restated** (added 2026-09-23, native `0994c559` HIGH; UNREVIEWED — until then this fixture covered only the merger path). **The same holds for a SUCCESSFUL scanner finding that satisfies the deterministic-blocker predicate: on the merger path `determine_status` FAILs it, and on the built-in path it is (A-iv)'s second rule and fixtures 9 and 10, cited and not restated** (added 2026-09-23, native `b344739e` HIGH; UNREVIEWED) — the second half is the positive control, without which the check passes by minting nothing ever. Reuse the relevant item 8 SHAPE-level checks pre-baseline; **severity/confidence POLICY tests stay behind item 8's gate**, which is the split (2-ORD) already drew. Without this rule "every executed review" reads as every attempt, under which a single retried transient makes the aggregate permanently incomplete.

**The consumers, named rather than demanded** (an earlier draft said "list the four consumers", which put a meta-instruction on the critical path of the row it calls startable today): extraction — `parse-narrative.py:87-88` and `:104-112`, and `blueprint-review/scripts/lib/extract_review_json.py:735`; schema validation — `validation.sh:114-164`; merger — `lib/merge-findings.py:91-96` and `:185-188`; marker writers — `run-review-loop.sh:3115`/`:3585` and `write-review-marker.sh:102`, **plus the PR pair `:735-746`/`:748-785`**, **plus the PASS-FAST marker writer at `run-review-loop.sh:3563`** (`printf 'PASS-FAST-%s-%s'`), which has its own acceptance path and is not reached by the two writers above. **Read the no-review mint sites in this list through bound (i), not through the completeness invariant — reconciled 2026-09-08.** `:3115` (short-circuit), `:1852` (`SKIPPED-NONE`) and `:2649`/`:2745` (`PASS-EXCLUDED`) appear here because a marker-format change reaches them, **not** because they must fail closed on `completeness`; what they owe is bound (i-a)'s distinguishable provenance and, for `:3115`, bound (i-b)'s rule that it cannot alone authorize a commit. An earlier version of this row required them both to fail closed and exempted them, which was a contradiction rather than a nuance.

**And the GATE-SIDE honouring path, which the writer-only list omitted — this is the actual enforcement point, so leaving it out makes the whole invariant a fail-open.** `hooks/gate-scripts/pre-pr-gate.sh:291-313` defines its **own** `verify_pr_artifact_gate()` as an inline `python3` heredoc checking only `status == PASS`, `diff_hash == expected` and `ts` freshness. It never sources the loop's helpers, so a completeness-aware writer paired with this completeness-blind gate still honours a truncated artifact. It must read `completeness` itself. Note it **duplicates** `verify_pr_artifact()` (`:710-722`) rather than sourcing it: the two are changed together, or unified in the same change — a divergence here is invisible until it is exploited. **This row's LOCKED deliverables are the three paths enumerated in the ONE global rule's `2a` bullet in *Execution order and acceptance* — cited, not restated here**, and that bullet's `--update` regen and `--check` obligations govern them. **CORRECTED 2026-09-09 (`全部批准`, round-14 finding [9]; UNREVIEWED):** this sentence read "**Every** deliverable in this row lands inside the gate-script content lock", which is false and, being a second enumeration in prose, could drift from the bullet that owns it — the row's envelope, schema and producer work lives outside `hooks/gate-scripts/**` and `scripts/hooks/**` and attracts no regen. The bullet is the authority for which of this row's paths are locked.

**The gate-side honouring set, DEFINED ONCE HERE — every other row cites this set, none restates it.** **PARTITIONED BY ARTIFACT, stated in this file so a standalone read agrees (2026-09-24; plan-only, UNREVIEWED):** `2a-env`'s (i-a) commit-marker cutover owns only the readers of the `litmus-passed.local` commit-marker artifact, including the readers of merge claims minted from it. The `pre-pr-gate.sh` arms read the PR-marker population and are OUTSIDE `2a-env`; they change at 2a convergence-policy / 2i(B) Stage 2, per the gate-script lock bullet. **MEMBERSHIP RULE (the normative definition; corrected 2026-09-09, round 9 finding [3]): every site that READS OR CLASSIFIES MARKER CONTENT, by whatever channel** — file read, shell argument, positional parameter. The earlier rule was "readers of the two marker FILENAMES", and that method is why two real consumers were missed: `hooks/gate-scripts/lib/validate-staged-litmus-marker.sh:366-390` (independently accepts `BUILTIN-<hash>` and bare 64-hex against the staged hash, minting a merge claim on each) and `hooks/gate-scripts/lib/merge_pending.py:1222-1245` (classifies marker CONTENT by shape to choose the ledger event and whether the SHA is appended to `reviewed-commits.local`) — both verified on `fix/issue-622-merge-commit-gate` at `1712e829`, and both receive the content as an argument rather than reading the file. **They are members of this set.** **RE-ENUMERATION IS OWED AFTER ITEM 0 LANDS**, because #622 sits ahead of everything here and can add consumers before the envelope changes; and because native git hooks are INSTALLED SNAPSHOTS, a compatible source commit on main does not update what an observation repository executes — require compatible INSTALLED consumers before activating new writers, and test an ordinary commit and a native merge transaction across the cutover. (Round 8 had already corrected the row for naming one reader while asserting that omitting the gate side "makes the whole invariant a fail-open".) Enumerated by grepping for *readers* of `litmus-passed.local` and `pr-review-passed.local` rather than by listing writers, at the checkpoint:
• `hooks/gate-scripts/pre-commit-gate.sh:943-966` — the **commit-mode** honouring point: `:943-954` accepts a `BUILTIN-<hash>` marker and `:955-966` accepts a bare 64-hex marker, both **by string shape**.
• `scripts/dispatcher-commit-block.sh:940-1085` — a **third, wholly separate** honouring path that independently re-validates the same marker file: it rejects `SKIPPED-NONE*`/`DEGRADED*`/`BUILTIN-*` by prefix at `:946-948`, handles `PASS-EXCLUDED-` at `:952`, and otherwise requires a bare 64-hex hash matching the staged diff at `:1076-1084`. It was named nowhere in this plan.
• `hooks/gate-scripts/pre-commit-gate.sh:777` — the **`SKIPPED-NONE` allow** arm, and `hooks/gate-scripts/pre-commit-gate.sh:871` — the **`PASS-EXCLUDED` allow** arm: the commit-mode readers of the two no-review mints, **added 2026-09-09 (round 9, finding [18])**. **`PASS-MERGE` was checked and is NOT a member — do not re-raise it:** it was retired by #782 / PR #841 (listed under *Merged since the 09-05 check*), and at the checkpoint `run-review-loop.sh:1877-1882` refuses the empty-diff merge ("PASS-MERGE auto-pass is retired") minting nothing, while `pre-commit-gate.sh:808-812` rejects `^PASS-MERGE-[0-9]+$` unconditionally under "Do not reintroduce an acceptance branch". `scripts/design-clear.sh:145-150` names both marker files only to REFUSE draining them, so it is out of scope exactly as `post-pr-consume-marker.sh` is.
• `hooks/gate-scripts/pre-pr-gate.sh:291-313` — the PR-artifact gate described immediately above.
• `hooks/gate-scripts/pre-pr-gate.sh:449-468` — honours `PASS-FAST-<hash>-<epoch>` and `PASS-EXCLUDED-<hash>-<epoch>` on a **regex match** and **deliberately never calls** `verify_pr_artifact_gate`. **Stage 2 CHANGES this reader for the `PASS-FAST` branch (2026-09-24; UNREVIEWED): it refuses a `PASS-FAST` marker by FORM, the parent's #12 rule for every review-derived form other than (i-a)'s.** `PASS-FAST-<hash>-<epoch>` has no `completeness` field, and none is added. **SPLIT BY ARM (2026-09-13, native b5bddf50 HIGH; UNREVIEWED):** the `PASS-FAST` branch is review-derived and is refused as just stated; the `PASS-EXCLUDED` branch is a no-review mint (`:2649`) governed by bound (i), not by the completeness invariant.
• `hooks/gate-scripts/post-commit-consume-marker.sh:154+` — a **post-hook consumer that reads the marker's CONTENT**: it classifies by prefix (`SKIPPED-NONE`, `BUILTIN-`) to decide whether the commit is recorded in `reviewed-commits.local`, making it a LEDGER WRITER whose classification decides that file's content — **corrected 2026-09-09 (round 9, finding [22]): the claim that `pre-pr-gate` reads it to skip the multi-voice review is FALSE at the checkpoint.** `git grep reviewed-commits.local` at `727d4652` returns `post-commit-consume-marker.sh`, a comment in `pre-commit-gate.sh:946`, `marker_check.py:4787` (anti-forge list) and `design-clear.sh`; `pre-pr-gate.sh` contains no occurrence, and its authorization runs on the PR artifacts plus the PASS-FAST/PASS-EXCLUDED markers at `:449-468`. It consumes rather than gates, but a marker format it cannot classify silently changes what that smart path skips.
• `hooks/gate-scripts/post-pr-consume-marker.sh:151-155` — listed for completeness and **NOT** affected: it inspects no content, unlinking the PR marker and both verdict artifacts unconditionally after a successful `gh pr create`. Named here so a future reader does not have to re-derive that it is out of scope.
*(For completeness of the opposite kind: `hooks/gate-scripts/lib/marker_check.py:4738-4739` and `pre-implementation-gate.sh:385/389` name these files in the **anti-forge write-block and writer-hint lists**. They are not honouring readers, they are unaffected by the artifact's field content, and trusted-writer-only remains in force.)*
**Stated as an owed constraint, not designed here:** **the MAJORITY of the sites above — every one that matches a bare 64-hex hash or a literal prefix, which is what the membership rule admits them for — discriminate by string shape or prefix on a bare hash** (**count REMOVED 2026-09-09, round 10, finding [10]:** this read "four of the six sites", a number written next to a set that is *defined* rather than enumerated here and that has since grown — round 9 alone added `validate-staged-litmus-marker.sh:366-390`, `merge_pending.py:1222-1245`, `pre-commit-gate.sh:777` and `:871`. A hard-coded count beside a growing set is the same defect as a restated normative clause: it goes stale silently. **Derive the count from the list when one is needed; never write it here.**), so there is today **no field in which `completeness` could travel** to any of them. Whatever envelope carries it must be honoured at **every** site in this list **in the same change** — `scripts/dispatcher-commit-block.sh` with the TWO-ARM delta defined at item 2i(B) (A-iii), not as a plain honouring reader (2026-09-24; plan-only, UNREVIEWED) — and until that envelope exists this row's settling check cannot be satisfied at those sites. **The envelope's FORMAT is still not decided by this row** — choosing it is a design decision left open deliberately, and no part of this enumeration may be read as settling it. **READER COMPATIBILITY, the one rule a reader may rely on while the format stays open (2026-09-24; plan-only, UNREVIEWED):** a reader NOT changed at Stage 2 still accepts the pre-Stage-2 form it accepts today; the `PASS-FAST` reader IS changed, and refuses that form (the parent's #12; 2026-09-24; UNREVIEWED). **But the second half of that sentence is WITHDRAWN 2026-09-09** (it read: "and the separate question of whether a short-circuit mint becomes distinguishable at `dispatcher-commit-block.sh` … [is] NOT decided anywhere else in this plan"). **Chris REOPENED *Still needs Chris* #11 and answered (A): short-circuit distinguishability IS decided, by bound (i-a) above, and `dispatcher-commit-block.sh` IS one of the readers that must learn the new form.** Format and distinguishability were conflated in the withdrawn sentence; they are separable, and only the format remains open. Read the two together: **the mint must become distinguishable (decided); what the envelope looks like is not (open).** Two of the listed sites need their required check stated honestly rather than assumed: the **merger has no field to check** (it must first carry one — `_ingest` at `:91-96` discards every top-level key but `issues`), and **`validation.sh:162-163` is fail-OPEN by design**, so "fail closed" there is a behavioural change carrying its own acceptance, not a conformance check.

**Overflow has a contract, because "report everything inside the output budget" is unsatisfiable at eleven findings.** **Stated precisely, because an earlier draft counted two prompt blocks in ONE script as two "surfaces" and re-labelled the orphan "live":** `init-review-loop.sh:464` and `:554` are **two prompt blocks in a single live script**, both carrying the 10-issue cap; `skills/litmus/SKILL.md:641` is genuinely live and restates the **3-new drip rule with no 10-cap**; `prompt_template.txt:61` carries both but **has no consumers** and is dispositioned separately above. So with eleven findings either an omission is silently accepted — recreating the laundering under a new name — or the omission is classed as incompleteness, which would block a review whose only omitted finding was advisory, contradicting this row's promise that severity behaviour is untouched. Overflow **completes within the same reviewed scope and iteration** (additional report pages collected *before* the verdict is computed, never deferred to the next iteration). **But specify the TRANSPORT before mandating fixtures — the existing machinery actively defeats the naive reading.** Verified at the checkpoint: `try_last_review_object()` (`extract_review_json.py:735`) returns the **LAST** top-level review-shaped object and `_resolve()` (`:690-699`) refuses only on malformed or unresolvable regions — so a second page **replaces** the first rather than accumulating, and *a blocking first page followed by a clean final page yields the clean verdict*. The item must therefore name: how a page is requested; how pages are framed and identified; how the extractor **accumulates rather than replaces** (this is a change to `try_last_review_object`'s contract — name it); how page boundaries bind to `reviewed_diff_hash`; and how `lib/merge-findings.py` combines across pages, given that its dedup is description-similarity based.

**Status of that transport: UNSPECIFIED as of 2026-09-08 (history, below); SPECIFIED 2026-09-13 at "THE ENVELOPE" in this row.** Binding contract moved verbatim to `docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-transport-envelope.md` (2026-09-23 split; UNREVIEWED — the review loop does not read it). It remains part of this plan's requirements.

**Class:** hard; split around the baseline

**Status 2026-09-08:** **SPLIT** — **integrity half** is **item 2i**: its scope is the **2i NODE**, defined once in the dependency graph and **not restated here**. **The work of bounds (i-c), (i-a), (i-b) and (2-CAP) is NOT in that node** (list completed 2026-09-13, native b5bddf50 HIGH; UNREVIEWED) — it is the **`2a-env`** node, a sibling ahead of `baseline captured`, added 2026-09-09 (round 11) because (i-b) changes an authorization outcome, which (B) Stage 1 forbids by its own stated rule while (B) Stage 2 — the member that may change one — sits downstream of `baseline captured`; its gate and acceptance are the graph's (**re-cited 2026-09-09, round 12**: this said "the 2i NODE forbids that", a node-wide prohibition the same round's per-member correction removed). (An earlier version of this cell put detection in the TODO-now set, which contradicted item 2i(B) and made the dependency graph a cycle.) It was moved forward because this row itself identifies a laundering strictly worse than #844's and collecting the baseline behind it would contaminate the baseline exactly as #844 would; **convergence-policy half** (the `cap_exhausted` scope-reduction recovery, the rest of the prompt rewrite, and every refusing reader — the 3-new drip deletion and complete-result collection moved to `2a-env` under bound (2-CAP), 2026-09-13) **BLOCKED on `baseline captured` (its *Still needs Chris* #2 conjunct is SATISFIED — #2 ANSWERED 2026-09-09; the label is corrected here 2026-09-09, round 10, finding [13], because naming an answered row as a live gate reads as a phantom approval dependency)**. Neither half may be started on the other's authority

**Evidence / settling check:** Tests exercise the prompt actually emitted and consumed, **not the unused template**; delete or re-point the two reference docs in the same change. Fixtures cover a clean diff, multiple upfront findings, incomplete output, a disputed finding, a real regression and unchanged previously-adjudicated findings. Incomplete review and unresolved blockers never become automatic PASS. Severity/authorization behaviour is untouched — that belongs to item 8.
