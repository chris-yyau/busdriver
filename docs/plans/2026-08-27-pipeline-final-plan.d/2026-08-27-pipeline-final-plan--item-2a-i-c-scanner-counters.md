# Item 2a — (i-c) short-circuit preconditions: the scanner counters

> **INDEPENDENT DESIGN FILE: BINDING, AND UNREVIEWED.** This file is part of the requirements of
> `docs/plans/2026-08-27-pipeline-final-plan.md` (the parent). The parent cites it at § *Item 2a*, bound (i-c) and does
> not restate it. It is reviewed on its own: `init-design-review.sh docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-i-c-scanner-counters.md`, then
> `run-design-review-loop.sh`. Its review verdict covers only this file's spec hash. A verdict on the parent
> does not cover this file, and a verdict on this file does not cover the parent. It is not history.
>
> **Context for a reviewer who reads only this file.** The body below was moved verbatim from the parent on
> 2026-09-23 (Chris-approved split), and this header was added the same day; the body is unchanged. Item
> numbers, node names such as `2a-env`, `baseline captured` and `protocol-approved`, and bound labels such as
> (i-a), (i-b), (2-ORD) and (2-CAP) are defined in the parent's execution-order table (§ *Execution order
> and acceptance*) and § *Still needs Chris*, and, since the parent's SECOND MOVE (2026-09-24), in the
> children: the dependency graph, *Standing fallback*, *Recommended next actions* and § *Item 2* in the 2p
> file; § *Item 2i* and § *Item 8* in the item 8 file; § *Item 2a* in the (i-a) file; § *Item 7* in the
> item 7 file; § *#840 proposed revisions* in the 2p file. The parent keeps only one-line pointers there (2026-09-26; UNREVIEWED). A lens reviewing this file receives this file and is not assumed to open the parent or the checkout.
> Line anchors `:NNN` into the parent point at the parent as it stood before the 2026-09-23 splits. Anchors
> into other files name their own tree or checkpoint.

**(i-c) SHORT-CIRCUIT PRECONDITIONS — THE SCANNER COUNTERS. FOLDED IN FROM DECISION [13] 2026-09-10 (round-17 finding [3]; Photon technical decision, UNREVIEWED).** Stated here, with its siblings, because it governs whether the fast path runs at all; the header block cites this bound and no longer carries the deliverables. **EVERY SCANNER SCRIPT NAMED IN THIS BOUND IS UNDER `skills/litmus/scripts/lib/`, and is cited below as `lib/<script>:N` against that directory** — stated once here rather than repeated, because by the `L531–L536` convention an unqualified anchor resolves as the checkpoint path and these scripts are not `git show`-able there (PATH STATED ONCE 2026-09-12, native finding [04] MEDIUM; UNREVIEWED). `SAST_COUNT` (`run-review-loop.sh:2975`) and `MD_COUNT` (`:2979`) are each `python3 -c 'print(len(json.load(sys.stdin)))'` with a `|| echo "0"` fallback, feeding the conjunction at `:3087-3088` whose own comment claims ANY failing condition falls through. **Two independent defects, both measured at HEAD.** *(a) Parse failure reads as clean zero.* *(b) SHAPE failure reads as clean zero too, and the count cannot see it:* `[]` parses to a list of length 0, and `{}` and JSON `""` **also** report length 0 — so `len()` alone cannot separate them. **THE DELIVERABLE HAS TWO HALVES, because the failure is laundered UPSTREAM before either counter sees it — CORRECTED 2026-09-10 (round-18 finding [5]; Photon technical decision, UNREVIEWED; verified by EXECUTION at the declared checkpoint `727d4652`).** *(1) PRESERVE THE FAILURE AT EVERY UPSTREAM NORMALIZER — STATED AS A BOUNDARY RULE, WITH THE SITE LIST DERIVED FROM IT. CORRECTED 2026-09-12 (native 4efaea28 finding B3, codex issues[2] + grok issues[2] `completeness`, conf 0.97; UNREVIEWED). **The earlier "INVENTORY COMPLETED … all five" wording is WITHDRAWN — it was an EXHAUSTIVENESS claim and it was false.** Twice now a count has been asserted and then refuted: first "BOTH" (two of them), then "five". The defect is the SHAPE of the claim, not the arithmetic, so this bound no longer names a number. **THE RULE — WIDENED 2026-09-12 (native c94fb702 finding [4], codex issues[1] `completeness`, conf 0.9; UNREVIEWED): every point at which a scanner can yield a CLEAN RESULT DESPITE AN INCOMPLETE SCAN must emit the failure token instead — whether the cleanliness comes from INVOCATION STATUS, OUTPUT SHAPE, NORMALIZATION, a SHELL FALLBACK, or from SCANNER-NATIVE ERROR RECORDS THE NORMALIZER NEVER READS.** The earlier rule said only "can yield `[]`", which is a *symptom* of the class and not the class: it misses the case where a scanner **exits zero and emits well-formed JSON while reporting its own analysis errors inside that JSON.** Measured: the Semgrep normalizer iterates `data.get('results', [])` at `lib/sast-runner.sh:100` and **never reads `errors`**, so `{"results": [], "errors": [...]}` normalizes to a clean empty array and passes every container check this bound proposes. Semgrep reports syntax errors **without a nonzero exit** unless `--strict` is set, so this is reachable, not hypothetical. **Which scanner-native error records make a scan INCOMPLETE must be stated per scanner, independently of process exit status and JSON well-formedness, and routed through the failure mechanism this bound already defines — no new mechanism.** Two valid-JSON fixtures: `errors` beside empty `results` ⇒ failure token; `errors` beside a genuine advisory finding ⇒ **the finding is RETAINED and authorization is WITHHELD** (both, not either). **THE OTHER TWO SCANNERS GET THEIR OWN MEMBERSHIP TESTS, BECAUSE THEY CANNOT INHERIT SEMGREP'S — ADDED 2026-09-13 (native 79ad7ef4 finding [11] MEDIUM; UNREVIEWED).** Only Semgrep's `errors` was specified, and "a site absent from the list is still governed by the rule" does not supply an implementable test for a scanner that emits no `errors` channel at all. Measured at HEAD 2026-09-13, `grep -c errors skills/litmus/scripts/lib/sast-runner.sh` ⇒ **0**: the word appears nowhere, so even Semgrep's array has no reader today and **writing that reader is part of this deliverable**, not an assumed capability. **SHELLCHECK** emits no `errors` array and is run PER FILE as `_sast_timeout "$timeout_sec" shellcheck --enable=… -f json "$f" 2>/dev/null) || true` (`:142-146`), so a timed-out or failed file yields EMPTY output, the `|| true` discards its exit status, and the very next line's `[ -n "$file_output" ]` test skips it while the merged success array is still emitted — a file silently not scanned. **Its membership test is therefore per FILE, not per JSON document: a file whose invocation exits 0 (no issues) or 1 (issues found) with well-formed JSON output is SCANNED; a file whose invocation exits 2 or higher, times out, or yields empty or unparseable output is UNSCANNED, and one unscanned file makes the scan INCOMPLETE.** CORRECTED 2026-09-13 (native 2a9a6900 finding [3]; UNREVIEWED): this read "exits non-zero", but ShellCheck exits 1 whenever it reports any finding (`man shellcheck`, RETURN VALUES: 0 no issues, 1 some issues, 2 some files could not be processed), so every file with a finding would have been UNSCANNED and no shell-touching diff could ever short-circuit. ACCEPTANCE adds a positive control: a file yielding exit 1 with only informational findings is SCANNED. That requires CAPTURING the per-file exit status instead of discarding it, which is named here as part of the work. **TRUFFLEHOG** is invoked `trufflehog filesystem --json --no-update "$tmpdir"` (`:203`) with stderr discarded (`2>/dev/null`), over a tmpdir holding only `ln -s` symlinks (`:199`) and no `--max-symlink-depth` — JSON-**LINES** on stdout, while per upstream TruffleHog source (`main.go`, `pkg/sources/filesystem/filesystem.go`; source-read by the native FINAL13 arbiter, issues[0]/[1], NOT executed — TruffleHog is not installed on this host — and version-dependent, pinned with the scanner VERSION cohort dimension) per-file scan errors are LOGGED TO STDERR without changing the exit status and, at symlink depth 0, every symlink is skipped (CORRECTED 2026-09-15; UNREVIEWED) — and its failure branch is `|| { rm -rf "$tmpdir"; echo "[]"; …}`, which returns an EMPTY FINDINGS ARRAY on any failure and is indistinguishable from a clean scan. **Its membership test (a FUTURE delivery requirement, not present tested behaviour): a non-zero exit, a trailing line that is not parseable JSON (truncation), any per-file scan error on the SEPARATELY CAPTURED stderr, or staged input the scanner did not read (the wrapper passes the regular files of the pinned scan root defined at SCANNED BYTES below, so no symlink reaches it) makes the scan INCOMPLETE; ACCEPTANCE through the real wrapper: an unreadable file ⇒ INCOMPLETE, a known-detectable secret fixture yielding a clean result ⇒ INCOMPLETE, and a clean readable tree ⇒ SCANNED (control)** — and because the current failure path manufactures `[]`, that path must route the failure token instead of swallowing it. All three tests route through the failure mechanism this bound already defines; **no new mechanism and no new token form**. The list below is DERIVED from that rule and, as this document's own convention requires, the count is read off the list and never written beside it. A site absent from the list is governed by the rule, not exempted by the omission — that is the whole reason the rule leads and the list follows.* `_sast_merge_json` (`lib/sast-runner.sh:58-69`; callers `:259`, `:267`, `:275`) catches a parse error, warns, and prints the accumulated JSON anyway, and `lib/markdown-checker.sh:186-195` does the same behind a bare `pass` — measured, malformed JSON, `{}` and JSON `""` **all return `[]`**. **THREE FURTHER PARSE-LEVEL SITES — READ FROM SOURCE AND THEN EXECUTED 2026-09-12, so these are REPRODUCED rather than clause-grounded (the earlier "NOT executed" qualifier on them is WITHDRAWN). These five are the MEASURED members of the rule's set, not its extent:** `lib/sast-runner.sh:94-97` — the Semgrep normalizer's `json.load` `except (json.JSONDecodeError, ValueError)` does `print('[]'); sys.exit(0)`, so malformed Semgrep output becomes a clean empty array **with a success exit status**; `lib/sast-runner.sh:219-222` — the TruffleHog per-line loop's `except` does a bare `continue`, silently DROPPING the malformed record while the surrounding scan still reports success; and `lib/markdown-checker.sh:202-211` — the byte-identical bare-`pass` merge on the `LITMUS_CHECK_URLS=1` branch, which the `:186-195` sibling's fix does not reach. **WHAT THE EXECUTION SHOWED (each snippet run as the source has it, no runtime change and no worktree mutation — `lib/sast-runner.sh` sha256 `4854e7b1…`, `lib/markdown-checker.sh` `5b7ce30a…`): every one of the five MEASURED sites exits `rc 0`, and four of the five emit a clean `[]`.** The six derived sites below were read and pinned in both trees but NOT executed; they are named here on the rule, not on a measurement, and this bound says so rather than implying a coverage it does not have. `:94-97` malformed ⇒ `[]`, rc 0 — indistinguishable from the valid-empty control, which also prints `[]`. `:219-222` malformed JSONL line beside a valid one ⇒ the valid sibling alone, rc 0, the dropped record named nowhere. `:186-195` fed an explicit non-JSON failure string (the proof's probe, `SCANNER_FAILURE` — no scanner emits it today) followed by `[]` ⇒ `[]`, rc 0 — **an upstream that DID try to signal failure in band is swallowed by the bare `pass`**, which is the sharpest form of this defect: a producer that DID try to signal failure is laundered anyway; fed a malformed sibling beside a valid array, the valid array is returned with no failure token. `:202-211` is byte-identical and behaves identically. `:58-69` alone leaves a trace — `WARNING: Failed to parse SAST output line (malformed JSON)` on **stderr** — and then prints the accumulated `[]` on stdout regardless, so the trace is not a token: nothing downstream reads it and the verdict is unchanged. **SIX FURTHER DOORS, MEASURED 2026-09-12 AND ABSENT FROM EVERY EARLIER LIST (native 4efaea28 B3; byte-identical in the working tree and at `727d4652` via `git show`):** `lib/sast-runner.sh:89` — `|| { echo "[]"; return; }` converts Semgrep INVOCATION FAILURE and TIMEOUT to `[]`, a status-level door no parse-level fix reaches; `:100` — `for r in data.get('results', [])` turns a parsed-but-INVALID Semgrep object (`{}`) into `[]` **without ever entering the `:94-97` except**, so shape and parse are genuinely different doors; `:111` — a second `2>/dev/null || echo "[]"` fallback on the same path; `:161-164` — **the ShellCheck normalizer's own `except (json.JSONDecodeError, ValueError): print('[]'); sys.exit(0)`**, an entire scanner the earlier lists omitted; `:178` — repeats it; `:203` — TruffleHog EXECUTION FAILURE to `[]`. **Every site named in this bound, listed or derived, must emit a distinct non-`[]` failure token**, and **`[]` must never be reused to mean failure**, so a genuine empty scan and a CONFIGURED SKIP both stay distinguishable from a failed one. A per-record drop counts as a failure of its scanner: a scanner that could not parse one of its own records has not produced a complete result, and reporting the survivors as a clean scan is the same laundering one level down. Correcting only the counters is insufficient: `|| echo "0"` would re-launder whatever the merge produced. *(2) PROPAGATE IT INTO THE EXISTING SENTINEL* — the consumer rule is unchanged: validate the parsed value **is a LIST before counting it**; a non-list takes the sentinel, exactly like a parse failure. **SCANNED BYTES ARE THE PINNED BYTES — ADDED 2026-09-15 post-14 (native round-14 issues[0] HIGH and issues[14] MEDIUM; Photon correction decision; UNREVIEWED).** A scan counts toward this bound only if it read the bytes being authorized. Today it does not: commit mode pins the index (`run-review-loop.sh:2012`, `:2024`) and PR mode pins `_PR_TIP` (`:2204`), but the pin only LISTS names into `FILTERED_FILES` (`:2537`; PR mode `:2355`; anchors in this rule at HEAD `34887cb7`), and every tool then opens the WORKING-TREE path — Semgrep's positional paths (`lib/sast-runner.sh:85-89`), ShellCheck's `[ -f "$f" ] || continue` then `"$f"` (`:141-145`), TruffleHog's `ln -s "$repo_root/$f"` (`:195-199`) and markdownlint's `[ -f "$f" ] || continue` then `$lint_cmd "$f"` (`lib/markdown-checker.sh:35-38`) — so a staged vulnerable blob under a clean unstaged edit scans clean, and a staged file deleted from the worktree is skipped as if scanned. **THE RULE:** every scanner and the markdown check read a PRIVATE SCAN ROOT materialized from the same pin as `REVIEWED_DIFF_HASH` (`:2405`) and run from it over the same repo-relative names — in commit mode the entries `GIT_INDEX_FILE="$_INDEX_SNAPSHOT" git ls-files -s -z -- <names>` lists, in PR mode the entries `git ls-tree -r -z "$_PR_TIP" -- <names>` lists, each written as `git cat-file blob <oid>` into the root, for modes `100644` and `100755` only. **Not `git checkout-index`**, rejected on measurement: it applied a worktree-only `.gitattributes` rewrite to the pinned bytes (`y\n` came out `y\r\n`) and wrote tracked symlinks as live links out of the root, in both modes. **A NAME IN A SCANNER'S INPUT SET THAT THE ROOT CANNOT CARRY IS UNSCANNED, NEVER SKIPPED:** a symlink (`120000`), a gitlink (`160000`), any other non-blob entry, and a name that does not round-trip through the newline-delimited `FILTERED_FILES` make that scanner's scan INCOMPLETE through the failure token this bound already defines — no filename exclusion and no new token; a name is a DELETION — no bytes, and not unscanned — ONLY when a status record from the same pinned endpoints marks it `D`: in commit mode `:2537`'s own read with `--name-status -z --no-renames` in place of `--name-only`, and in PR mode `:2355`'s own read likewise — its `"${_PR_BASE_REF}...${_PR_TIP}"` endpoints unchanged, `_PR_BASE_REF` being the pinned `_PR_BASE_SHA` (`:2222`) — with the same pathspec and exclusions; `--no-renames` only makes each record carry one path, and changes no name `FILTERED_FILES` lists. Classification compares each `FILTERED_FILES` line byte-for-byte with the NUL-delimited paths of that status and of the pin's `-z` listing, so a line equal to no NUL-delimited path — quoted, split or otherwise mangled — is neither a pin entry nor a `D`, and it, like any name with no pin entry and no `D` record, is UNSCANNED; and the `[ -f ]` guards stop being skips, since a name the root should carry and does not is UNSCANNED. The cost is stated: a change touching a tracked symlink or submodule cannot short-circuit or authorize on its scan (this repository tracks none, `git ls-files -s` at `34887cb7`). **MEASURED, AND ONLY THIS:** the post-14 isolated fixture (three executions, the last `run.tSauOK` 30 ok / 0 FAIL; stub files and git only; script archived at `~/.hermes/reports/840-post14-correction-prechange-archive-20260915/fixture/fixture.sh`) reproduced the names-from-pin, bytes-from-worktree defect and showed the raw form yields the pinned bytes in both modes — staged-only files, a name with a space, a post-pin `git add` excluded, no symlink written, worktree and live index untouched. NOT MEASURED: any real scanner over the root (reported paths and TruffleHog's handling included), executable bits, submodules, newline names and `cat-file` cost on large sets. **ACCEPTANCE, through the real wrappers and in both modes where the mode exists:** a staged vulnerable blob under a clean worktree edit ⇒ the finding is reported, per scanner and for markdownlint; a clean staged blob under a vulnerable worktree edit ⇒ no finding; a PR-tip blob differing from the worktree ⇒ the tip's bytes are scanned; a staged file deleted from the worktree ⇒ scanned from the pin; a worktree-only `.gitattributes` rewrite ⇒ the scanned bytes equal the pinned blob; a tracked symlink in a scanner's input set ⇒ nothing is read through it and the scan is INCOMPLETE; a staged deletion in commit mode and a file deleted on the branch in PR mode ⇒ a `D` record, not unscanned, and no INCOMPLETE on its account; a staged file whose name contains a newline, which `--name-only` lists quoted ⇒ that line matches no NUL-delimited pin path and no `D` record, so it is UNSCANNED and the scan INCOMPLETE; and a known-detectable secret in a real staged file ⇒ TruffleHog reports it. **SEMGREP INPUT COVERAGE — ADDED 2026-09-16 post-15 (native round-15 issues[1] MEDIUM; Photon correction decision `840-post15-plan-correction-decision.md`; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED).** The installed Semgrep 1.174.0 skips any target over `DEFAULT_MAX_TARGET_SIZE = 1000000` bytes (`semgrep/constants.py:133`, wired as the `--max-target-bytes` default at `commands/scan.py:174-177`) and records the skip apart from `errors`, and its JSON `paths.scanned` is built from all targets with `skipped` unset (`output.py:639-648`) — all source-read, not executed; whether `paths.scanned` omits size- or ignore-skipped targets is NOT established. So Semgrep gets its own membership test, beside ShellCheck's and TruffleHog's: the wrapper passes `--max-target-bytes 1000000` EXPLICITLY; before launch, every pinned Semgrep target larger than that literal is UNSCANNED; and every launched target absent from `paths.scanned` is UNSCANNED — each making the scan INCOMPLETE through this bound's token. **D4 CONSEQUENCE (Photon technical default):** a change carrying a Semgrep target over 1,000,000 bytes cannot short-circuit or authorize on its scan, where HEAD silently passes it. ACCEPTANCE (local rule file, `--metrics off`, no registry): a pinned `.py` of 1,000,001 bytes ⇒ INCOMPLETE; a 10-byte pinned control ⇒ SCANNED, or UNSCANNED by the membership test, never silently clean. **MARKDOWNLINT — ADDED 2026-09-17 (native run `7886ed9f` MEDIUM, conf 0.95; UNREVIEWED: Semgrep, ShellCheck and TruffleHog each received an explicit failure classification in this bound and markdownlint received none, so this bound's blanket failure-preservation rule had NO concrete predicate to fire on for that scanner).** It is run PER FILE from `skills/litmus/scripts/lib/markdown-checker.sh`, the executable chosen at `:25-32` — `markdownlint-cli2` if present, else `markdownlint`, else **`echo "[]"` and return** — and invoked `file_output=$($lint_cmd "$f" 2>&1) || true` (`:38`), so the per-file exit status is DISCARDED and stdout and stderr are MERGED. That merged stream is then filtered at `:44-63` by `re.match(r'^(.+?):(\d+)(?::\d+)?\s+(MD\d+/\S+)\s+(.*)')`, which keeps finding-shaped lines and SILENTLY DROPS every other diagnostic, and the stage ends `2>/dev/null || echo "[]"`. A configuration or processing failure therefore yields a clean `[]` at status 0, indistinguishable from a genuinely clean file — the same defect as TruffleHog's `|| { … echo "[]"; }` above, reached by a different route: here the diagnostic IS present on the merged stream and is discarded by the filter, so the repair is Semgrep's SEPARATELY CAPTURED stderr rather than ShellCheck's captured exit status alone. **Its membership test is per FILE, like ShellCheck's: a `.md` file whose invocation completes with its diagnostics ACCOUNTED FOR — findings emitted, and every non-finding diagnostic CAPTURED rather than dropped — is SCANNED; a file whose invocation fails, times out, or produces a diagnostic the filter cannot classify is UNSCANNED, and one unscanned file makes the scan INCOMPLETE through (i-c)'s existing failure token — no new mechanism.** **THE NUMERIC EXIT CODES ARE DELIBERATELY NOT PINNED HERE.** Neither executable is installed on this host (`command -v markdownlint-cli2 markdownlint` returns nothing, checked 2026-09-17), and writing a code table from anything but the installed tool would repeat the error this bound already corrected once for ShellCheck — "exits non-zero" against a tool that exits 1 on every finding. **THE REQUIREMENT IS CAPTURE-AND-CLASSIFY: the per-file exit status is CAPTURED instead of discarded by `|| true`; stderr is captured SEPARATELY instead of merged by `2>&1`; and the findings-present code is pinned by the ordinary-finding positive control below, read from the installed executable at implementation time rather than from this document.** BOTH executables are in scope — the classification is per executable, and the checker identity, version and config digest are pinned by item 2p's SCANNER CONFIGURATION part (4), with `LITMUS_CHECK_URLS` and its timeout beside them (added 2026-09-23 (native run `58a6b41b` MEDIUM, arbiter-confirmed; plan-only, UNREVIEWED); this sentence previously pointed at the scanner VERSION dimension, whose parts (1)–(3) named only Semgrep, ShellCheck and TruffleHog). **TWO NON-FAILURE OUTCOMES STAY DISTINCT FROM FAILURE, under the valid-empty / configured-skip / failure distinction this bound already draws:** a staged diff carrying no `.md` file at all (`:22`) is a CONFIGURED SKIP, and a clean `.md` file with no findings is VALID-EMPTY. **The absent-executable path at `:29-32` is NEITHER — it is a FAILURE**, because the files were in scope and nothing scanned them, and it must reach the failure token rather than print `[]`. **ACCEPTANCE, through BOTH entry paths named at THE ENVELOPE's MARKDOWN ENTRY PATHS (the driver-sourced path and the direct-run path, defined once there and cited, not restated):** a configuration-error control — a `.md` file beside a malformed lint configuration — is UNSCANNED and makes the scan INCOMPLETE; an ordinary-finding control emits its findings AND is SCANNED, so a file with findings is never mistaken for a failure; a clean control is SCANNED and valid-empty; and an absent-executable control is a FAILURE, never `[]`. Findings from successful SIBLING files are RETAINED in every one of those cases. **NON-ASCII NAMES — ADDED 2026-09-16 post-15 (native round-15 issues[11] MEDIUM; Photon correction decision `840-post15-plan-correction-decision.md`; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED).** The four file-list reads `run-review-loop.sh:2227` and `:2411` (`ALL_STAGED_FILES`) and `:2355` and `:2537` (`FILTERED_FILES`) run WITHOUT `-c core.quotePath=false`, which the same file sets elsewhere (`:620`, `:669`, `:1078`, `:2405`, `:2486`, `:3045`), so a name with bytes ≥ 0x80 is octal-quoted and matches no NUL-delimited pin path — UNSCANNED under the classification above, while at HEAD the scanners' `[ -f ]` guards skip it silently (source-read). All four gain `-c core.quotePath=false` TOGETHER, so both lists render names identically for the exclusion `comm` (`:2591-2594`), and the direct entries (`lib/sast-runner.sh:296`, `lib/markdown-checker.sh:227`) gain it too; git then emits bytes ≥ 0x80 raw (git documentation, not measured on this host). **D3 CONSEQUENCE (Photon technical default):** a name git still quotes with that setting — one containing `"`, `\` or a control character — stays UNSCANNED ⇒ INCOMPLETE, as the newline rule above already does; a NUL-delimited `FILTERED_FILES` end to end, which the arbiter offered, is NOT adopted, and such a name's change therefore cannot authorize on its scan. ACCEPTANCE, in both modes: a staged `日本.sh` carrying a ShellCheck finding ⇒ the finding is reported and no exclusion is reported for it; a staged `a"b.sh` ⇒ UNSCANNED ⇒ INCOMPLETE. **SCANNER CONFIG COMES FROM THE SAME PIN — ADDED 2026-09-16 post-15 (native round-15 issues[12] MEDIUM; Photon correction decision `840-post15-plan-correction-decision.md`; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED).** The root above carries only the scanned names, so a tracked `.shellcheckrc`, markdownlint config or `.semgrepignore` is absent unless it is itself in the change.  **TRANSITIVE IN-REPO REFERENCES — ADDED 2026-09-16 post-16 (native run `5e995822` MEDIUM; Photon manual-resume brief `manual-802-838-840-847-20260916.md` #840 section, on the `840-post16-readonly-diagnosis-result.md` roots; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED; the earlier text materialized each tool's config at its lookup position but said nothing about what that config itself references in-repo).** A tracked config whose IN-REPO reference — a markdownlint `extends`, a `.semgrepignore` include, a ShellCheck `source-path` or sourced file — is NOT materialized from the SAME pin makes that scanner's scan INCOMPLETE, fail-closed; materializing transitive references is OPTIONAL, and silently scanning against a worktree copy is not. Fixture: a pinned config extending an unchanged base, beside a conflicting worktree-only copy ⇒ INCOMPLETE, or a result matching the pin — never silently different. Each tool's tracked config files at its lookup positions for the scanned names — ShellCheck's `.shellcheckrc` from a script's directory upward, markdownlint-cli2's `.markdownlint-cli2.*`/`.markdownlint.*` from the working directory and a file's directories, Semgrep's `.semgrepignore` at the root; lookup rules per the tools' documentation and NOT verified per installed version — are materialized from the SAME pin by the same `git cat-file blob` rule, each tool runs from the root, and the scan result records `config_changed_in_diff` when the change touches such a file. Configuration outside the repository is not addressed. **D1 CONSEQUENCE (Photon technical default; security):** a change can still weaken its own scan by editing tracked config, exactly as at HEAD; the record makes that visible and does not prevent it. ACCEPTANCE: a pinned `.shellcheckrc` that disables a rule changes the finding set as the same file does in the worktree; a worktree-only config differing from the pin has no effect; and a change touching a config file carries `config_changed_in_diff`. **(2-ORD) THE TOKEN AND ITS REFUSING READER LAND TOGETHER — ADDED 2026-09-12 (native ca145c70 finding C0, codex issues[0] `architecture`, conf 0.99; UNREVIEWED).** Nothing above ordered the producer against the consumer, and the two live on different gates: this bound's token production is `2a-env`'s, a graph prerequisite of `baseline captured`, while the rule that makes a malformed scanner input **non-authorizing** is item 8's ingestion boundary, stated once there and CITED here rather than restated. A producer landing first mints a non-`[]` token that **no reader yet refuses**, so for the whole interval between them a failed scanner travels beside clean ones and a mixed run still authorizes — the exact fail-open both halves exist to close, re-opened by sequencing rather than by logic. **THAT INTERVAL'S FAIL-OPEN IS DEMONSTRATED, NOT ONLY ARGUED FROM ORDERING — EXECUTED 2026-09-12:** a malformed Semgrep output was laundered by `:94-97` into `[]` at rc 0 and then merged beside a clean envelope, and `lib/merge-findings.py` (sha256 `511be928…`) returned `{"status": "PASS", "issues": []}` at rc 0 — **a run containing a scanner that failed still produced an authorizing verdict, with nothing anywhere in the output naming the failure.** The same shape holds with one unparseable input beside a valid empty envelope. This measures the CURRENT runtime and therefore fixes the ordering clause's premise; it does **not** mean the defect is fixed — **the runtime defines no failure token at all**: the proof searched candidate `ec6949ba` and the three scanner libs and found no such name anywhere except as its own synthetic probe input, so today there is no producer to sequence and the constraint below binds the moment one is written. (The probe string quoted in this bound and in (1) above is the proof's input, **not** a deliverable naming the token's form; naming that form is still `2a-env`'s, per the format-neutral rule already stated.) **Therefore: no producer may emit the failure token until the readers that treat it as non-authorizing — the merger/short-circuit reader AND the exit-3 built-in handoff check of item 2i(B) (A-iv) (added 2026-09-23, native `0994c559` HIGH; UNREVIEWED) — are in the same change or already landed.** Fail-closed direction only — this constrains the ORDER of two already-approved deliverables and **adds no new mechanism, no new token form and no new gate.** **THE ORDERING AS FIRST WRITTEN WAS UNSATISFIABLE — CORRECTED 2026-09-12 (native 4efaea28 finding B2, codex issues[0] + grok issues[1] `architecture`, conf 0.95; UNREVIEWED). The sentence "does not touch item 8's own gates" is WITHDRAWN: it was the false claim.** The graph itself refutes it, and the graph is SoT: `2i NODE ──► 2a-env` carries `SCOPE: (i-c) … INCLUDING both upstream normalizers`, so token production is 2a-env work; `baseline captured` lists `2a-env` as a prerequisite; item 8's node puts the mutating reader behind `2-B AND "baseline captured"`. So the reader cannot precede `baseline captured`, `baseline captured` cannot precede 2a-env, 2a-env cannot complete without emitting the token, and (2-ORD) forbids the token before the reader — **every ordering forbidden, and the `baseline captured` acceptance fixture therefore unreachable.** *(The mutating half has no alternate home: its gate is stated at both of item 8's statements of it, and the cycle shape is recorded independently in the execution-order section.)* **THE SPLIT THAT RESOLVES IT:** the MINIMAL scanner-input rejection — the non-`[]` failure token plus the readers that treat it as NON-AUTHORIZING — the merger/short-circuit reader AND the exit-3 built-in handoff check of item 2i(B) (A-iv), which the merger never sees because that handoff exits before it (added 2026-09-23, native `0994c559` HIGH; UNREVIEWED) — moves into **`2a-env`**, landing beside token production and AHEAD of `baseline captured`. Item 8 KEEPS, behind its existing gate, the medium-blocking policy decision and the record-level severity/confidence validation. This **narrows** item 8's gate to the half that genuinely needs the baseline; it does not weaken item 8's acceptance, and every fixture item 8 owns stays item 8's. The graph names the reader's home node explicitly and carries an **ACYCLICITY CHECK over {`2a-env`, item 8, `baseline captured`}** — a check, not a new edge and not an edge exception. 2-A/2-B independent start authority is unchanged. **The negative controls are untouched and stay authorizing-eligible:** `[]` STILL means a genuine clean zero and STILL fires the short-circuit, and a CONFIGURED SKIP stays distinguishable from both. **`[]` STAYS ZERO AND STILL FIRES THE SHORT-CIRCUIT** — it is the honest clean scan this fast path exists for, and the historical scanner contract in the file's own docstring. **THE SENTINEL IS PINNED BY MEASUREMENT, NOT BY ANALOGY — the obvious choice is a FAIL-OPEN.** Measured 2026-09-10: `[[ "UNPARSEABLE" -eq 0 ]]` is **TRUE** (`-eq` evaluates a non-numeric operand as a variable name, unset ⇒ 0) and so is the empty string — both would FIRE the short-circuit — while `1abc` raises an arithmetic syntax error. Only two forms fall through for every candidate tested: a **negative-integer sentinel (`-1`)** or **string comparison (`[[ "$SAST_COUNT" == "0" ]]`)**. **Both are required:** the sentinel alone leaves `-eq` exposed to an empty value, and the predicate alone still fires on the literal `"0"` the current fallback produces. **Fixtures — invalid shapes AND valid-empty negative controls, at this boundary:** malformed scanner JSON + a prose-only staged diff ⇒ short-circuit does NOT fire; `{}` ⇒ does NOT fire; JSON `""` ⇒ does NOT fire; **`[]` ⇒ DOES fire** (the control that keeps the fast path alive); and — ADDED 2026-09-10 (round-18 finding [5]) — **ONE scanner malformed while the others are clean, prose-only diff ⇒ does NOT fire**, the per-scanner case a whole-pipeline-failure fixture never reaches, together with a control that the clean all-`[]` path is unchanged. **ANCHORS ARE BRANCH-REVISION `34887cb7` MEASUREMENTS (round-17 finding [10]):** at the declared checkpoint `main@727d4652` the same three sites are `:2985`, `:2989` and `:3097-3098`. **NOT WRITTEN:** pulling scanner ARRAYS into 2i(B)(c-4) — they are not review envelopes and that exclusion is untouched — or leaning on (i-b) to refuse the mint downstream, since the loop would still exit 0 with no review having run.
