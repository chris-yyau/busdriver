# Item 2a — transport status (THE ENVELOPE)

> **INDEPENDENT DESIGN FILE: BINDING, AND UNREVIEWED.** This file is part of the requirements of
> `docs/plans/2026-08-27-pipeline-final-plan.md` (the parent). The parent cites it at § *Item 2a*, the transport status and does
> not restate it. It is reviewed on its own: `init-design-review.sh docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-2a-transport-envelope.md`, then
> `run-design-review-loop.sh`. Its review verdict covers only this file's spec hash. A verdict on the parent
> does not cover this file, and a verdict on this file does not cover the parent. It is not history.
>
> **Context for a reviewer who reads only this file.** The body below was moved verbatim from the parent on
> 2026-09-23 (Chris-approved split), and this header was added the same day; the body is unchanged. Item
> numbers, node names such as `2a-env`, `baseline captured` and `protocol-approved`, and bound labels such as
> (i-a), (i-b), (2-ORD) and (2-CAP) are defined in the parent's execution-order table (§ *Execution order
> and acceptance*) and § *Still needs Chris*, and, since the parent's SECOND MOVE (2026-09-24), in the
> children: the dependency graph, *Standing fallback*, *Recommended next actions* and § *Item 2* in the 2p
> file; § *Item 2i* and § *Item 8* in the item 8 file; § *Item 2a* in the (i-a) file; § *Item 7* in the
> item 7 file; § *#840 proposed revisions* in the 2p file. The parent keeps only one-line pointers there (2026-09-26; UNREVIEWED). A lens reviewing this file receives this file and is not assumed to open the parent or the checkout.
> Line anchors `:NNN` into the parent point at the parent as it stood before the 2026-09-23 splits. Anchors
> into other files name their own tree or checkpoint.

**Status of that transport: UNSPECIFIED as of 2026-09-08 (history, below); SPECIFIED 2026-09-13 at "THE ENVELOPE" in this row.** The five clauses above are the questions the transport must answer, not the answers — this row sets the bar "specify the TRANSPORT before mandating fixtures" and then mandates three fixtures against a transport it has not specified, so an engineer reaching this row cannot implement it. *History, not instruction:* the envelope was left open on 2026-09-08 because it sat behind `baseline captured`, so nothing was delayed; item 2a bound (2-CAP) (2026-09-13) moved it onto the path to that milestone, which removed that reason. It is now DUE in `2a-env`, and the convergence-policy half keeps its remaining work behind `baseline captured` (its *Still needs Chris* #2 conjunct is SATISFIED — #2 ANSWERED 2026-09-09; the label is corrected here 2026-09-09, round 10, finding [13], because naming an answered row as a live gate reads as a phantom approval dependency). **THE ENVELOPE — SPECIFIED 2026-09-13 (R2; Photon delegated technical decision; UNREVIEWED). DEFINED ONCE HERE; bound (2-CAP) cites it.** It is derived from the constraints below and three verified facts rather than invented: **(a)** per-response limits are unchanged, so a page is a SEPARATE RESPONSE — several pages inside one response would raise the per-response output; **(b)** a pass that outlives the harness Bash cap has no verified compliant path (`skills/litmus/SKILL.md:152-166`, `:241`; #368), so no detached lifecycle is available and the one overall deadline is the existing `LITMUS_TIMEOUT` budget; **(c)** both retry engines already give every attempt the REMAINING budget and cap each backoff (`scripts/lib/resolve-cli.sh:3401-3481` `_run_review_with_retries`; `:3702-3757` with attempts at `:3812`/`:3822` `_execute_codex`; verified at HEAD `34887cb7`). **HOW A PAGE IS REQUESTED:** the loop re-dispatches the SAME reviewer slot on the SAME `reviewed_diff_hash` in the SAME iteration, with a continuation instruction carrying the chain id, the next page index, the FINDING KEYS (defined below) of the findings already accepted in the chain and the chain's OUTSTANDING withheld keys; each page response obeys the unchanged per-response limits and states its own `completeness`. **CONTINUITY BY WITHHELD-KEY MANIFEST — 2026-09-13 (native 2a9a6900 finding [4]; Photon remaining-contract decision; UNREVIEWED; this replaces a same-day clause that only stated the limit):** a continuation page is served by a FRESH process (`resolve-cli.sh:3822`, `:3956`), and the resolver at HEAD `34887cb7` has no session-resume or result-cursor path, so NO continuity is claimed from CLI state. It is carried in the response contract and in the continuation driver instead (key, admission, validation and consumers completed 2026-09-13, Photon continuity-closure decision; UNREVIEWED; anchors in this clause verified at HEAD `34887cb7`). **THE FINDING KEY — DEFINED ONCE HERE; MADE COLLISION-SAFE 2026-09-13 (Photon assembly-closure decision; UNREVIEWED):** the six members `{file, line, category, severity, confidence, description}` projected from a finding's OWN existing fields — no new finding field and no identifier — with `line` the prompt contract's integer (`init-review-loop.sh:451`, `:541`; 0 for a file-level finding). **WHY THESE SIX AND NO FEWER:** the earlier triple `{file, line, category}` gave two DISTINCT findings at one place and category, differing in `description`, the SAME key, so emitting one discharged the other's withheld entry; `description` is the existing field that tells them apart; `severity` and `confidence` are the existing fields that `determine_status` (`merge-findings.py:52-88`) and the backstop writer (`run-review-loop.sh:974`) block on, so a finding withheld at a blocking grade is never discharged by the same finding re-emitted at an advisory one — the GRANT this row's closing rule forbids; `suggestion` is left out because no identity or authorization decision reads it. Keys compare by EXACT equality of all six members — strings byte-exact, numbers exact, no normalization and no similarity — and a withheld list is a MULTISET of keys: two withheld entries with equal keys are two findings, never one continuity cell, and each needs its own discharge. **The key is NOT the merger's deduplication and is not derived from it:** `deduplicate` pairs findings on the same file with a line within ±3 and description similarity > 0.6 (`lib/merge-findings.py:27`), a fuzzy relation that is not an identity, and nothing in this envelope matches by it. **THE WITHHELD MANIFEST:** a page stating `truncated` also lists `withheld`, the keys of the findings it found and did not emit — one entry per such finding, and no entry for a key the driver already carries as outstanding, which is handed back rather than restated (keys are not findings and do not count toward the per-response issue limit; a restated outstanding key counts as one more withheld finding, which can only cost a completion); a page stating `complete` carries no `withheld`, or an empty one. **OUTSTANDING AND DISCHARGE — owned by the continuation driver:** for each accepted page, FIRST each finding that page emits discharges at most ONE outstanding entry whose key is exactly equal, THEN that page's `withheld` entries are added — so a page's own emissions never discharge its own withheld entries, emitting finding A never discharges a withheld B whose key differs, and a second withheld entry with A's key stays outstanding until a second finding with that key is emitted (the earlier "(outstanding ∪ `withheld`) minus emitted keys" set arithmetic let a page that emitted A and withheld B at the same triple discharge B on the spot); a key is discharged ONLY by a later page emitting a finding whose key is exactly equal — not a nearby line, not a similar description, not a regraded severity — and a finding missing a key member discharges nothing; the continuation instruction hands the outstanding keys back verbatim, so restating one exactly is the producer's obligation; and a page may end the chain T1 `complete` ONLY while the outstanding set is empty. **SIZE BOUND, so the manifest cannot grow a response without limit:** the instruction for page `index` asks for at most 10·(P − index) + 1 withheld keys (entries counted with multiplicity; `index` is 1-based per BINDING, so page 1 may list at most 10·(P − 1) + 1 and the last page, `index` = P, at most one, which already trips T3), and a chain whose outstanding set after page `index` exceeds 10·(P − index) — the most its remaining pages can emit at the unchanged per-response limit — ends T3 `cap_exhausted` at that page. **ADMISSION AND VALIDATION — the driver's per-response PAGE VALIDATOR is the ONE admission point, fail-closed, run on each extracted response BEFORE it joins a chain or reaches the merger.** Each of the following is a failed page attempt (bound (2-CAP) T2), never merged and never allowed to replace the chain: a `page` that is not an object carrying exactly `chain_id` (string), `index` (integer, not boolean, ≥ 1) and `reviewed_diff_hash` (string), and each BINDING refusal below; a page whose `status` is not exactly `PASS` or `FAIL` (item 2i's RECOGNIZED `status` SET, cited not restated; the assembled `status` below reads it); an `issues` that is absent or not an array, or any `issues` member that is not an object (item 8's ENVELOPE PREDICATE — a RECOGNIZED `status` AND a list-shaped `issues` — cited, not restated; assembly concatenates `issues`, so a non-list page would otherwise be flattened before item 8's shape check sees it); an `issues` array with more than 10 members — the unchanged per-response limit (`init-review-loop.sh:464`/`:554`) that this row's 10·P capacity and size bound already assume, counted over every member whether or not it carries a FINDING KEY — so no page carrying more can end T1 however it states `completeness` (ENFORCED HERE 2026-09-15 post-14, native round-14 issues[3] MEDIUM; UNREVIEWED: the limit was prompt text only); an UN-PAGED dispatch keeps today's admission, and at the backstop the Stage-2 producer cutover that adopts paging states the same limit in `agents/pr-security-backstop.md`, a consequence of the existing limit, not a new one; a finding object missing a FINDING KEY member is still ADMITTED and discharges nothing, unchanged; a `withheld` that is present but not an array; a `withheld` entry that is not an object carrying exactly `file` (non-empty string), `line` (integer ≥ 0), `category` (string), `severity` (string), `confidence` (number, not boolean) and `description` (non-empty string); a page stating `complete` with a non-empty `withheld`, or with keys still outstanding once its own findings are applied; and a page stating `truncated` that leaves nothing outstanding — an empty or missing `withheld` with every carried key discharged. A `truncated` page with an empty `withheld` that still leaves carried keys outstanding is ADMITTED: it re-emitted part of the outstanding set within the per-response limit and names nothing new (corrected 2026-09-13: the earlier set arithmetic let such a page restate its outstanding keys harmlessly, but under the multiset above a restated key counts twice, so keeping the earlier refusal would leave that page no admissible form). **WHERE THE FIELDS STOP — NOT PERSISTED:** `page` and `withheld` are per-response fields, consumed by the driver and kept in `2a-env`'s per-page attempt record (bound (2-CAP) (d)); they are NEVER written to an authorization artifact. A chain reaches every downstream consumer as ONE ASSEMBLED OBJECT per slot, with EVERY member defined here (completed 2026-09-13, Photon assembly-closure decision; UNREVIEWED): **`status`** — `FAIL` if ANY accepted page states `FAIL`, else `PASS` (every accepted page states a recognized value, by admission above); never taken from the last page and never upgraded, so page 1 stating `FAIL` on medium-only findings survives a clean page 2; each reader keeps its own severity rule unchanged — the merger still recomputes status from `issues` (`merge-findings.py:186`), except that it never yields PASS from an input envelope that stated FAIL (item 8's rule and fixture E1, added 2026-09-23 (native run `98787112`, arbiter-reproduced; Chris-approved plan-text fix; UNREVIEWED)), and the backstop writer still yields `FAIL` on an explicit `FAIL` or any high (`run-review-loop.sh:974`); **`issues`** — the accepted pages' findings in index order; **`completeness`** — the chain's terminal value; **`reviewed_diff_hash`** — the chain's bound hash, which BINDING already requires of every accepted page and which a dispatcher that stamps it (the backstop, `run-review-loop.sh:1619-1622`) stamps identically on each; **`slot`** — the reviewer slot id the chain was requested for (item 2a bound (ii)(b)), stamped by the dispatcher and never taken from producer text; it is the merger's slot channel (bound (ii), Stage 1 owner) and is REMOVED before any persistence boundary, exactly as `page` and `withheld` never reach one; **`model`** — the ONE backend that actually served every accepted page, read from `2a-env`'s per-page attempt record (bound (2-CAP) (d), filled only from its SERVING-BACKEND IDENTITY CHANNEL, which is IMPLEMENTATION-PENDING there) or, at the backstop, from the dispatcher's own stamp (`:1619-1622`, argv `opus`), never from producer text; when accepted pages were served by DIFFERENT backends — the in-chain fallback this row permits — `model` is ABSENT, never picked and never fabricated, so a reader that requires it refuses fail-closed (the backstop writer's `missing model`, `:926-928`) while the merger, which reads no `model`, merges the chain as it merges a fallback-served review today. (The PR lead's marker `model` is the writer's constant at `:741`; paging neither reads nor changes it.) Item 2i(B)'s Stage 1 deliverables (`ALLOWED_TOP`, the constructor at `:976-983`, the backstop schema, `write_codex_lead_verdict`, the marker payload) carry `completeness` only and are UNCHANGED by paging. **CONSUMERS, each labelled PLANNED or TODAY:** **(i) extractor — TODAY, no change:** `_resolve` returns the decoded object verbatim (`extract_review_json.py:690-699`, serialized at `:836`), so `page` and `withheld` already survive extraction; **(ii) continuation driver — PLANNED, a `2a-env` deliverable:** the page validator, outstanding set, exact discharge, size bound and assembly above; **(iii) merger — PLANNED only as item 2i(B) (c-3)/(c-4) already scope it:** it receives the one assembled object per slot, so the fold sees ONE participant per chain rather than one `truncated` participant per page, and dedup runs once at `:185` (constraint (3)); it does NOT compute or read the outstanding set, which is the driver's alone; **(iv) `lib/validation.sh` `validate_review_schema` (`:114-164`) — TODAY fail-OPEN,** it requires no `line` and no paging field and always returns 0 (`:162`), so it is NOT the admission point; `page` and `withheld` join its accepted set beside `completeness` with NO gate effect, and `review-output.schema.json` (no runtime consumer, item 2i(B)) takes the same documentation-only change; **(v) PR backstop — TODAY its strict writer REFUSES both fields,** fail-closed: `ALLOWED_TOP` (`run-review-loop.sh:911`) rejects any unknown top-level key and `ALLOWED_ISSUE` (`:890`) any unknown issue key. `--run-backstop` pipes one captured `claude -p` verdict to `_persist_backstop_verdict` (`:819`), so paging that dispatch makes it an ADOPTION SITE: the driver sits between capture and persist, and the persist step receives the assembled object, never a page. **BACKSTOP ADOPTION IS SCHEDULED, NOT OPTIONAL — PLACED 2026-09-13 (Photon assembly-closure decision; UNREVIEWED):** the backstop states no `completeness` until its producer, `agents/pr-security-backstop.md`, joins item 2i(B) Stage 2's producer cutover (item 2i(B), cited); before that it cannot state `truncated` and there is nothing to page, so its adoption lands IN THAT SAME STAGE-2 CHANGE — not in `2a-env` and not later. It reuses the `2a-env` driver, which has already landed because `2a-env` precedes `baseline captured` and Stage 2 lands downstream of that milestone. Stage 1 sits in the 2i NODE, whose graph edge 2i NODE ──► 2a-env places it earlier still, so `ALLOWED_TOP` already admits `completeness` (that edge names the NODE while this reason is (B) STAGE 1's alone; the dependency graph's GATE line carries the correction and the narrowing question as *Still needs Chris* #17 — cited here, not restated — ANSWERED (B) 2026-09-17 BY PHOTON, the 2i NODE edge RETAINED. ADDED 2026-09-17, native run `7886ed9f` MEDIUM; UNREVIEWED), and the persist step receives exactly `{status, model, issues, reviewed_diff_hash, completeness}` — all members the writer admits after Stage 1 — while `page`, `withheld` and `slot` never reach it. From that change, a backstop chain that does not end T1 is NOT persisted: `--run-backstop` (`:1028`) exits non-zero with no artifact, as a failed dispatch does today, so the gate stays blocked. That is an authorization-outcome change on the backstop path, stated here rather than left silent; it lands inside Stage 2, the change that already makes a non-`complete` verdict non-authorizing, so it adds no gate change before Stage 2 and leaves Stage 1 outcome-neutral. Until that change the backstop dispatch is un-paged and keeps today's semantics and budget exactly. The writer's rules (`:911`-`:974`) and the backstop schema change in no other way; the size bound keeps this row's arithmetic (the backstop contract names no per-response issue limit, so the constant 10 is only conservative there); and the backstop dispatch has no droid arm, so its assembled `model` is never mixed. **IMPLEMENTED TODAY: NONE** — `grep -rn "withheld\|chain_id\|discharges"` over `skills/litmus/scripts/`, `scripts/lib/resolve-cli.sh` and the extractor finds no paging field, validator or reader, only three unrelated prose comments in the extractor. **HONEST LIMITS:** the manifest is the producer's own statement, trusted exactly as far as `completeness` itself; it makes a found-and-withheld finding that a fresh page fails to reproduce DETECTABLE and non-`complete`, it does not raise recall, and no live producer is claimed to honour it until measured. EXACT discharge also costs completions: a fresh page that re-finds a withheld finding at a drifted line, or under another category, leaves its key outstanding, so that chain never reaches T1 and ends non-authorizing — a cost accepted, not softened by fuzzy matching. The six-member key widens that cost to a reworded description or a regraded severity or confidence, accepted on the same terms. A withheld entry also carries its `description`, so a `truncated` page's output BYTES grow with its manifest: the COUNT is bounded by the size bound, and at the backstop the bytes are also bounded by the existing 4 MiB envelope cap (`run-review-loop.sh:1320`), but a page is not byte-identical to an un-paged response; the per-response ISSUE limit, the one deadline and P are unchanged. Continuity is a property of the chain, not of the merged output: the merger's existing dedup (±3 lines, similarity > 0.6) may still collapse two findings the chain kept distinct, unchanged (constraint (3)). ACCEPTANCE (the CONTINUITY PAIR): page 1 `truncated` withholding key K, then a fresh page stating `complete` without emitting a finding keyed exactly K, is a failed page attempt and never `complete`; the same chain whose later page emits a finding keyed exactly K ends T1 `complete`. ACCEPTANCE (MANIFEST VALIDATION, fail-closed): a later page emitting a finding at K's file and category but one line away does NOT discharge K; a `withheld` entry missing `line`, or missing `description`, is a failed page attempt; a page-1 `withheld` of more than 10·(P − 1) keys ends the chain `cap_exhausted`; and after an accepted page, a page whose `issues` is `{}`, `null` or `[null]` is a failed page attempt with the chain's accepted pages unchanged, while a page with `issues: []` is admitted (valid-empty control); and a page of 10 findings is admitted, while a page of 11, and a single page of 31 findings stating `complete` with no `withheld`, are each a failed page attempt that never ends T1. ACCEPTANCE (COLLISION AND ASSEMBLY, added 2026-09-13, Photon assembly-closure decision; UNREVIEWED): page 1 `truncated` emitting finding A and withholding a DISTINCT finding B at the same file, line and category with a different `description`, followed by a page that emits A again and states `complete`, is a failed page attempt with B still outstanding, while a page emitting B ends T1 `complete`; two withheld entries with EQUAL keys need two emitted findings with that key, and a page emitting only one cannot end `complete`; a page re-emitting withheld B at a lower `severity` does NOT discharge B; page 1 stating `FAIL` with only medium findings followed by page 2 stating `PASS` assembles to `status` `FAIL`, which the backstop writer persists as `FAIL`; a chain whose accepted pages were served by two backends assembles with NO `model`, which the backstop writer refuses and the merger merges; a first page with `index` 0 is refused; and two assembled chains carrying the SAME `slot`, with a second required slot absent, do not authorize. Binding on the Stage-2 change that adopts paging at the backstop, not on `2a-env`: a backstop chain ending other than T1 writes no artifact. **HOW PAGES ARE FRAMED AND IDENTIFIED:** each page object carries `page: {chain_id, index, reviewed_diff_hash}`, and a page whose `reviewed_diff_hash` differs from its chain's is refused and never merged. **HOW PAGES ACCUMULATE RATHER THAN REPLACE:** each response is still extracted as ONE review object, so `try_last_review_object`'s per-output contract (`extract_review_json.py:735`) is unchanged; accumulation happens ACROSS responses in the loop, which assembles the chain's accepted pages into the one object per slot defined at WHERE THE FIELDS STOP and hands that to `lib/merge-findings.py`, while an object WITHOUT `page` keeps today's replace semantics (constraint (1)) ONLY for an un-paged dispatch. **BINDING — ADDED 2026-09-13 (native 2a9a6900 finding [7]; UNREVIEWED):** a response joins a chain only if its `chain_id` is the requested chain and its `index` is the requested next index — `index` is 1-BASED (stated 2026-09-13, Photon assembly-closure decision; UNREVIEWED): a chain's first page is requested at `index` 1 and its n-th at `index` n, so an accepted `index` lies in 1..P and equals one more than the chain's accepted page count; a foreign `chain_id`, a repeated, skipped or out-of-order `index`, and a response WITHOUT `page` to a continuation request are each refused, counted as a failed page attempt (bound (2-CAP) T2), never merged and never allowed to replace the chain. ACCEPTANCE: a foreign-chain page, a wrong-index page and a paged-then-unpaged response are each refused with the chain's accepted pages unchanged. **HOW THE MERGER COMBINES:** dedup runs once, over the accumulated pages (constraint (3)). **ONE OVERALL DEADLINE (constraint (5)) — REUSED, NOT NEW:** every page dispatch and every retry of a page draws from ONE remaining `LITMUS_TIMEOUT` budget started once per chain, through the existing remaining-budget engines of fact (c) — never a fresh budget per page. That chain budget is itself BOUNDED BY THE INVOCATION (CORRECTED 2026-09-15, native FINAL13 issues[8] MEDIUM; reserve and scanner stage CORRECTED 2026-09-15 post-14, native round-14 issues[1], [14], [15] and [16] MEDIUM, Photon correction decision; UNREVIEWED): an invocation clock starts at loop invocation, so time spent before the chain (`run_sast_scan` `run-review-loop.sh:2899` and `run_markdown_checks` `:2988`) is charged against it, and each page dispatch or retry receives min(chain remaining, invocation remaining (of the 600s harness cap) − persistence reserve). **THE PERSISTENCE RESERVE IS A FIXED 60 s:** the headroom the SHIPPED DEFAULT encodes, 600 − 540, read from the two existing literals (the harness cap and the `:-540` default at `:3254`) and never from the operator's effective `LITMUS_TIMEOUT`. It REPLACES the earlier definition as the headroom between the effective `REVIEW_TIMEOUT` and the cap, which was 0 at `LITMUS_TIMEOUT=600`, negative above and inflated below (480 s at 120) — settings `skills/litmus/SKILL.md:161`/`:243` name as live. A raised `LITMUS_TIMEOUT` therefore moves only the chain bound while the invocation bound keeps the reserve, and a lowered one leaves the chain bound governing. T4 records its terminal value inside the reserve rather than after a caller kill; that 60 s suffices for that write is NOT measured. **THE SCANNER AND MARKDOWN STAGES DRAW ON THE SAME INVOCATION CLOCK:** today each Semgrep, TruffleHog and per-file ShellCheck call gets a fresh `LITMUS_SAST_TIMEOUT=30` window (`lib/sast-runner.sh:75`/`:117`/`:184`, per file at `:140-156`), per-file markdownlint runs with no timeout (`lib/markdown-checker.sh:35-38`) — its per-file MEMBERSHIP TEST and failure classification were added to bound (i-c) 2026-09-17 (native run `7886ed9f` MEDIUM; UNREVIEWED) and are defined once there, cited here and not restated —, commit mode's file gate counts only files with additions (`run-review-loop.sh:2846-2852`) and PR mode only warns (`:2786-2800`) — anchors from here to the end of this bound at HEAD `34887cb7`, while `:2899`, `:2988` and `:3254` above are the `727d4652` checkpoint's (HEAD `:2889`, `:2978`, `:3244`) — so the stage grows with the change. Each such call instead receives min(its existing per-call window — `LITMUS_SAST_TIMEOUT`'s, which markdownlint adopts through the same `_sast_timeout` wrapper, no new value, on both entry paths named below —, invocation remaining − persistence reserve) and captures to a regular file as bound (2-CAP) (d)'s CAPTURE OWNERSHIP requires of every inner reviewer capture, because `$(_sast_timeout …)` waits on any descendant holding its pipe. A call whose window is ≤ 0 is not launched; a call not launched, cut off by its window, or whose file the scan root should carry and does not (SCANNED BYTES, bound (i-c), cited) is UNSCANNED under that scanner's membership test and makes the scan INCOMPLETE through (i-c)'s existing failure token — no new mechanism. **MARKDOWN ENTRY PATHS — THE HELPER IS REQUIRED ON BOTH:** `lib/markdown-checker.sh` has two real entries, sourced by the driver (`run-review-loop.sh:2188`, called at `:2978`) and run directly (`lib/markdown-checker.sh:221-229`), and `_sast_timeout` is defined in `lib/sast-runner.sh`, which the checker does not source; the driver's order (`:2182` before `:2188`) supplies it on the first path only. So before launching any lint the checker itself requires `declare -F _sast_timeout` and, when it is absent, sources `sast-runner.sh` from its own directory by the `$(dirname "${BASH_SOURCE[0]}")` form `sast-runner.sh:14` already uses — sourcing does not run that file's direct-run block (`:291`), and its `set -euo pipefail` then holds on the direct entry as the driver's (`:193`) already does on the sourced one. If the helper is still undefined, no lint is launched and every `.md` name is UNSCANNED: through (i-c)'s token when sourced, and on direct entry as a non-zero exit naming the unscanned files on stderr, never `[]`. Direct entry has no invocation clock and no T4 to persist, so its window is `LITMUS_SAST_TIMEOUT`'s alone, and a call it cuts off is UNSCANNED with the same non-zero exit; direct entry is not a gate path, and nothing here makes it one. **THE URL CHECK IS PART OF THE MARKDOWN STAGE — ADDED 2026-09-16 post-15 (native round-15 issues[3] MEDIUM; Photon correction decision `840-post15-plan-correction-decision.md`; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED).** Today `_md_check_urls` (`lib/markdown-checker.sh:67-162`, run only when `LITMUS_CHECK_URLS=1`, `:198`) runs one `grep` and one `python3` per matched line in two extraction passes (`:82-100`, `:103-115`), keeps the first 20 URLs (`head -20`, `:119`) and gives each `urlopen` a fresh `LITMUS_MARKDOWN_TIMEOUT` (default 5 s, `:69`; `:138`) — a per-socket-operation timeout, not a per-URL wall bound — reading WORKING-TREE files and consulting no invocation clock. **THE RULE:** extraction and every request run in ONE Python process launched through the CAPTURE OWNERSHIP launcher and captured to a file, with window min(20 × `LITMUS_MARKDOWN_TIMEOUT` + 5, invocation remaining − persistence reserve) on the sourced path — the existing 20-URL limit times the existing per-URL timeout, no new value — and that product alone on direct entry; it reads the pinned bytes of the scan root (SCANNED BYTES, bound (i-c)); file names reach Python as argv data and are never interpolated into its source. **D2 CONSEQUENCE (Photon technical default):** a launch refused (window ≤ 0) or cut off, or any URL left unchecked, makes its `.md` names UNSCANNED ⇒ INCOMPLETE through (i-c)'s token — on direct entry a non-zero exit naming them — so an opt-in run that HEAD silently truncates now blocks. **SOURCE-READ, NOT REPRODUCED (post-15 diagnosis, `~/.hermes/reports/840-post15-end-to-end-recovery.md`):** N1 — the file name is interpolated into Python SOURCE TEXT at `:96` and `:111`, so a staged `.md` whose name contains `'` runs arbitrary Python when the opt-in is set; N2 — the first extraction loop (`:82-100`) prints to the function's stdout, which the merge's bare `pass` then drops; the single-process rule removes both, and neither was executed. ACCEPTANCE: twenty stalled local URLs with 15 s of invocation remaining end the stage within its window and INCOMPLETE; a pinned name carrying a Python-injection payload creates nothing; and a reachable local URL set completes with the stage SCANNED (control). No file gate is added in either mode: the cost is that a large change ends INCOMPLETE and its chain T4 instead of being killed at the cap. **THE WINDOW BOUNDS THE WAIT ONLY WITH A KILL-AFTER AND GROUP OWNERSHIP — CORRECTED 2026-09-16 post-15 (native round-15 issues[2] MEDIUM; Photon correction decision `840-post15-plan-correction-decision.md`; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED; the earlier sentence keeping a TERM-ignoring leader as a stated residual contradicted this bound's DEFINED-outcome promise and is withdrawn).** Today `_sast_timeout` (`lib/sast-runner.sh:22-55`) passes no kill-after (`:29`, `:31`) and its perl arm neither calls `setpgrp` nor signals more than the direct child (`:38-46`), so a TERM-ignoring leader holds the stage past its window. It therefore delegates to bound (2-CAP) (d)'s CAPTURE OWNERSHIP launcher — `_portable_timeout` on its non-review arm — whenever `declare -F _portable_timeout` holds, which it does on the driver path (`run-review-loop.sh:15` before `:2182`); on the direct entries (`lib/sast-runner.sh:291`, `lib/markdown-checker.sh:221`) the helper rule of MARKDOWN ENTRY PATHS above extends to those SEMANTICS, not merely to a function of that name: a coreutils arm with `-k 5` and the SAME perl text as the library, byte-equality pinned by a test. Each call's kill-after and reap grace are charged against the invocation clock before the reserve (Δ4, cited). A `setsid`/`setpgid` escapee is reached by no group signal; its file capture keeps it from holding the wait, and it may outlive the stage — a stated residual that no longer denies a DEFINED outcome. No part of this stage is measured against real scanners. Expiry stays fail-closed (T4, never `complete`); this bound promises every chain a DEFINED outcome, not time: when the invocation's budget is exhausted, a later chain ends T4. **THE FALLBACK IS THE ONE PLACE THIS IS A CHANGE RATHER THAN A REUSE:** the post-loop droid escalation is granted its own FULL `_ECX_DURATION` (`resolve-cli.sh:3956`, `:3963`), outside the shared budget, which constraint (5) forbids for a paged chain. So within a chain a fallback dispatch receives only the chain's REMAINING budget (as bounded above by the invocation); a fallback that runs and fails ends the chain `errored`, and one the remaining budget cannot fund is the overall deadline firing — findings so far are retained, and the result is never a clean verdict. A single un-paged review keeps today's fallback budget unchanged. **HARD PAGE CAP (constraint (2)):** P is a fixed value recorded with the contract version in the protocol artifact — a value OF the contract version, implemented by `2a-env` and TRANSCRIBED by 2p at `protocol-approved` (conjunct added to that graph node 2026-09-13, native 2a9a6900 finding [5]; UNREVIEWED), so a different P is a new contract version and a new cohort, never a protocol edit. **P = 3 — SELECTED 2026-09-13 as a TECHNICAL DEFAULT (Photon remaining-contract decision; UNREVIEWED), not measured producer evidence and not a cap waiver:** three pages at the unchanged per-response limit of 10 issues (`init-review-loop.sh:464`) admit 30 findings, which exercises every OVERFLOW fixture (eleven findings need two pages; the cap-plus-one fixture uses 31) while every extra page draws on the same one `LITMUS_TIMEOUT` budget, so a larger P buys more T4 deadline stops rather than more `complete` chains; reaching it with a page still stating `truncated` yields `cap_exhausted` (constraint (4)). **Constraints any transport must satisfy, recorded now because they were verified and would otherwise be rediscovered late (the list is the authority; its length is derived, never restated):** (1) accumulating arbitrary review-shaped objects would **destroy the extractor's existing ability to treat a later object as a CORRECTED verdict that replaces**, so the paged and un-paged cases must be distinguishable — a page must be self-identifying, and an object without that identification must keep today's replace semantics; (2) continuation within a single iteration must carry a **hard page cap**, or overflow becomes a way to keep reviewing past the iteration-based stopping limits; (3) dedup must run **across pages after accumulation**, not per page, or the merger's own similarity matching never sees the duplicates it exists to collapse. **(4) Cap exhaustion is a THIRD terminal outcome, distinct from both `complete` and `truncated` — decided 2026-09-08, because three of this row's own requirements could not hold simultaneously at the cap.** With the 10-issue page budget verified at the checkpoint (`init-review-loop.sh:464`/`:554`) and a finite page cap P, a review carrying more than 10P findings could only omit one — the laundering this row exists to remove — or mark itself `truncated`, which blocks a review whose every omitted finding may have been advisory. The resolution is neither: such a review is stamped `cap_exhausted`, which is **non-authorizing and requires SCOPE REDUCTION** (a smaller diff, a split commit), and which is **approved as an operational limit rather than a defect to be engineered away**. **The cost is stated rather than hidden: an advisory-only review CAN fail to authorize at the cap.** The unqualified promise that eleven advisory findings never block holds only **within** the cap; beyond it, unreportability withdraws authorization regardless of severity. **(5) OVERALL DEADLINE — ACCEPTED 2026-09-09 (Chris, round-13 finding [12]); UNREVIEWED.** Any transport must specify a single OVERALL deadline spanning all continuation pages, retries and fallback attempts — or an explicitly supported DETACHED execution lifecycle not bounded by the caller's timeout — and must define the outcome of a deadline hit MID-CONTINUATION. Per-page budgets do not compose, and the hazard is already verified at the checkpoint, which is this list's own admission criterion: `run-review-loop.sh:3254` sets `REVIEW_TIMEOUT="${LITMUS_TIMEOUT:-540}"` explicitly *"UNDER the 600s harness Bash cap"*, `:1134` carries the same `:-540` for the PR backstop, and `:1329` already records that a 3-attempt sequence *"could reach 3x the timeout plus backoff -- past the 600s"* cap; page dispatches each carrying a fresh budget reproduce that same killed-process failure under any finite page cap P, and a process killed mid-continuation has no defined outcome under the clauses above. A deadline hit mid-continuation **RETAINS the findings captured so far** and yields a **NON-AUTHORIZING incomplete** outcome — `truncated`, or `cap_exhausted` per (4) — **never a clean verdict**. ACCEPTANCE: a fixture in which the deadline fires after the first page retains that page's findings and does not authorize; a fixture whose scanners each consume their full timeout before a continuation chain still records T4 before the 600s cap; in commit mode and in PR mode, enough slow shell files that their per-file windows sum past the cap end the scan INCOMPLETE and record T4 before the cap; a markdownlint stub sleeping past the remaining budget is cut off and makes the scan INCOMPLETE; the same stub through a direct `bash lib/markdown-checker.sh` entry is cut off at `LITMUS_SAST_TIMEOUT` and exits non-zero naming the file; a checker that cannot define `_sast_timeout` launches no lint and reports its `.md` names UNSCANNED on either entry, never `[]`; a scanner stub leaving a detached descendant on its stdout returns by its window; a TERM-ignoring scanner leader is killed with its group by its window plus the kill-after, makes the scan INCOMPLETE and still lets T4 be recorded before the cap; and at `LITMUS_TIMEOUT=600` and `=700` T4 is still recorded before the cap with the 60 s reserve intact, while at `=120` the chain bound governs. **FIXTURES — BINDING IN `2a-env`'s ACCEPTANCE NOW THAT THE ENVELOPE IS SPECIFIED (2026-09-13, R2; UNREVIEWED). Until then they were conditional on an unchosen envelope, which the history below records.** This row's own bar is "specify the TRANSPORT before mandating fixtures", and the transport is DUE in `2a-env` under item 2a bound (2-CAP) (moved 2026-09-13; UNREVIEWED), no longer behind the convergence-policy half, which stays BLOCKED on `baseline captured` (its *Still needs Chris* #2 conjunct is SATISFIED — #2 ANSWERED 2026-09-09; the label is corrected here 2026-09-09, round 10, finding [13], because naming an answered row as a live gate reads as a phantom approval dependency). *History:* the four below were written as a specification of what a chosen envelope must make testable, not schedulable work. With the envelope specified they are binding acceptance of `2a-env`, together with TWO envelope fixtures: a page carrying a foreign `reviewed_diff_hash` is refused and never merged; and a chain whose fallback the remaining budget cannot fund ends non-`complete` with its earlier pages' findings retained. The transport gates them (marker added 2026-09-09, round 9, finding [25]). The four: **eleven advisory findings, which must NOT block**; **a blocking finding beyond the first page, which MUST block**; the reverse case the current extractor makes possible — **a blocking first page followed by a clean final page, which must still block**; and **a cap-plus-one advisory fixture** — one advisory finding more than the hard cap can carry — which must yield `cap_exhausted`, must NOT authorize, and must name scope reduction as the remedy. **(End of the envelope fixtures; what follows was already binding.)** Neither omission nor iteration advancement may ever **GRANT** an authorization that the undeduped, uncapped review would not (the earlier wording said "change the authorization outcome", which cap exhaustion contradicts by design — it WITHDRAWS one).
