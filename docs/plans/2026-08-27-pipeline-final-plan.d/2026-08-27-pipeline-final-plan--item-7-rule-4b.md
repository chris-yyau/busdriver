# Item 7 — rule (4b): genuinely size- or containment-indeterminate ⇒ DENY

> **INDEPENDENT DESIGN FILE: BINDING, AND UNREVIEWED.** This file is part of the requirements of
> `docs/plans/2026-08-27-pipeline-final-plan.md` (the parent). The parent cites it at § *Item 7*, rule (4b) and does
> not restate it. It is reviewed on its own: `init-design-review.sh docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-7-rule-4b.md`, then
> `run-design-review-loop.sh`. Its review verdict covers only this file's spec hash. A verdict on the parent
> does not cover this file, and a verdict on this file does not cover the parent. It is not history.
>
> **Context for a reviewer who reads only this file.** The body below was moved verbatim from the parent on
> 2026-09-23 (Chris-approved split), and this header was added the same day; the body is unchanged. Item
> numbers, node names such as `2a-env`, `baseline captured` and `protocol-approved`, and bound labels such as
> (i-a), (i-b), (2-ORD) and (2-CAP) are defined in the parent's execution-order table (§ *Execution order
> and acceptance*) and § *Still needs Chris*, and, since the parent's SECOND MOVE (2026-09-24), in the
> children: the dependency graph, *Standing fallback*, *Recommended next actions* and § *Item 2* in the 2p
> file; § *Item 2i* and § *Item 8* in the item 8 file; § *Item 2a* in the (i-a) file; § *Item 7* in the
> item 7 file; § *#840 proposed revisions* in the 2p file. The parent keeps only one-line pointers there (2026-09-26; UNREVIEWED). A lens reviewing this file receives this file and is not assumed to open the parent or the checkout.
> Line anchors `:NNN` into the parent point at the parent as it stood before the 2026-09-23 splits. Anchors
> into other files name their own tree or checkpoint.

**(4b) Genuinely size- or containment-indeterminate — DENY (fail-closed).** **Stated over KINDS, not over a residual clause. RESOLVED 2026-09-09 as a WORKER DEFAULT under *Still needs Chris* #13 — the "DECIDED 2026-09-08" label was struck 2026-09-09 (round 9, finding [0]) because no approval carried it; this disposition carries Chris's 2026-09-09 delegation instead, is vetoable by row ID, and is NOT an operator approval. It is the one live-behaviour TIGHTENING in this plan and is named as such in #13.** **(4b) DENIES EXACTLY THE KINDS THE CANONICAL TABLE ABOVE MARKS DENY, AND THE FLAT ENUMERATION THAT STOOD HERE IS RETIRED — CORRECTED 2026-09-09 (`批准840`, round-14 findings [1] and [11]; UNREVIEWED).** That enumeration listed `oversized`, `uncontained`, `size_indeterminate` and `probe_unavailable` with their sites, and **omitted `trust_root_unavailable` entirely** — which is how the same kind came to carry DENY at `:75` and ALLOW at `:234-239` with no surface holding both readings at once. Under the define-once rule this paragraph now names no member and no site: **the table is the authority, and this rule is simply "deny every kind the table marks DENY".** Two consequences of that table are worth stating here because rules elsewhere depend on them and neither is a restatement of a disposition: `:198`'s non-ENOENT branch is `probe_unavailable` and is DENIED, while its open-time-ENOENT branch takes whatever kind the CANONICAL TABLE, entry 8, assigns on the identity test there — never an unconditional `not_found` (POINTER ADDED 2026-09-10, round-18 finding [6]; UNREVIEWED), so the branch split (stated once above) is load-bearing for this rule; and `:75` now contributes a single kind rather than three. CORRECTED 2026-09-09, round-13 finding [3]; UNREVIEWED). Every one of the sixteen sites is now mapped to a kind and every kind has a disposition, so the earlier phrase "the remaining unclassified `null` sites" is **retired**. **The follow-on claim that "no site falls through to an unstated default" was WRONG and is withdrawn here (round 9, finding [8]): the sixteen-site table is accurate but it is not the gate's decision surface.** Verified at the checkpoint in `scripts/hooks/pre-read-size-advisory.js`, `run()` returns ALLOW on four paths that never consult a probe kind — `:219-221` (payload/JSON parse failure), `:224-226` (`offset`/`limit` present, or a non-string/unsafe `file_path`), `:234-239` (failure to establish the trust ROOT via `fs.realpathSync(cwd)` / `fs.statSync(cwdReal)`) and `:242-243` (`resolveContained(...) === null`) — and a fifth, `:247-248`, folds `countLines(...) === null` into `lines <= THRESHOLD`, so a failed probe and a small file take the same branch. Against (4b) as written, `:234-239` and `:247-248` are fail-OPEN on exactly the kinds (4b) says DENY, and `:242-243` is a uniform fail-OPEN on the single DENY kind `uncontained` — **CORRECTED 2026-09-09 (`批准840`, round-14 finding [8]; UNREVIEWED): this read "collapses an allow-kind and a deny-kind into one allow", which was true only under the withdrawn three-way `:75` split; with `resolveContained` returning `null` for `uncontained` and nothing else, no allow-kind reaches this branch. The reachability proof is stated once above and cited here.** `:224-226` is the exception and stays an allow **only in its PATH-SAFETY half** — CORRECTED 2026-09-09 (round 10, finding [3]). The unqualified "stays an allow" was the other side of the rule (1) contradiction: the guard fires on `offset` or `limit` being defined at all, so preserving it whole made every rule-(1) denial fixture unsatisfiable. Under (1-a) above it splits: the non-string / empty / unsafe `file_path` half stays an allow (a scope filter over paths this advisory cannot resolve), and the range half exempts only a WELL-FORMED, IN-BUDGET range, sending every malformed or over-budget range to the disposition table. Read (4b) and rule (1) together; neither is a residual clause of the other. **The discriminated result contract is TAGGED KINDS ON THE PROBES ONLY: `resolveContained` / `scanWindow` / `countLines` each return a tagged `ProbeResult` carrying its kind, and (4b) then sets the disposition rather than the site. **SUCCESS PAYLOADS ARE EXPLICIT AND PER-FUNCTION — ADDED 2026-09-10 (round-18 findings [2] and [9]; Photon technical decision, UNREVIEWED): `resolveContained` ⇒ `{kind: 'resolved', path}`; `scanWindow` ⇒ `{kind: 'scanned', lines, lastByte, offset}`; `countLines` ⇒ `{kind: 'count', count}`. These are INTERMEDIATE PAYLOADS, NOT DISPOSITION MEMBERS:** the canonical table's TEN kinds and their per-kind dispositions are unchanged, `resolved` / `scanned` / `count` are never added to it, and nothing is disposed by a blanket "not a failure ⇒ ALLOW". Failure kinds on those three functions stay exactly as already mapped. **THERE ARE TWO UNWRAP BOUNDARIES, NOT ONE.** Naming only `:247` left the first unstated, and a literal implementer would have broken the ordinary path: a tagged object is never `=== null`, so `:241-243` goes dead, `openSync` throws on an object, the outer catch at `:198` returns null, `:247` swallows it, and `:251` becomes unreachable for a contained over-threshold file. `run()` therefore unwraps at **BOTH** — at **`:241`** and at **`:247`** — and **AT EACH BOUNDARY IT SWITCHES ON `result.kind`**: `count` proceeds to the threshold comparison; every ALLOW kind in the CANONICAL TABLE returns `{exitCode: 0}`; every DENY kind returns the (1-b) deny result, never with `additionalContext` (round-14 [16]). That switch is the CANONICAL TABLE's ONE APPLICATION SITE — the dispositions are applied here and nowhere else, and no kind is added, removed or re-disposed by it. During **7a-i** every kind returns `{exitCode: 0}`, so `run()`'s SIX returns — `:220`, `:225`, `:239`, `:243`, `:248`, `:251` — remain OBJECT-IDENTICAL and the hook object stays UNTAGGED; **7b flips only the DENY arms.** That is also how entry 6 (`uncontained` — `resolveContained` returns bare `null` at `:75`, which `:241-243` maps to `{exitCode: 0}` today) acquires its DENY at 7b: by the switch, not by a second dispatch. Kinds whose disposition is CONDITIONAL keep it — the switch applies the CANONICAL TABLE's disposition for the kind it is handed, and decides none of its own. (APPLICATION SITE ADDED 2026-09-12, native findings [23] HIGH and [00] MEDIUM; UNREVIEWED) **THE SUCCESS PATH CONTINUES PAST THE SWITCH — CLARIFIED 2026-09-12 (native ca145c70 finding C2, codex issues[2]; native category `implementation` is TDD-deferred, so this is CONTRACT PRESERVATION of acceptance already stated in the 7a status cell, NOT a new requirement; UNREVIEWED).** "Every ALLOW kind returns `{exitCode: 0}`" is the disposition, not the whole behaviour, and read alone it would delete the one ALLOW outcome this item exists to produce. `count` is the success kind: it does **not** terminate at the switch — it **proceeds to the threshold comparison**, and a CONTAINED file OVER the threshold still emits the `:251-258` `additionalContext` advisory exactly as today. That advisory IS an `exitCode: 0` allow, so it is consistent with the ALLOW disposition rather than an exception to it. **This is the same acceptance the 7a criterion already pins** — `run()`'s SIX returns `:220`, `:225`, `:239`, `:243`, `:248`, `:251` object-identical before and after the refactor, `:251` among them — and it is restated here only because the switch sentence sits between a reader and that criterion. 7a-i therefore keeps every kind on `{exitCode: 0}` **including the over-threshold advisory**. **THE 7b HALF OF THAT SENTENCE WAS WRONG AND IS WITHDRAWN — CORRECTED 2026-09-12 (native 4efaea28 finding B1, codex issues[1] + grok issues[0] `architecture`, conf 0.95; UNREVIEWED).** As first written it said 7b flips the DENY arms *without touching the `count` arm or the threshold comparison*, which — combined with canonical entry 1 disposing `count` as "the threshold decides (not a failure kind)" — left **no deliverable anywhere that denies an over-threshold contained read.** Measured: `:246` counts, `:247-249` returns `{exitCode: 0}` at or below THRESHOLD, and `:251-258` returns `{exitCode: 0, additionalContext}` for a contained file ABOVE it, which `run-with-flags.js:83-84` renders as advisory text. So after BOTH halves land, an unbounded Read of a contained **201-line** text file is still ALLOWED — the exact case this item's opening sentence commits the gate to denying, and the same gap defeats the unbounded-`cat` clause (c), which routes to the same entry. (`oversized`, entry 4, is `:156` — `size > MAX_SCAN_BYTES`, 50 MiB — a different condition that does not cover this.) **THE CORRECTION, IN 7b ONLY:** after the switch unwraps `kind === 'count'` and the threshold comparison reports OVER threshold, 7b **REPLACES the advisory with the (1-b) deny transport** — `hookSpecificOutput.permissionDecision: 'deny'`, and per round-14 [16] with **no `additionalContext` on that object**. This is the APPLICATION of canonical entry 1, not a new kind and not a rewrite of its disposition: the table stays the one authority, and the threshold stays what decides. **7a-i is untouched, so the SIX-return object-identity criterion at `:220`/`:225`/`:239`/`:243`/`:248`/`:251` still holds exactly as pinned** — the change lives entirely in 7b, which is where DENY arms were always going to flip, and the round-15 [1] separation is unaffected. **Paired fixture (7b):** the same contained 201-line text file emits the `:251-258` advisory BEFORE activation and `permissionDecision=deny` AFTER, while `limit=50` on that same file stays ALLOW in both states. **THE SWITCH IS NOT THE TABLE'S ONLY APPLICATION SITE — ENTRY 10 HAS A SECOND ONE, AND THE EARLIER "ONE APPLICATION SITE" CLAIM IS WITHDRAWN (corrected 2026-09-12; native c94fb702 finding [3], agy issues[2] escalated to HIGH, conf 0.9; UNREVIEWED).** Canonical entry 10 maps `trust_root_unavailable` to DENY and sites it at `:234-239`, but the trust-root resolution `fs.realpathSync(cwd)` sits in a try at `:235` whose catch **RETURNS at `:239` — before `resolveContained` at `:241`**, so the function has already returned and no switch arm can reach entry 10's disposition. It is inside `run()`, not a probe, so it produces no ProbeResult to tag, and `resolveContained` is not exported. Worse, 7a-i's object-identity criterion pins `:239` to `{exitCode: 0}` — **7a-i pinned ALLOW at exactly the site the table says must DENY**, making the mandated 7b trust-root fixture unsatisfiable by any change inside 7b's declared scope. That is an internally contradictory partition, not an omitted deliverable. **THEREFORE: `pre-read-size-advisory.js:234-239` is named as a 7b deliverable in its own right** — at 7b activation that catch returns the (1-b) deny result (no `additionalContext`) or routes through the same disposition handler the switch uses — **and the identity that survives ACROSS the 7b boundary is restated as the ONE return 7b does not touch (`:220`, unparseable hook input) — 7a-i's own incumbent→7a-i criterion, all SIX returns object-identical, is UNCHANGED, because 7a-i keeps every kind on `{exitCode: 0}` (scope of this narrowing CORRECTED 2026-09-13, native b5bddf50 MEDIUM; UNREVIEWED: it had conflated incumbent→7a-i identity with the 7a-i→7b change) — **NARROWED FROM "FIVE" 2026-09-13 (native 79ad7ef4 finding [4] MEDIUM; UNREVIEWED)**, because the other four named there are changed by 7b under THIS SAME CELL's other rules and the claim could not hold beside its own fixtures: `:225` is the range half, which (1-a) rewrites to exempt "only a WELL-FORMED, IN-BUDGET range, sending every malformed or over-budget range to the disposition table"; `:243` is entry 6 `uncontained`, which the cell says acquires its DENY at 7b; `:248` is named in the cell as fail-OPEN "on exactly the kinds (4b) says DENY"; and `:251` is the subject of the B1 correction's paired 7b fixture, which requires the same contained 201-line file to emit the `:251-258` advisory BEFORE activation and `permissionDecision=deny` AFTER. Identity over those four and that fixture cannot both hold. `:239` remains pinned to `{exitCode: 0}` FOR THE DURATION OF 7a-i ONLY and exempted from identity across the 7b boundary; it is now one of FIVE returns so exempted (`:225`, `:239`, `:243`, `:248`, `:251`), and the single return whose identity also holds across 7b is `:220`; incumbent→7a-i identity stays over all SIX.** The mandatory positive control is unchanged: an ordinary contained over-threshold read still emits the `:251` advisory through 7a-i. **NOTEBOOKS GET AN EXPLICIT DISPOSITION BEFORE LINE COUNTING (same correction; finding [6], codex `technical-accuracy`, conf 0.75).** A `.ipynb` is NUL-free JSON that routinely exceeds 200 serialized lines, but the Read tool returns cells and their outputs rather than a line-indexed slice, so `offset`/`limit` do not mean over a notebook what rules (1) and (4a) assume. **A notebook Read is treated as another NON-LINE-ORIENTED format and is EXEMPT, exactly as binary is** — the smallest option consistent with the existing partition, and it avoids denying ordinary notebook reads on a line count that does not describe them. Fixtures: an oversized `.ipynb` ⇒ ALLOW as non-line-oriented, plus a `limit`-only control on the same file ⇒ ALLOW. **THE MECHANISM IS NAMED, BECAUSE "EXACTLY AS BINARY IS" DOES NOT REACH IT — ADDED 2026-09-13 (native 79ad7ef4 finding [12] MEDIUM; UNREVIEWED).** Binary's exemption is delivered by the `null` branch, attributable to binary content at `:99` (NUL byte in window) or to a non-regular file at `:154`, and decided on the tool's DECLARED FORMAT HANDLING. **A notebook is NUL-free JSON**, so `scanWindow` finds no NUL, `:99` never fires, `countLines` returns a NUMBER, the file arrives at the switch as kind `count`, and the threshold decides it — which the cell's own PDF reasoning states is "denied by rule (1) as an ordinary oversized text read, whose only exemption is a line `limit`". The canonical table has no notebook member either, so **as written, no surface delivered the ALLOW these fixtures assert**. The exemption is therefore a **SCOPE FILTER, not a probe kind**: `run()` screens the `.ipynb` suffix and returns `{exitCode: 0}` **BEFORE the line-count probe runs**, in the same class as the PATH-SAFETY half of `:224-226` but placed AFTER the trust-root and containment screens (`:234-243`) and before `countLines` at `:246` (placement CORRECTED 2026-09-13, native b5bddf50 MEDIUM, verified at `727d4652`; UNREVIEWED: at `:224-226` no containment screen is yet ahead of it, so an uncontained notebook would have been ALLOWED; fixture added: an uncontained `.ipynb` path takes entry 6 `uncontained`'s disposition, DENY at 7b), which (b) below already establishes "is NOT a probe outcome at all" but "a scope filter over paths this advisory cannot resolve". **No ELEVENTH kind is added, the canonical table is untouched, and its count is unchanged.** Three bounds, stated so the filter is not widened: it keys on the SUFFIX, because that is what the Read tool's own cell-rendering dispatch keys on, and a mis-suffixed notebook is deliberately OUT OF SCOPE — the alternative is content-sniffing JSON to guess a format, which is a new mechanism this bound forbids; it exempts the notebook from the LINE-COUNT rules only, leaving every containment and path-safety screen ahead of it in force; **and it applies ONLY when the hook payload's `tool_name` is `Read`** (ADDED 2026-09-15 post-14, native round-14 issues[4] MEDIUM; Photon correction decision; UNREVIEWED). The exemption's reason — Read renders cells — does not describe the raw JSON a bare `cat` prints, and the agy-read gate row's `cat` normalization sends that operand into this same `run()` path, so a suffix-only filter would exempt `cat big.ipynb`. `tool_name` is read from the PreToolUse stdin payload and carried through that normalization, never inferred from the command text or the operand; the incumbent reads no `tool_name` at `34887cb7`, so carrying it is part of this 7b deliverable. Fixture pair at 7b: `Read` of a contained oversized `.ipynb` ⇒ ALLOW; a bare `cat` of the same file ⇒ the ordinary over-threshold `cat` disposition, DENY. *(Weighted below codex's 0.97 in the source finding because exact Read-tool notebook semantics are an external contract this document cannot pin from the repository; the partition gap itself was never in doubt.)* RECONCILED 2026-09-10 (round-16 finding [12]; Photon technical decision, UNREVIEWED): this APPLIES the round-15 [1] separation — the ProbeResult kinds and the hook output are different surfaces, and only the kinds are 7a-i's to change — and does NOT reopen it. The earlier claim that the contract "extends to `run()`" so each of its returns MUST carry a kind is WITHDRAWN, and no kind is invented for "an allow": (b) already settles that `:224-226` is not a probe outcome at all.** **The round-11 correction that ADDED `run()`'s returns to the site→kind mapping is RETIRED with it — the mapping stays bounded to probe sites, which is where the canonical table has members.** The "all sixteen" table enumerated only sites inside `resolveContained` / `scanWindow` / `countLines` (`:75`…`:198`) and named none of `run()`'s, so the sentence above required a kind the mapping could not supply; and two of the five had no representable kind, because the nine are all probe outcomes ABOUT A FILE. Both are closed here rather than by narrowing the promise: **(a)** `:234-239`, the trust-ROOT establishment failure (`fs.realpathSync(cwd)` / `fs.statSync`) returning a bare `{ exitCode: 0 }`, takes a TENTH kind **`trust_root_unavailable`** — **whose disposition is the canonical table's DENY, not the ⇒ ALLOW written here in round 11; WITHDRAWN 2026-09-09 (`批准840`, round-14 finding [1]; UNREVIEWED)**, with the reasoning and the refutation of the "would block every read" rationale stated once in the resolution note above and cited, never restated. What survives from round 11 is only the observation that prompted the kind: `:234-239` returns a bare `{ exitCode: 0 }` on a trust-ROOT establishment failure (`fs.realpathSync(cwd)` / `fs.statSync`), and no file-outcome kind could represent it; **(b)** `:224-226` is NOT a probe outcome at all: under (1-a) its two halves are DISPOSITIONS, and each carries the kind of the branch it routes to — the path-safety half an allow, the range half the disposition table. **The contract therefore closes at the membership the canonical table carries**, and the count is derived by reading that table and is written nowhere — **the bare figure that stood here is struck 2026-09-09 (`批准840`, round-14 finding [11]; UNREVIEWED)**, since a count written beside a list is exactly how this row came to declare one number and enumerate another. **`not_found` is also SPLIT OUT of `probe_unavailable`:** a file that is not there is not a probe that failed, and the two must not share a disposition. **THE SITE IS CORRECTED 2026-09-09 (round 12) — the earlier justification named a mechanism that does not exist.** It claimed `resolveContained`'s catch at `:74-76` swallows ENOENT together with a genuine containment error. It does not: `resolveContained` (`scripts/hooks/pre-read-size-advisory.js:69-77`) calls `assertWithinTrustedRoot`, which resolves a nonexistent descendant through `realpathNearestExisting` (`scripts/lib/path-safety.js:39`, used at `:67` and `:94`) and RETURNS the path rather than throwing. **ENOENT for a nonexistent in-root file therefore surfaces later, in `countLines`' `fs.openSync` at `:152`, and is caught by the OUTER catch at `:197-198`** — which the sixteen-site table maps unconditionally to `probe_unavailable`. So under the resolved DENY default the `not_found` kind was unreachable at the site named, and the case it exists to ALLOW was being denied instead. **The split is therefore made at the outer catch: an ENOENT from `openSync` yields `not_found`; every other error there stays `probe_unavailable`.** Dispositions unchanged: `not_found` ⇒ ALLOW (there is nothing to advise about), `uncontained` ⇒ DENY. **Every disposition in this paragraph, the `not_found` split included, is RESOLVED 2026-09-09 as a worker default under *Still needs Chris* #13 — recorded, vetoable by row ID, and authorizing no implementation** — the same live-behaviour policy choice that governs `probe_unavailable`.

---

## Moved from the parent 2026-09-24: § *Item 7*

> **MOVED VERBATIM (2026-09-24; Chris-approved shrink; plan-only, UNREVIEWED).** The text below was the parent's § *Item 7*, at parent spec `8bfd530e…`; the parent keeps a one-line pointer to this file at its place. It binds as part of this file and is reviewed with this file's spec hash; a verdict on the parent does not cover it. Inside it, "this file", "this document", "above", "below" and line anchors `:NNN` into the parent refer to the parent as it stood before this move.

**agy-read gate** in the plugin (`hooks/hooks.json`): PreToolUse on `Read` and bare Bash `cat <file>` (no pipe/redirect), denying reads over the 200-line threshold **unless the call carries a bounded-read exemption as defined in rule (1) of the settling check** — **and the Bash half needs its own deliverable, ADDED 2026-09-09 (round 11), because every rule below is written over `Read`'s `tool_input.file_path` / `offset` / `limit` while Bash supplies `tool_input.command`, and the incumbent hook reads only the former (`pre-read-size-advisory.js:223`). The promise is KEPT, not narrowed: extending the registration alone would leave every `cat` call on the `:224-226` allow branch, since `file_path` is undefined there. The deliverable therefore specifies (a) the SUPPORTED `cat` GRAMMAR — a single-command `cat` with exactly one non-option operand, no pipe, no redirect, no substitution, everything else out of scope and allowed unchanged; (b) how that operand is NORMALIZED into the probe (joined to the payload `cwd` and lexically normalized, exactly as the path-safety arm does, so the same containment result is reached); and (c) that rule (1)'s exemption is UNREACHABLE for a `cat` call, which carries no `offset`/`limit` — so a `cat` over the threshold reaches the disposition table rather than an undefined exemption** — deliberately *not* "without offset/limit", which is the incumbent's presence check and would not discriminate. Message leads with the trust rule (dispatch only content whose authors you trust; it leaves the machine), then the exact `dispatch.sh --cli agy-read` line. Replaces the advisory `pre-read-size-advisory.js` (registered at `hooks/hooks.json:301` via `run-with-flags.js`; keep its bounded probe — its text still says "route to pi"). Documented as a **cost gate, NOT a security boundary**; Bash coverage deliberately narrow. **Failure behaviour is split into two distinct cases by rules (2) and (3) — do not conflate "infra failure", "hook-launch failure" and "helper-down", which earlier drafts used interchangeably.** Read doctrine leaves busdriver's project `.claude/CLAUDE.md` for the SessionStart brief. pi-read leaves the routing doctrine; lane code + `.pi_read.model` stay for the pi-replacement program. Amends ADR 0040, ADR 0034. **Direction preserved from the 2026-08-27 approval — #840's downgrade to an opt-in cohort is NOT adopted** (see disposition). Note the gate is a Claude Code PreToolUse hook, so OMP and Cursor-agent were never subject to it; the multi-harness objection does not reach it.

**Class:** hard

**Status 2026-09-08:** **SPLIT**, and 7a's readiness is stated honestly rather than overstated. **7a-i** (the discriminated-result refactor, UNIT tests over the returned kinds asserting NO change to observed allow/deny, and the measurement harness — **plus the `run()` OUTPUT-COMPATIBILITY criterion ADDED 2026-09-10 (round-15 finding [1]; Photon technical decision, UNREVIEWED)**, because "no change to allow/deny" cannot catch the regression this refactor actually risks: **the ProbeResult KINDS and the HOOK OUTPUT are different surfaces, and only the kinds are 7a-i's to change.** Verified at HEAD: `scanWindow` returns bare `null` at `:99` and `:108`, `countLines` collapses both at `:178` (`if (window === null) return null`), and `run()` tests `if (lines === null || lines <= THRESHOLD)` at `:247-248`. A tagged object is neither `=== null` nor `<= 200`, so unless `run()` is re-plumbed in the same change, **every path that returns bare `null` today — `binary`, `non_regular`, `probe_unavailable`, `uncontained`, `not_found`, `size_indeterminate` — falls through to the `:251-258` `additionalContext` "route to pi" advisory.** Allow/deny is `exitCode 0` on both branches, so that silent-allow-to-advisory regression is invisible to every fixture in either half — which is why it is an ACCEPTANCE gap, not an implementer's-care gap. **THE CRITERION: `run()`'s SIX returns — `:220`, `:225`, `:239`, `:243`, `:248`, `:251` — must be object-identical before and after the refactor** (**CORRECTED 2026-09-10, round-16 finding [15]; UNREVIEWED** — the count was five and the anchors offered in review, `:238`/`:257`, do not exist; five of the six are bare `{ exitCode: 0 }` returns and only `:251` carries `additionalContext`, so identity is asserted across the whole set). **ONE POSITIVE CONTROL IS MANDATORY, NOT OPTIONAL — ADDED 2026-09-10 (round-18 finding [2]; Photon technical decision, UNREVIEWED): an ordinary CONTAINED file OVER the threshold must still emit the `:251` advisory carrying its `additionalContext`.** Object-identity over `{exitCode: 0}` alone cannot catch what this criterion exists to prevent — five of the six returns ARE `{exitCode: 0}`, so a refactor that silently reroutes the ordinary contained case into an early return passes an identity-only corpus while the advisory disappears entirely over a fixed corpus — small file, oversized text, binary, non-regular, missing path, unparsable payload, unresolvable trust root — asserting the exact returned object **including the presence or absence of `additionalContext`**, not merely the absence of a `permissionDecision`. **`run()` MUST unwrap the tagged result at `:247`** so the silent-allow branch keeps its current membership. This changes no observable behaviour and so stays inside 7a-i under the round-12 partition rule; it exists to guarantee that) **TODO** after item 1. **RE-PARTITIONED 2026-09-09 (round-13 finding [2]; UNREVIEWED)** — this cell previously placed the exemption-semantics fixtures per rule (1) in 7a-i, and, after #13 resolved, the indeterminate-probe fixtures per rule (4) "alongside the rest of 7a-i". Both assert allow/deny, so under the partition rule stated once above they are **7b's**, behind `baseline captured`, together with (1-a)'s `:224-226` guard split and (1-b)'s transport. As written, 7a-i was simultaneously advisory-only, unable to emit a `permissionDecision` (its transport is in 7b), and required to pass fixtures asserting actual denial — unsatisfiable, or else enforcement before the baseline, contaminating the very measurement this plan protects. The **indeterminate-probe fixtures per rule (4) were NEEDS CHRIS #13**, split out 2026-09-09 (round 9, finding [0]) because rule (4b)'s allow/deny default was unsettled — **#13 RESOLVED 2026-09-09 (worker default: DENY); that default is now a 7b input.** **7a-ii** (the **helper-down failure fixtures**) was **NEEDS CHRIS #10** — rule (3) says no engineer can write that test until the policy is chosen — **and #10 RESOLVED 2026-09-09 (worker default: (A)), which DROPS the helper-unavailable test with option (B), so 7a-ii ceases to exist as a separately gated half rather than becoming schedulable.** **7b** (registering the PreToolUse hook in `hooks/hooks.json`, **and the read-doctrine rewrite in `skills/orchestrator/session-brief.md`** — the brief is *delivered context*, so rewriting it is a mutating change under the graph's own rule and does not belong in 7a, **and flipping the DENY arms of the `result.kind` switch that `run()` performs at `:241` and `:247`** — that switch is the CANONICAL TABLE's one application site, it lands in 7a-i with every arm returning `{exitCode: 0}`, and 7b changes only the DENY arms, so the table is no longer a table without an application site: DELIVERABLE ADDED 2026-09-12, native findings [23] HIGH and [00] MEDIUM; UNREVIEWED, **and the notebook `.ipynb` suffix screen and the Bash `cat` grammar**, both of which change an observable result and so are 7b deliverables, while 7a-i keeps today's `:251` advisory for an oversized contained notebook — named here 2026-09-13, native 2a9a6900 finding [10]; UNREVIEWED) **BLOCKED on `baseline captured` AND *Still needs Chris* #10 — #10 RESOLVED 2026-09-09, so that conjunct is SPENT and 7b's live gates are `baseline captured` AND item 3's paired measurement AND 7a-i landed (the graph's `7a-i ──► 7b` edge; added 2026-09-24; plan-only, UNREVIEWED); 7b is still not startable**. The old "independent after 1" reading is withdrawn

**Evidence / settling check:** **Four numbered rules. An engineer must be able to write the gate, its fixtures and its failure tests from these alone; earlier drafts of this cell stated three mutually incompatible contracts in prose.**

**(1) Exemption = a bounded read.** A positive integer `limit` no greater than the threshold. `offset` alone is **NOT** an exemption. **ORDERING, ADDED 2026-09-10 (round-16 finding [1]; Photon technical decision, UNREVIEWED) — (1)/(1-a) and (4a) could not both hold for a PDF and the cell never ordered them: FORMAT DETERMINATION RUNS BEFORE THIS EXEMPTION IS APPLIED.** The `offset`/`limit` exemption is scoped to **line-oriented text reads only**. For a PDF, `limit` is **NON-EXEMPTING** and the only bounded read is a valid `pages` bound per (4a); a PDF read carrying `limit` alone ⇒ **DENY**. The ordering is load-bearing rather than editorial: the incumbent range guard at `scripts/hooks/pre-read-size-advisory.js:224-226` is taken **before** `resolveContained` / `countLines` and before any format determination, so `Read(large.pdf, limit=50)` is exempted today without ever establishing a `pages` bound — on the exact reasoning (4a) already gives for why a line limit cannot be the PDF recovery. **Three registered-hook fixtures pin the order** (7b's, like the rest of this rule): oversized **text** with `limit=50` ⇒ ALLOW; oversized **PDF** with `limit=50` and no `pages` ⇒ DENY; oversized **PDF** with a valid `pages` bound ⇒ ALLOW. Fixtures, **each with its EXPECTED RESULT stated — CORRECTED 2026-09-09 (round-13 finding [2]; UNREVIEWED), because a blanket "assert denial" swept in the one control that must be ALLOWED**: offset-only ⇒ DENY, `limit=201` ⇒ DENY, `limit=0` ⇒ DENY, `limit=-1` ⇒ DENY, `limit='abc'` ⇒ DENY, **`limit=50` ⇒ ALLOW** (a positive integer within the threshold is the exemption this rule defines; asserting denial for it would contradict the rule's own first sentence). Each DENY fixture asserts **actual tool denial** (a `permissionDecision`), not the presence of a message; the ALLOW fixture asserts no denial is emitted. **These are 7b's fixtures, not 7a-i's** — they observe allow/deny, which the partition rule above assigns to 7b behind `baseline captured`. **One further 7b fixture, ADDED 2026-09-09 (round-13 finding [3]; UNREVIEWED): a path that does not exist INSIDE the trusted root ⇒ ALLOW**, proving `not_found` is reachable at `:198` and is not swallowed by `probe_unavailable`'s DENY. Pair it with its negative control — a **vanished trust root** (the `fs.statSync(cwdReal)` re-stat at `:164` raising ENOENT) ⇒ **DENY**, since that is `probe_unavailable` and a naive `err.code === 'ENOENT'` test in the outer catch would wrongly ALLOW it. Both are allow/deny observations, so both are 7b's; **7a-i stays outcome-neutral** and covers this ground only with unit tests over the returned kinds. **TWO FURTHER 7b FIXTURES PIN THE MISSING-TARGET / UNAVAILABLE-TRUST-ROOT DISCRIMINATION — ADDED 2026-09-09 (`批准840`, round-14 findings [1] and [8]; UNREVIEWED),** because the canonical table now disposes those two conditions OPPOSITELY and discriminates them by SITE rather than by a thrown message, so nothing in the fixture set would otherwise fail if an implementer collapsed them: **(i) a missing or non-string `file_path` ⇒ ALLOW**, taken at `:224-226`'s path-safety half before any probe kind exists — this advisory has nothing to say about a path it cannot resolve, and the fixture asserts no `permissionDecision` is emitted; **(ii) an UNRESOLVABLE TRUST ROOT — a payload `cwd` that `fs.realpathSync` cannot resolve, so `:234-239` fails ⇒ DENY** as `trust_root_unavailable`, which is the fixture that fails against the incumbent's bare `{ exitCode: 0 }` and against the withdrawn ⇒ ALLOW disposition alike. Run (ii) alongside the vanished-trust-root control above and keep the two distinct: that one enters `countLines` and surfaces at `:164`/`:198` as `probe_unavailable`, while this one never reaches `countLines` at all. All four DENY the same way under the table, which is the point — the discrimination they pin is of KIND and SITE, and (i) is the only ALLOW among them.

**DENY IS RETAINED — approved 2026-09-09, and rules (1) and (4b) are reconciled here rather than one of them being deleted (round 10, findings [3] and [11]).** The two rules were mutually unsatisfiable as written: rule (1) demands an actual denial for every one of its fixture inputs, while (4b) preserved `:224-226` as an allow — and that guard returns `{ exitCode: 0 }` whenever `offset` **or** `limit` is defined **at all**, so it swallowed the entire fixture set. Verified at the checkpoint. Two repairs, both owed by this row:
**(1-a) The `:224-226` guard SPLITS into two guards with different dispositions.** It currently conflates a *scope* filter with a *path-safety* filter in one condition. The path-safety half — `typeof file_path !== 'string'`, empty, or `!isSafePath(...)` — **stays an allow**, unchanged: this advisory has nothing to say about a path it cannot resolve. The range half is **narrowed to a WELL-FORMED, IN-BUDGET range only**: a positive integer `limit` no greater than the threshold exempts the read, and every other range shape — `offset` with no `limit`, `limit` of `0`, a negative `limit`, a non-integer `limit`, a `limit` above the threshold — is **no longer exempt** and reaches the disposition table, where rule (1) denies it. That is exactly the set rule (1) enumerates, so the fixtures become satisfiable without weakening any allow that (4b) intended to keep.
**(1-b) The denial needs a TRANSPORT, and the incumbent registration has none.** Verified at the checkpoint: `hooks/hooks.json:301` routes this advisory through `scripts/hooks/run-with-flags.js`, and that launcher contains **zero** occurrences of `permissionDecision` or `hookSpecificOutput`. **THE CONCLUSION DRAWN FROM THAT WAS FALSE AND IS WITHDRAWN 2026-09-09 (round-13 finding [4]; UNREVIEWED).** It read: "so a denial written by the advisory cannot reach the harness no matter how the advisory is changed." Measured in `scripts/hooks/run-with-flags.js` (byte-identical at HEAD and the checkpoint): when the delegate returns an object carrying a `stdout` key, `:86-87` forwards `String(output.stdout)` **verbatim**, and `:258` writes it to real stdout. The launcher never needs to KNOW the schema — it is a pass-through. So the advisory can already emit a harness-level denial by returning `{ stdout: JSON.stringify({ hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason: … } }), exitCode: 0 }`, and **no change to the content-locked launcher is required for the transport**. That is the contract (1-b) now specifies. The `run-with-flags.js` lock regen this row previously mandated is therefore **not needed for (1-b)** and is retained only if 7b changes that file for some other reason. Naming the emission point is therefore part of this row: **the ADVISORY renders the DENY itself**, returning a discriminated result whose `stdout` carries the harness-level `hookSpecificOutput.permissionDecision`, which the launcher forwards verbatim. **CORRECTED 2026-09-09 (`全部批准`, round-14 finding [10]; UNREVIEWED)** — this sentence previously read "the launcher must render a DENY", contradicting the pass-through finding two sentences above it and re-imposing the launcher change that finding withdrew. **ONE CONSTRAINT ON THAT RESULT, ADDED 2026-09-09 (`全部批准`, round-14 finding [16]; UNREVIEWED), and it is load-bearing: a deny result MUST NOT also set `additionalContext`.** Measured at HEAD: `resolveHookResult` (`run-with-flags.js:74`) tests `additionalContext` FIRST at `:83-84` and returns on it, so an object carrying both keys has its `stdout` — the deny payload — silently DISCARDED and rendered as advisory context instead, which is the incumbent's fail-open reached through the very result shape this rule introduces. The two keys are mutually exclusive on this path and the fixtures must pin that. **`scripts/hooks/run-with-flags.js` is inside the gate-script content lock** (`scripts/hooks/**`) — see the ONE global rule in *Execution order and acceptance* — but since (1-b) requires no edit to it, its `--update` regen is owed **only if 7b modifies it for some other reason**, and this row's lock enumeration names `pre-read-size-advisory.js` alone. **`hooks/hooks.json` is NOT content-locked, but its registration shape is pinned by `tests/test-node-hook-containment.sh`**, so a transport change that alters the registration must be reconciled with that suite in the same change. **A denial rule whose transport cannot carry it is not a tightening, it is a no-op** — which is why this is stated here rather than left to the implementer.

**Why the basic denial regression is kept — the premise for dropping it was false.** An earlier draft argued that `pre-read-size-advisory.js` "already returns `exitCode 0` when `offset` or `limit` is present", so "201 lines without limit → denied" could not discriminate. Re-measured at the checkpoint: line **`:224`** is the presence check (`:223` is the destructuring statement — the old anchor was off by one), and more consequentially the oversized-read branch at **`:251-258`** returns `{ exitCode: 0, additionalContext: [...] }`. **The incumbent never denies anything.** So the basic denial test *does* discriminate, and it stays. The fixtures above are exemption-**semantics** tests, not the thing that distinguishes the gate from the advisory.

**(2) Hook-launch failure stays fail-open / advisory.** `PATH=/nonexistent` node → Read still allowed. This test covers a **launch** failure and nothing else; it proves nothing about rule (3).

**(3) Helper-down policy — ONE choice, and it is NOT settled here.** Either (A) no-limit reads stay denied and the agent paginates, or (B) a bounded, cached helper failure downgrades the gate to advisory — which requires naming the cache path, the TTL, and the rule that dispatch is never on the gate's critical path. **This was an open implementation decision carried as *Still needs Chris* #10; RESOLVED 2026-09-09 (worker default) = (A). (B) is rejected as fail-open on a dependency failure and as putting a dispatch on the gate's critical path, and rule (3)'s wholesale-read language and the helper-unavailable test are DROPPED with it. Stated once in that row; this rule cites it.** It is distinct from the item-7 *direction*, which is settled (the cost-gate is kept; #840's opt-in cohort is REJECTED). An earlier closing sentence here — "ordinary reads must remain possible when the helper is unavailable" — asserted (B) in passing while the body asserted (A); that contradiction is removed rather than silently resolved.

**(4) Indeterminate probe — the case the first three rules omitted. SCOPE FIRST, then the default.** The retained bounded probe returns `null` — and therefore currently **allows** the read — for a large family of inconclusive outcomes; verified at the checkpoint in `pre-read-size-advisory.js`, `return null` appears at `:75`, `:99` (binary: NUL byte in the scanned window), `:108`, `:126`, `:130`, `:138`, `:140`, `:154` (not a regular file), `:156` (size > `MAX_SCAN_BYTES`), `:158`, `:165` (**trust-root mutation mid-check**), `:170` (not a descendant), `:178`, `:186`, `:195`. "Cannot determine the size" is therefore not a rare corner. **The two halves must be separated, because an earlier draft chose deny for all of them and thereby wrote a NEW lockout into the acceptance criteria.**

**(4a) Out of scope — ALLOW, evaluated BEFORE (4b).** If the `null` is attributable to **binary content** (`:99`) or to a **non-regular file** (`:154`), the read is not a LINE-count question at all and this cost gate has nothing to measure. Allow it. This is not a loophole: rule (1)'s only exemption is a line `limit` no greater than the threshold, and a line limit is meaningless for these formats — the Claude Code `Read` tool is the documented way to open **images** (no line concept whatsoever) and **PDFs** (paginated by a `pages` parameter, not `offset`/`limit`). **The exemption is decided on the tool's DECLARED FORMAT HANDLING, BEFORE any line-count evaluation — corrected 2026-09-08 (round 8, M13), because keying it on `:99` is false for PDFs and the failure lands OUTSIDE rule (4) entirely.** PNG always trips `:99` (the IHDR length field is `00 00 00 0D` immediately after the signature) and JPEG effectively always does (segment length high bytes), but **PDF need not**: the format permits entirely uncompressed, human-readable object and content streams, and a **5471-byte NUL-free PDF was measured as 237 lines** by the checkpoint's own `countLines`. Such a file returns a **number above the threshold**, so it never reaches rule (4) at all — it is denied by **rule (1)** as an ordinary oversized text read, whose only exemption is a line `limit` that the `Read` tool does not apply to PDFs (they are paginated by `pages`). That is the same no-recovery lockout this row states it removed, reached by a path the row does not guard. **So: **corrected 2026-09-09 (round 9, finding [16]) — the two disjuncts made `pages` alone a caller-controlled bypass: `Read(file_path='app.py', pages='1')` exempted an arbitrary oversized TEXT file, and since the `Read` tool applies `pages` only to PDFs the read still returned the whole file, buying exactly the unbounded read rule (1) exists to prevent. The test is now a CONJUNCTION on format:** exempt **iff** magic bytes or extension identify a binary or paginated `Read` format, **AND** — for PDF — a `pages` bound is present. **`pages` alone is never an exemption.** So qualified, the read is EXEMPT regardless of what the probe returns, and the test runs before any line count.** Scope it to **exemption only** — this rule may never tighten a deny, so a spoofed header buys at most the allow the incumbent already gives. **Fixture, CORRECTED 2026-09-09 (`全部批准`, round-14 finding [4]; UNREVIEWED): a NUL-free PDF over 200 physical lines carrying NO `pages` bound, driven through the REGISTERED HOOK rather than against `countLines` in isolation, asserted DENY — with `pages` named in the denial as the recovery.** It previously asserted **ALLOW** on precisely that input. That text predates the conjunction stated just above and contradicts it head-on: `pages` alone is never an exemption, and for PDF a `pages` bound is *required* for one, so a pages-less PDF is not exempt and a fixture greening on it would pin the bypass the conjunction exists to close. The companion allow case — the same PDF opened **with** `pages` — is already carried in the allow list below and is not duplicated here. **The blanket claim that "every PNG, JPG and PDF is exempt before `:99` is consulted" is STRUCK in the same pass**, for the same reason: it holds for PNG and JPG, which have no line concept and no recovery parameter at all, and it fails for PDF, which has exactly one. So the no-recovery lockout argument covers images only — a denial on an unbounded PDF read is actionable, because it names a parameter the `Read` tool really does apply to that format. Note the direction of the change: the incumbent **never denies anything** (`:224` short-circuits to `exitCode 0` whenever `offset` or `limit` is present, and the oversized branch at `:251-258` returns `{ exitCode: 0, additionalContext: [...] }`), so this would have been a new prohibition, not a tightening. Fixtures — **allow**: a PNG, a PDF opened with `pages`, a JPG, and a non-regular file (fifo/device).

**PREREQUISITE, added 2026-09-08 (round 8): rules (4a) and (4b) are NOT implementable against the probe as it stands, and the refactor that makes them implementable is a NAMED DELIVERABLE of this row. **ASSIGNED 2026-09-09 to 7a-i (round 10, finding [9]) — it previously belonged to neither half, so it had no gate, no acceptance owner and no settling check: the exact defect items 2i and 2p were created to repair elsewhere in this document, and worse here because the target file is lock-pinned.** 7a-i is the correct half because the refactor is *diagnostic plumbing* — it changes no disposition on its own, it is what rules (4a)/(4b) are stated over, and 7a-i is already the half gated on item 1 alone rather than on `baseline captured`. **7a-i IS PURELY ADVISORY — DECIDED BY CHRIS 2026-09-09 (round 12), and this is a scope boundary, not a preference.** Round 12 found 7a-i carrying work that ships live enforcement ahead of `baseline captured` while being described as outcome-neutral, which is the same contradiction the 2i NODE carried. **The rule, stated once here and governing every later reference: anything that changes what a gate ALLOWS OR DENIES belongs to 7b, behind `baseline captured`. 7a-i may only add discrimination that no disposition reads yet.** Concretely: the discriminated-result refactor and its kind mapping stay in 7a-i because nothing consumes the kinds until (4a)/(4b) are registered; **the (1-b) denial transport moves to 7b**, because a denial that can reach the harness is enforcement by definition. Where the refactor's acceptance names `scripts/hooks/run-with-flags.js` alongside `scripts/hooks/pre-read-size-advisory.js` for the lock regen, that half of the acceptance travels with 7b too — a lock regen over the launcher is only needed once the transport lands. It therefore starts when 7a-i starts, and 7b's registration still waits on the gates the dependency graph carries for it — cited, not restated here, since Chris #10 is SPENT. **OWNERSHIP CONTRADICTION REMOVED 2026-09-09 (round-13 finding [2]; UNREVIEWED):** this cell said the `run-with-flags.js` lock regen "travels with 7b too" and then, one sentence later, "Its acceptance is 7a-i's, plus the lock regen" — putting the same deliverable on both halves. **The lock regen is 7b's, once, per the rule above.** 7a-i's own acceptance regenerates the lock only for the files IT changes: `scripts/hooks/pre-read-size-advisory.js` is inside `scripts/hooks/**`, so `./scripts/gate-integrity.sh --update` lands in 7a-i's reviewed diff and `--check` exits 0 at that HEAD; `scripts/hooks/run-with-flags.js` is regenerated in **7b's** diff, on the same rule, **only if 7b actually modifies it** — and per (1-b) below, as re-measured 2026-09-09 (round-13 finding [4]), **the transport does NOT require modifying it**: the launcher already forwards a delegate's `stdout` verbatim. So the lock regen for that file is conditional, not scheduled.** Verified at the checkpoint: every one of the return sites this row reasons over returns a **bare `null`** — `:75, :99, :108, :126, :130, :138, :140, :154, :156, :158, :165, :170, :178, :186, :195`, **plus `:198`**, the outer catch in `countLines` that an earlier version of this row did not list (sixteen sites, not fifteen). `countLines` is typed `number`-or-`null` and the failure *reason* is discarded at each site, so a caller receiving `null` cannot tell binary content (`:99`, allow) from an oversized file (`:156`, deny) — the two rules cannot both be honoured. **Deliverable: change the probe's return contract to a discriminated result** — a tagged union whose members are enumerated ONCE, below, and whose count is DERIVED from that enumeration and never written beside it — **the "exactly nine" figure that stood here is struck 2026-09-09 (round-13 finding [8]; UNREVIEWED): this cell declared nine, forbade any restatement, and then added a tenth (`trust_root_unavailable`) later in the same cell, so the authoritative surface contradicted itself and a reader taking the first count implemented a union missing a kind. The union's membership, its count and every disposition are now carried by the CANONICAL KIND → DISPOSITION TABLE below, which is the sole authority for all three — REPOINTED 2026-09-09 (`批准840`, round-14 finding [11]; UNREVIEWED), because the surface this sentence used to name as authoritative was itself the one that declared TEN and listed NINE.** The earlier correction narrative is kept as history (round 10, finding [12]), because the row said "seven" and then defined an eighth (`not_found`) further down, and supplied no kind at all for the incumbent's payload/JSON parse-failure allow at `:219-221`**.

**THE CANONICAL KIND → DISPOSITION TABLE — ADDED 2026-09-09 (`批准840`, round-14 findings [1] and [11]; UNREVIEWED). This list is the ONE authority for BOTH the union's membership AND every kind's disposition.** It replaces the nine-member enumeration that stood here, which declared TEN and listed NINE — omitting `trust_root_unavailable`, so a reader who implemented the list built a union missing the very kind the rest of the row reasons over. No other surface in this row or this document restates a member or a disposition; each cites this table, and the count is derived from it and never written beside it. **The SITE lists in the parentheticals below are a reading aid, not a second authority: the sixteen-site mapping remains authoritative for site→kind **across the PROBE sites it enumerates — BUT ONLY WHERE THE KIND IS A FUNCTION OF THE SITE ALONE. CARVE-OUT ADDED 2026-09-10 (round-18 finding [6]; Photon technical decision, UNREVIEWED): where a kind depends on RUNTIME IDENTITY rather than on position, the CANONICAL TABLE owns it and the mapping is a pointer to it.** The live instance is `:198`: entry 8 disposes an open-time ENOENT as `not_found` ⇒ ALLOW **only** when the captured trust root re-stats to the same `{dev,ino}`, and as `trust_root_unavailable` ⇒ DENY otherwise — while the mapping row and (4b)'s consequence state that same site unqualified. Without this carve-out the row's own supremacy rule elects the UNQUALIFIED FAIL-OPEN over entry 8, and the 7b fixture pair this row mandates (root removed between `:234-237` and `:152` ⇒ DENY; missing target under an unchanged root ⇒ ALLOW) fails against the spec as written. **Entry 8 stays the ONE definition: the mapping row and (4b) CITE it and do not restate it — no new kind, no changed disposition, Chris #13's `not_found` ⇒ ALLOW split preserved.** For site-only kinds the mapping still wins where the two disagree, and this table is the surface to repair. **SUPREMACY BOUNDED 2026-09-10 (round-15 finding [13]; UNREVIEWED):** the clause read as covering every site, but the mapping does not enumerate `run()`'s own exits (`:219-221`, `:224-226`, `:234-239`, `:241`, `:242-243`, `:246-248`, `:251-258`), so declaring it authoritative there made it authoritative over sites it never lists. **For `run()`'s exits this canonical table is the authority**; the count stays derived from the list and is never written beside it.** The ten kinds:
  **1. `count` (lines)** — the probe succeeded ⇒ **the threshold decides** (not a failure kind): at or under the threshold ⇒ **ALLOW**; OVER it with no in-budget limit ⇒ **DENY**, emitted through the (1-b) denial transport, and that deny is item 7b's deliverable (stated in this table 2026-09-24; plan-only, UNREVIEWED)
  **2. `binary`** — `:99` ⇒ **ALLOW**, **except a recognized PDF**: format determination runs before any
  exemption or probe (rule (1)), so a PDF read carrying `limit` and no valid `pages` bound is DENIED
  there, NUL bytes or not, and never reaches this kind; fixture: a PDF containing a NUL byte, opened
  with `limit=50` and no `pages` ⇒ DENY (added 2026-09-26; plan-only, UNREVIEWED). Otherwise this
  states the LIVE behaviour and adds no second rule:
  at the checkpoint `scanWindow` returns `null` at `:99` on ANY NUL byte, whether or not a format is
  identified, `countLines` collapses it at `:178`, and `run()` allows a `null` count at `:247`, all
  before any line count exists. (4a)'s format conjunction governs only its pre-probe exemption and
  does not turn this kind into a DENY (restated 2026-09-24; plan-only, UNREVIEWED).
  **3. `non_regular`** — `:154` ⇒ **ALLOW**, under (4a)
  **4. `oversized`** — `:156` ⇒ **DENY**
  **5. `size_indeterminate`** — `:108`, `:186`, `:195` ⇒ **DENY**
  **6. `uncontained`** — `:75`, `:165`, `:170` ⇒ **DENY**
  **7. `probe_unavailable`** — `:126`, `:130`, `:138`, `:140`, `:158`, and `:198` on its non-ENOENT branch ⇒ **DENY**
  **8. `not_found`** — `:198` on an open-time ENOENT originating from the `fs.openSync` at `:152` **AND the captured trust root re-stats to the SAME `{dev,ino}` captured at `:234-237`** ⇒ **ALLOW**; if that root is **missing or its identity changed**, the same ENOENT is **`trust_root_unavailable` ⇒ DENY**. **FOLDED IN FROM DECISION [11] 2026-09-10 (round-17 finding [0]; Photon technical decision, UNREVIEWED) — the header stated it, this canonical table did not, and this table is the authority.** Origin alone does not establish that the root still exists: `realpathNearestExisting` returns a reconstructed path for a missing descendant, and the incumbent re-stat at `:164-165` is reached ONLY on the succeeded-open path, so a root removed between `:234-237` and `:152` would otherwise map to ALLOW against Chris #13. Discrimination stays **by SITE plus captured identity**, never by errno string. **7b fixture pair:** root removed between `:234-237` and `:152` ⇒ DENY (`trust_root_unavailable`); missing target under an unchanged root ⇒ ALLOW (`not_found`)
  **9. `unparsable_payload`** — `:219-221`, the hook could not read its own stdin payload ⇒ **ALLOW**, since nothing about a file has been established; deliberately NOT folded into `probe_unavailable`, which denies
  **10. `trust_root_unavailable`** — `:234-239`, **and additionally `:198` on an open-time ENOENT whose captured root is missing or identity-changed, per entry 8** ⇒ **DENY**. **The "ONLY origin site" claim is WITHDRAWN 2026-09-10 (round-17 finding [0]; UNREVIEWED)** — it is exactly what left entry 8's mapping unqualified. This ADDS an origin; it changes no disposition, invents no kind, and preserves Chris #13's `not_found` ⇒ ALLOW split. The reachability correction below is unaffected
plus the **two declared NON-probe dispositions** at `:224-226` under (1-a), which are branches of `run()` rather than probe outcomes about a file: the path-safety half (non-string, empty or unsafe `file_path`) ⇒ ALLOW, and the range half ⇒ routed to this table.

**`trust_root_unavailable` CARRIED TWO OPPOSITE DISPOSITIONS ACROSS FOUR SURFACES AND NOW CARRIES ONE — RESOLVED 2026-09-09 (`批准840`, round-14 finding [1]; UNREVIEWED).** The `:75` site→kind mapping said all three of its kinds DENY; (4b)(a) introduced the kind at `:234-239` with ⇒ ALLOW; (4b)'s deny enumeration named it nowhere; and *Still needs Chris* #13's failure ladder omitted the rung entirely. **The disposition is DENY**, on this row's own already-stated doctrine rather than on a new argument: a gate that IS RUNNING and cannot establish its trust root has reached no conclusion about containment, and (4b) states once that such a gate blocks — the identical rule that disposes `probe_unavailable`. (4b) itself already called `:234-239` "fail-OPEN on exactly the kinds (4b) says DENY", so DENY is what two of the four surfaces were carrying; **the ALLOW is the outlier and is WITHDRAWN.** Its stated rationale — that failing closed "would block every read in a workspace whose root cannot be resolved" — is close to the real cost, and that cost is ACCEPTED, not minimized (corrected 2026-09-24; plan-only, UNREVIEWED): `run()` screens the `offset`/`limit` exemption at `:224` and then resolves the trust root at `:234-239`, before `countLines` (`:246`) knows any size, so the DENY blocks EVERY `Read` in that workspace that carries no `offset` or `limit`, whatever the file's size, a 5-line file included. The recovery is the one rule (1) already names for every other DENY kind. **Like every disposition in (4b), this is a WORKER DEFAULT under *Still needs Chris* #13 — recorded, vetoable by row ID, and authorizing no implementation** — and it is part of the one live-behaviour TIGHTENING that row already flags, not a second one.

**`:75` MAPS TO EXACTLY ONE KIND, AND THE TWO CONDITIONS ARE DISTINGUISHED BY SITE — CORRECTED 2026-09-09 (`批准840`, round-14 finding [8]; UNREVIEWED).** The three-way split by thrown error written into `:75` in round 13 is **withdrawn**: two of its three branches are unreachable from the only caller, so it invented a discrimination the code cannot make and does not need. Verified at the checkpoint — `resolveContained` is called at exactly ONE site, `run()`'s `:241`, and is not exported (`module.exports` at `:261` carries `run`, `countLines`, `isSafePath`, `THRESHOLD`). By the time `:241` runs, `:224-226` has already returned ALLOW for a non-string or empty `file_path`, so `assertWithinTrustedRoot`'s *missing destination path* throw (`scripts/lib/path-safety.js:85-87`) cannot fire; and `cwd` is `input?.cwd || process.cwd()`, never falsy, so its *no trusted install root resolved* throw (`:88-90`) cannot fire either. **The only throw that reaches `:75` is `!isWithinRoot` (`:91-93`)** — either a genuine containment rejection, or a canonicalization failure inside `isWithinRoot`'s own single `try/catch` (`path-safety.js:65-72`), whose source comment already fixes the reading: *cannot prove containment → deny*. Both are **`uncontained`**: one kind, one disposition. **So a missing target and an unavailable trust root are discriminated BY SITE, not by parsing a thrown message** — a missing or non-string target is screened at `:224-226` before any probe kind exists at all, and an unresolvable trust root is `:234-239`. That is why `trust_root_unavailable` needs no representation at `:75`, and why the root-vanishes-mid-check race (root resolvable at `:236`, gone by `:71`) collapses correctly into `uncontained`: under this table both kinds DENY, so the race changes no outcome. **This also corrects (4b)'s reading of `:242-243`, at that site rather than here** — `resolveContained` returning `null` now means `uncontained` and nothing else, so `:242-243` is a uniform fail-OPEN on a single DENY kind, which is a simpler defect and still a defect. State rules (4a)/(4b) over **kinds**, not over line numbers that move. **`scripts/hooks/pre-read-size-advisory.js` is inside the gate-script content lock** (`.gate-integrity.lock` line 58) — see the ONE global rule in *Execution order and acceptance*; this refactor's diff must carry the `--update` regen. **The refactor must reach `scanWindow`, not only `countLines`:** `scanWindow` returns a bare `null` for **both** the NUL-byte case (`:99` → binary) and the chunk-budget-exhausted case (`:108` → size-indeterminate), and `countLines` sees only the collapsed `:178`, so the binary signal is destroyed one frame **above** `countLines` and no change confined to `countLines` can recover it.

**Site → kind, all sixteen (the two-column mapping the rules are stated over):** `:75` catch in `resolveContained` → **`uncontained`, one kind — the round-13 three-way SPLIT BY THE THROWN ERROR is WITHDRAWN 2026-09-09 (`批准840`, round-14 finding [8]; UNREVIEWED)**, because two of its three branches are unreachable from `run()`, the only caller; the reachability proof and the by-SITE discrimination that replaces it are stated once in the reachability correction above and cited here, never restated. The round-13 entry is kept as history: it was written to stop a flat site→kind mapping from making the kind depend on which stack frame a containment failure surfaced in, and that concern is answered better by there being only one reachable frame. Its disposition is the canonical table's · `:99` NUL byte in window → **binary** · `:108` chunk budget exhausted, window not fully scanned → **size_indeterminate** · `:126` `lsof` exited non-zero → **probe_unavailable** · `:130` `lsof` emitted no `n` field → **probe_unavailable** · `:138` platform neither darwin nor linux → **probe_unavailable** · `:140` catch around the `lsof` spawn → **probe_unavailable** · `:154` `fstat` says not a regular file → **non_regular** · `:156` size > `MAX_SCAN_BYTES` → **oversized** · `:158` `fdPath === null`, the downstream consequence of `:126`/`:130`/`:138`/`:140` → **probe_unavailable** · `:165` trust-root `dev`/`ino` changed mid-check → **uncontained** · `:170` fd path not a descendant of the trusted root → **uncontained** · `:178` `scanWindow` returned `null` → **COLLAPSED: binary (`:99`) OR size_indeterminate (`:108`)**, which is the defect the paragraph above requires fixing rather than a kind of its own · `:186` early EOF, window not fully scanned → **size_indeterminate** · `:195` file grew past the scanned window → **size_indeterminate** · `:198` outer catch in `countLines` → **TWO kinds, not one: every throw caught there OTHER than an open-time ENOENT from the `fs.openSync` at `:152` yields `probe_unavailable`; that ENOENT's kind is IDENTITY-DEPENDENT and is NOT stated here — the CANONICAL TABLE, entry 8, owns it (POINTER ADDED 2026-09-10, round-18 finding [6]; UNREVIEWED)** — the single mapping is (4b)'s, CITED here and NOT restated (CORRECTED 2026-09-09, round-13 finding [3]; UNREVIEWED: this entry read unconditionally `probe_unavailable`, which under the resolved DENY default made `not_found` unreachable at the only site that can raise it and DENIED every missing in-root file — the exact case `not_found` exists to ALLOW). **The discrimination needs an ORIGIN marker, and that is its own deliverable:** the catch at `:197` is written `catch {` with no binding, and ENOENT can also arise from the `fs.statSync(cwdReal)` root re-stat at `:164`, which is a trust-root failure and must stay `probe_unavailable` — so the refactor must bind the error AND distinguish an open-time ENOENT from any other, e.g. a narrow `try` around `:152` alone. A bare `err.code === 'ENOENT'` test in the outer catch is NOT sufficient and would mis-map a vanished root to ALLOW.

**(4b) Genuinely size- or containment-indeterminate — DENY (fail-closed).** Binding contract moved to `docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-7-rule-4b.md` (2026-09-23 split; UNREVIEWED — the review loop does not read it). It remains part of this plan's requirements. **The `probe_unavailable` DENY paragraph that follows is binding (4b) text, and this file is its one binding location (aligned with the parent 2026-09-24; plan-only, UNREVIEWED).**
**The `probe_unavailable` disposition is DENY, and the reasoning is recorded because it is not self-evident.** A gate that **is running** and returns `probe_unavailable` has reached no conclusion, and this repo's doctrine is that such a gate blocks. **Rule (2) is unchanged and remains distinct:** a hook-**launch** failure is the gate not running at all and stays fail-open. The two are different states and the row does not merge them. **The cost is ACCEPTED, not overlooked:** on any host that is neither darwin nor linux (`:138`), and intermittently on macOS whenever `/usr/sbin/lsof` is slow or fails (`:126`, `:130`, `:140`, and `:158` downstream of them), an unbounded `Read` is denied where the incumbent allowed it — a **new prohibition** over a probe that today never denies anything. The agent's recovery is the one rule (1) already names: pass a `limit`. This is a deliberate fail-closed posture on a **cost** gate, chosen in full sight of that lockout. **Fixtures — deny:** a simulated `lsof` non-zero exit, and a non-darwin/non-linux platform; both must DENY and both must offer the `limit` recovery in the block message. Rationale — this repo's doctrine is that an enforcement gate which cannot verify its condition blocks, and a *cost* gate is uniquely cheap to satisfy under that rule, since the agent clears it by passing a `limit`, which is meaningful for exactly these cases. This does **not** contradict rule (2): a hook-**launch** failure is the gate not running at all (fail-open, unchanged), whereas an indeterminate probe is the gate running and reaching no conclusion. Fixtures — **deny**: oversized file, containment-check failure, trust-root mutation mid-check. **"Binary file" is deleted from the deny fixtures**; it now appears under (4a) as an allow case.

**[#840 adopted as the measurement half]** Additionally record total elapsed time, primary-agent tokens, available total cost, citation accuracy and rereads caused by missing context — measurement is part of acceptance, not a precondition for shipping the gate.
