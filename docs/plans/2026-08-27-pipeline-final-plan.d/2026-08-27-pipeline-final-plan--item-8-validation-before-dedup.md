# Item 8 — validation runs over all ingested records, before deduplicate()

> **INDEPENDENT DESIGN FILE: BINDING, AND UNREVIEWED.** This file is part of the requirements of
> `docs/plans/2026-08-27-pipeline-final-plan.md` (the parent). The parent cites it at § *Item 8* and does
> not restate it. It is reviewed on its own: `init-design-review.sh docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-8-validation-before-dedup.md`, then
> `run-design-review-loop.sh`. Its review verdict covers only this file's spec hash. A verdict on the parent
> does not cover this file, and a verdict on this file does not cover the parent. It is not history.
>
> **Context for a reviewer who reads only this file.** The body below was moved verbatim from the parent on
> 2026-09-23 (Chris-approved split), and this header was added the same day; the body is unchanged. Item
> numbers, node names such as `2a-env`, `baseline captured` and `protocol-approved`, and bound labels such as
> (i-a), (i-b), (2-ORD) and (2-CAP) are defined in the parent's execution-order table (§ *Execution order
> and acceptance*) and § *Still needs Chris*, and, since the parent's SECOND MOVE (2026-09-24), in the
> children: the dependency graph, *Standing fallback*, *Recommended next actions* and § *Item 2* in the 2p
> file; § *Item 2i* and § *Item 8* in the item 8 file; § *Item 2a* in the (i-a) file; § *Item 7* in the
> item 7 file; § *#840 proposed revisions* in the 2p file. The parent keeps only one-line pointers there (2026-09-26; UNREVIEWED). A lens reviewing this file receives this file and is not assumed to open the parent or the checkout.
> Line anchors `:NNN` into the parent point at the parent as it stood before the 2026-09-23 splits. Anchors
> into other files name their own tree or checkpoint.

**THE VALIDATION RUNS OVER ALL INGESTED RECORDS, BEFORE `deduplicate()` — CHRIS-APPROVED 2026-09-09 (`批准840`, round-14 finding [0]); PLAN TEXT ONLY, UNREVIEWED.** The ordering is load-bearing, not an implementation detail. `main()` calls `:185 deduped = deduplicate(all_findings)` and only then `:186 status = determine_status(deduped)`, so a record eliminated by the dedup election at `:36-38` is unobservable to any validation placed inside `determine_status` — the four fail-opens above would be closed for a record that arrives alone and left open for the same record arriving as a duplicate. Measured against the checkpoint-identical worktree merger before writing: a duplicate pair of one valid `medium` (`a.js:10`, `confidence: 0.9`) and one **severity-ABSENT** twin (`a.js:11`, same description, over the `0.6` ratio) returns **PASS at `LITMUS_ITERATION=3` in BOTH input orders**, the malformed record dropped by the election either way; the malformed record ALONE at the same iteration also returns PASS, and the iteration-1 control FAILs on the valid `medium`. So downstream of `:185` the malformed record cannot be blocked, or even counted. **The validation therefore sits at the merger's stdin/argv INGESTION point, ahead of `:185`, and rejects on the record as ingested; a record that fails validation is a blocking validation failure and never enters the election.** It is not a `determine_status` guard and must not be implemented as one — which is the same placement the `[#840 adopted]` paragraph above already requires for the deterministic-blocker contract, stated there for a different reason and cited here rather than re-argued. **Two paths are ALREADY fail-closed and must be preserved, not re-implemented:** `confidence: None` returns FAIL at `:76-78`, and an unparseable `confidence` returns FAIL at `:80-81`; positive infinity also FAILs, incidentally, via the `> 1.0` normalization. **The policy for a VALID `medium` is UNCHANGED** — blocking at iterations 1–2, advisory from iteration 3 — and a fixture pins that explicitly so the tightening cannot be mistaken for a severity-policy change. **Acceptance — FIXTURES CARRY STABLE IDs AND THE TOTAL IS DERIVED FROM THE LIST, NEVER WRITTEN BESIDE IT (corrected 2026-09-10, round-18 findings [10] and [11]; Photon technical decision, UNREVIEWED — the prior tally read "TEN" against a list whose membership had already changed, which is the drift this document's define-once rule exists to prevent).** **R1–R6** assert FAIL at the record level (absent, null, unrecognized-including-miscased, NaN, plus **a `bool` confidence and a NEGATIVE confidence, FOLDED IN FROM DECISION [9] 2026-09-10 (round-17 finding [2]; Photon technical decision, UNREVIEWED — the header carried these and this row, which is the authority, did not**). **THE TRAILING CLAUSE IS DELETED — CORRECTED 2026-09-17 (native run `7886ed9f` MEDIUM, conf 0.93; UNREVIEWED).** It read ", two asserting the incumbent behaviour of the valid `medium` at iteration 1 (FAIL) and iteration 3 (PASS)", and it attributed EIGHT assertions to a SIX-ID range: the parenthesis alone enumerates six record-level shapes. The same paragraph below gives those two their own IDs as **M1** and **M2**, and the derived inventory reads `R1–R6 + N1–N2 + S1–S8 + M1–M2 + O1 + X1–X5` (N1 and N2, and E1 before its 2026-09-23 relocation to `2a-env`, added 2026-09-23 (native run `98787112`, arbiter-reproduced; Chris-approved plan-text fix; UNREVIEWED)), so a generator following the old sentence either put the `bool` and NEGATIVE cases into M1/M2's place — losing the two measured fail-opens this cell documents — or emitted eight R-fixtures and double-counted the mediums. **R1–R6 IS EXACTLY THE SIX RECORD-LEVEL FAILs NAMED IN THE PARENTHESIS, AND M1/M2 ARE THE ONLY MEDIUM-POLICY IDs.** This is the count-written-beside-an-enumerated-set defect the cell's own stable-ID rule exists to prevent, and it is repaired by DELETION rather than by renumbering, so no ID moves. **SHAPE fixtures S1–S8 at the ingestion boundary — ONE PER ENUMERATED SHAPE, not a sample (round-18 finding [11]; UNREVIEWED; LABEL CORRECTED 2026-09-17, run `7886ed9f`: the set this header introduces runs to S8 — S7 and S8 were folded in on 2026-09-13 and are the only members that DISCRIMINATE — while the header still read S1–S6, understating its own enumeration by two)**: S1 `{}`, S2 JSON `""`, S3 a bare number, S4 `{"issues": "lots"}` — the one the incumbent `_ingest` actually mis-ingests, since `lib/merge-findings.py:91-96` tests `"issues" in parsed` without checking the value is a list — each asserting BLOCKING; plus the two mandatory valid-empty controls S5 `[]` and S6 `{"status":"PASS","issues":[]}`, each asserting VALID; **plus S7 `{"issues": []}` with NO `status` key and S8 `{"status": "WAT", "issues": []}` with an UNRECOGNIZED status, each asserting BLOCKING — ADDED 2026-09-13 (native 79ad7ef4 finding [7] MEDIUM; UNREVIEWED), and they are the only members that DISCRIMINATE.** The REVIEW ENVELOPE predicate defined above in this cell — a dict input is an envelope **iff** it carries a RECOGNIZED `status` AND a list-shaped `issues` — differs from the incumbent `isinstance(parsed, list) or (isinstance(parsed, dict) and "issues" in parsed)` (`:91-96`, no status test) on exactly the shapes S7 and S8 describe. S1–S6 were all the inventory had, and **both predicates agree on every one of those six**: the entire suite passed while a dict carrying `issues` with an absent or unrecognized `status` was still silently accepted and contributed nothing, so the cell's claims that "the fixture set now matches it member for member" and is "ONE PER ENUMERATED SHAPE, not a sample" were FALSE as of the predicate correction, and the derived count was read off a list missing its two discriminating members. **The two-rejection retention fixture also gets a stable ID — X5** (two `internal:merge-findings` rejections beside one valid finding; asserts TWO surviving rejection diagnostics, not one, and retention of the valid sibling), because the same cell requires it while the inventory the count is derived from carried no ID for it, and a fixture with no ID is outside a count read off the list. The shape rule stated ABOVE in this cell ("A PARSE FAILURE IS NOT THE WHOLE CLASS — SHAPE VALIDATION ADDED 2026-09-10") enumerates four blocking shapes and two controls, and its own RECOGNIZED-`status` correction adds two more blocking shapes, S7 and S8 (SUMMARY FOLLOWED THROUGH 2026-09-17, run `7886ed9f`: this read "four blocking shapes and two controls" alone, the same two-member understatement corrected in this cell's header); the fixture set matches member for member across S1–S8, and the count is read off **R1–R6 + N1–N2 + S1–S8 + M1–M2 + O1 + X1–X5** rather than restated (S7, S8 and X5 folded in 2026-09-13, native b5bddf50 MEDIUM; UNREVIEWED). **THE DERIVATION WAS INCOMPLETE AND IS CORRECTED 2026-09-12 (native 4efaea28 finding B4, grok issues[9] + codex issues[5] `completeness`, conf 0.85; UNREVIEWED).** As written it read "R1–R6 + S1–S6" — twelve — which **silently dropped the two medium controls** (they had no IDs, so nothing to derive from) **and O1** (introduced in its own later paragraph). O1 is the ONLY fixture pinning validation BEFORE `:185`, i.e. the single check that holds the approved ordering, so a generator faithfully following the stated derivation would have produced a set missing the one fixture the ordering decision rests on. **The two medium controls are hereby `M1` (valid `medium`, iteration 1 ⇒ FAIL) and `M2` (valid `medium`, iteration 3 ⇒ PASS)**, and **O1** (validation runs before `:185`) is a member of the inventory rather than a remark beside it. **X1–X4 ARE MIXED-INPUT FIXTURES, ADDED BY THE SAME FINDING**, because nothing in R1–R6 or S1–S6 requires a rejected input to sit BESIDE a valid one — and neither do S7 and S8, added 2026-09-13 after this rationale was written, so extending the set did not close this gap (NOTED 2026-09-17, run `7886ed9f`; the sentence's conclusion is unchanged) — so an implementation that returns on first rejection and discards valid siblings passes every enumerated check while reproducing exactly the evidence loss this row's own C4 measurement recorded (`[null, <validFinding>]` and `source: null` each exiting rc 1 with EMPTY stdout, the valid sibling lost). Each of X1 unparseable-beside-valid (scoped to NON-SCANNER inputs: a scanner's failure token beside a clean review is owned by item 2a bound (i-c)'s (2-ORD) readers — merger/short-circuit, and the exit-3 built-in handoff check of item 2i(B) (A-iv) — and their mixed-run fixtures in `2a-env`, which X1 cites and does not re-implement — ownership cut stated 2026-09-13, native 2a9a6900 finding [11]; UNREVIEWED), X2 invalid-container-beside-valid, X3 non-dict-member-beside-valid, X4 null/non-string-`source`-beside-valid is driven through **BOTH stdin and argv** and asserts all four of: a VALID result JSON on stdout, `FAIL`, an **INDEXED** `internal:merge-findings` diagnostic naming the rejected input, and **RETENTION of the valid sibling in `issues[]`**. **THE INDEX IS NOT DERIVABLE FROM THE CURRENT CHANNELS, SO THE INFORMATION-FLOW CHANGE IS PART OF THIS DELIVERABLE — ADDED 2026-09-13 (native 79ad7ef4 finding [6] MEDIUM; UNREVIEWED).** Measured at the checkpoint: `_parse_concatenated_json` (`lib/merge-findings.py:99`) returns only `(parsed_values, parse_errors)`; the malformed payload is skipped by the restart scan at `:143-145` and **its position is never recorded**; `main()` then derives `total_inputs = len(parsed_values) + parse_errors` at `:166`, so **no input identity survives parsing at all**; and the argv path, which does have an index, discards it identically (`except … parse_errors += 1; continue`, `:158-161`). Requiring the diagnostic without requiring this change left the fixture unsatisfiable on both channels. **THE CHANGE: the parser returns a PER-FAILURE RECORD instead of a count** — at minimum `(ordinal, byte_offset, shape_observed)` — and `total_inputs` is derived FROM those records rather than recomputed from a tally, so the count and the diagnostics cannot disagree. **WHAT "NAMING THE REJECTED INPUT" MEANS IS PER CHANNEL, because the two channels do not carry the same identity:** on **argv**, the argv INDEX alone — argv items are JSON payloads decoded by `json.loads(arg)` (`:156-161`), not filenames; on **stdin**, the decode ORDINAL and the BYTE OFFSET at which the failed decode began, computed as `len(text[:char_index].encode('utf-8'))` from the CHARACTER index the restart scan at `:143-145` yields (fixture: a malformed input after a non-ASCII prefix) — and **NOT a reviewer or scanner identity**, because stdin inputs arrive concatenated into one buffer with no framing, so no identity is recoverable for a payload that FAILED to decode — any slot id bound under item 2a bound (ii)(b) travels inside a parsed envelope and is lost with the payload (scoped 2026-09-13, native b5bddf50 MEDIUM; UNREVIEWED). Demanding a reviewer name there would make X1–X4's stdin half permanently unsatisfiable, which is why the requirement is stated as an ordinal rather than quietly dropped for stdin. **THE INDEXED DIAGNOSTIC IS DEFEATED BY DEDUP FOR TWO OR MORE REJECTIONS, AND IS FIXED HERE — ADDED 2026-09-12 (native c94fb702 finding [12], agy issues[1] at reduced severity, conf 0.6; UNREVIEWED).** Verified in source: `_is_deterministic_blocker` (`:45-49`) exempts only `sast:`/`lint:` prefixes, `deduplicate` runs at `:185` **before** `determine_status` at `:186`, and the dedup key is file equality plus a `±3` line window (`:26-27`) plus description similarity `> 0.6` with severity-rank election at `:35-38`. Item 8's rejection findings all carry `file: ""` and `line: 0` with near-identical descriptions, so **two or more of them collapse to one** and the per-input index X1–X4 exist to observe is lost; a same-key higher-severity finding can also elect a rejection away. **THE FIX: rejection findings are NON-COLLAPSIBLE — add `source.startswith("internal:")` to the exemption alongside `sast:`/`lint:`. ONE ROUTE, NOT A CHOICE — CORRECTED 2026-09-13 (native 79ad7ef4 finding [5] MEDIUM, VERIFIED BY EXECUTION; UNREVIEWED).** This read *"…or make the rejection's dedup key input-specific (the input index or filename inside the description or a `rule_id`). Either satisfies the requirement; the exemption is the smaller change"* — and **"either" was false, measured**. The duplicate predicate is same `file` AND `abs(line delta) <= 3` AND `SequenceMatcher` ratio > 0.6 (`lib/merge-findings.py:26-32`), while every rejection finding carries `file: ""` and `line: 0`, so the first two conjuncts are satisfied unconditionally and only the ratio decides. Calling `deduplicate()` directly at the checkpoint: two rejections differing only by "input 1" vs "input 2" inside the description ⇒ similarity **0.984, ONE survivor**; the same pair with distinct `rule_id` ⇒ **ONE survivor**, because `rule_id` is not part of the predicate at all; an "argv[1]" vs "argv[2]" filename variant ⇒ **0.975, ONE survivor**. Every offered alternative loses exactly the per-input diagnostic X1-X4 exist to observe, so the disjunction is DELETED rather than annotated and the source-prefix exemption is the only route. The adopted algorithm (alpha) in item 2i(A) keeps that same edge predicate, so this carries forward unchanged and item 2i(A)'s exemption definition must be extended in the same breath — see the 2i(A) SPLIT-DEFINITION note in that row. **Scope this honestly: it is a DIAGNOSTIC loss, not a fail-open** — a surviving `internal:merge-findings` high with no `confidence` still reaches `:78` and returns FAIL — so it is medium, and the round-14 [11] unlocated rule does NOT cover it (that rule defines unlocated as `line` absent or `None`, while these carry an explicit integer `line: 0`, so the located-vs-located branch applies). **Fixture: two DISTINCT invalid inputs ⇒ TWO surviving rejection findings, not one.** **S5 (`[]`) and S6 (the valid empty envelope) remain the mandatory AUTHORIZING controls, unchanged** — nothing here narrows what is accepted, and the count stays derived from the list exactly as this cell's rule demands. **The two added rejections are measured, not inferred:** `float(False) == 0.0`, so a `high` finding with `confidence: false` returns PASS at `:75-86`, and `-1` and `-0.3` both return PASS because a negative misses the `> 1.0` normalization and the `>= 0.7` comparison alike. **Order is part of the deliverable: reject a `bool` BEFORE `float()`, then non-finite, then negative** — after coercion a `bool` is indistinguishable from `0.0`/`1.0`. The existing 0–1 / 0–100 normalization and the `>= 0.7` comparison are **UNCHANGED**. **NOT WRITTEN, deliberately:** a closed `[0,1]`-only domain (it breaks the 0–100 form), integer-only (it breaks 0–1 floats), or making an omitted/null `medium` FAIL at iteration 3 — the round-4 "confidence blocks unconditionally" over-correction stays reversed. **ORDERING FIXTURE O1 PINS THE ORDERING — ADDED 2026-09-09 (`批准840`, round-14 finding [0]; UNREVIEWED):** the malformed-vs-valid DUPLICATE pair measured above — one valid `medium` and one severity-ABSENT twin, same `file`, `line` within 3, descriptions over the `0.6` ratio — driven through the merger's ingestion interface and asserted to **FAIL at `LITMUS_ITERATION=3` in BOTH input orders**. Both orders are required because the election is order-sensitive at `:36-38` while the defect is not, and iteration 3 is required because it is the iteration at which the valid `medium` alone PASSes — so the only thing that can produce a FAIL is the malformed record actually being validated. It is the one fixture that fails if the validation is placed after `:185`, and the measured incumbent PASS in both orders is its negative baseline. Quote `:70-82` beside `:67` in the test so the author implements the code, not the prose. Cover the 0–1 vs 0–100 normalization branch, run reinitialisation, and both commit/PR modes — **verified on `origin/main`**. Note what the source itself says: the iteration relaxation is a deliberate **server-side override of the prompt contract**, which still tells the LLM to fail on any medium. That mismatch is deliberate and is exactly what item 2a must not silently "fix". **E1 IS `2a-env`'s ACCEPTANCE, NOT ITEM 8's, AND IS OUTSIDE ITEM 8's INVENTORY (relocated 2026-09-23 (native run `6e0afdc0` HIGH, arbiter-confirmed; Photon correction decision; plan-only, UNREVIEWED)).** It is stated below for the merger interface only. **N1, N2 AND E1 — ADDED 2026-09-23 (native run `98787112`, arbiter-reproduced; Chris-approved plan-text fix; UNREVIEWED).** **The non-finite check is `math.isfinite`** on the coerced value, in the order this cell already fixes (a `bool` before `float()`, then non-finite, then negative). **N1:** a `high` finding with `confidence: -Infinity`, driven through the merger's stdin and argv interfaces, merges to FAIL. Today it merges to PASS, because `-inf >= 0.7` is False. **N2:** the same with `confidence: +Infinity` merges to FAIL through validation, not through the `>= 0.7` comparison it happens to pass today. **E1:** **A merged verdict is never PASS when an ingested review envelope stated `status: "FAIL"` and carried NO finding (the empty `issues` list).** Such an envelope is a BLOCKING INGESTION INCONSISTENCY; a FAIL envelope that carries findings is decided by `determine_status` over them, so the iteration relaxation is unchanged and a valid medium-only FAIL envelope still merges to PASS at iteration 3 (2026-09-26; plan-only, UNREVIEWED): the merger emits the existing `internal:merge-findings` HIGH synthetic diagnostic naming that input, so the merged status is FAIL. The fixture: `{"status":"FAIL","issues":[],"completeness":"complete"}` alone, and beside a clean scanner `[]`, in both input orders, on both stdin and argv, merges to FAIL and carries the `internal:merge-findings` HIGH diagnostic naming that input. Its positive control is S6: the valid empty envelope stating PASS still authorizes. Neither the S5 and S6 controls nor any other accepted shape is narrowed.

---

## Moved from the parent 2026-09-24: § *Item 2i*

> **MOVED VERBATIM (2026-09-24; Chris-approved shrink; plan-only, UNREVIEWED).** The text below was the parent's § *Item 2i*, at parent spec `8bfd530e…`; the parent keeps a one-line pointer to this file at its place. It binds as part of this file and is reviewed with this file's spec hash; a verdict on the parent does not cover it. Inside it, "this file", "this document", "above", "below" and line anchors `:NNN` into the parent refer to the parent as it stood before this move.

**Pre-baseline integrity preconditions** (new 2026-09-08; the *shared root* of four round-5 HIGH findings). The plan's founding argument is that a gate with known fail-open paths cannot be measured, and it applies that argument to pull **#844** in front of collection. But #844 and item 2a's integrity half were specified *inside* rows whose status is BLOCKED, so the one class of work that must precede all measurement had **no legal start** — and the resulting deadlock is what produced the "standing fallback" that re-opened the bounded-tranche question. This row is that missing stage. It depends on **neither Chris #2 nor item 1**. Two members. **(A) #844 — dedup runs before status and ranks by severity alone.** ([Issue #844](https://github.com/chris-yyau/busdriver/issues/844), opened 2026-09-07, **CLOSED 2026-09-15 by PR #856 — status defined once at item 2i(A), cited here; the obligation is open and the change is absent from this branch**.) **Scope it to the general defect, not the scanner instance** — the round-5 arbiter *reproduced* both of these by executing the merger at the checkpoint: an LLM `{high, 0.6}` evicts a matching LLM `{medium, 0.9}` and returns **PASS in both input orders** at `LITMUS_ITERATION=1` (the medium alone returns FAIL), and matching `{high, 0.6}` vs `{high, 0.9}` return **PASS or FAIL purely by argument order** at iterations 1 and 3. Neither case involves a scanner finding, so the earlier "preserve `sast:`/`lint:` findings" repair fixes neither. Fix — **primary prescription corrected AGAIN 2026-09-08 (round 7), because "rank by blocking power with a deterministic tie-break" cannot deliver the order-independence this row's own settling check demands.** Reproduced at the checkpoint: `deduplicate()` (`lib/merge-findings.py:20-41`) is a greedy single pass over a mutable `seen` list which, on a win, does `seen.remove(s); seen.append(f)` — moving the survivor to the END and so changing the comparison order for every later finding — and the match predicate is `SequenceMatcher` ratio `> 0.6`, which is **not transitive**. With three same-file same-line findings whose pairwise similarities are A~B 0.921, A~C 0.653, B~C 0.587, a **fully deterministic** tie-break (severity rank, then confidence) still yields **THREE distinct surviving sets across the 6 input permutations** — `{B}`, `{B,C}` and `{A,B}`. A tie-break is necessary and demonstrably not sufficient; the order-dependence lives in the greedy traversal, not in the comparison. **So: replace the construction, do not re-rank it.** **First make the edge relation symmetric, then cluster.** `SequenceMatcher(None, a, b).ratio()` is **not symmetric**, so the graph's own edges depend on comparison order and no clustering over it can be order-independent. Measured 2026-09-08 on the chain fixture below: `A→B = 0.608108` but `B→A = 0.567568`, which **straddles the 0.6 threshold** — the A–B edge exists or does not exist depending purely on which finding the merger reaches first (`A→C = 0.428571` vs `C→A = 0.396825` is asymmetric too, though it does not cross). **Required: compare each pair in a canonical order — sort the two NORMALIZED descriptions (the lower-cased text the merger already compares) lexicographically and always call `SequenceMatcher` on `(lo, hi)`.** Canonical ordering is specified because it is deterministic and costs one comparison rather than a second `SequenceMatcher` call; `max(fwd, rev)` and `min(fwd, rev)` are also order-independent, but they are **different clusterings** — on the measured fixture `min` retains a finding the other two elect away — so the symmetrization must be NAMED, not left to the implementer. Any agreement between canonical and `max` on a particular fixture is a property of that fixture, **not** a general equivalence. **Next, state the EDGE PREDICATE in full — it is not description similarity alone. Corrected 2026-09-08 (round 8); an earlier version of this clause said only "build the similarity graph over ALL findings" and specified only the `SequenceMatcher` comparison.** Verified at the checkpoint, `deduplicate()` reaches `SequenceMatcher` only after two guards: `f.get("file") == s.get("file")` and `abs(f.get("line", 0) - s.get("line", 0)) <= 3` (`lib/merge-findings.py:26-27`). Those guards are **requirements of this row**, not incidental incumbent behaviour that an implementer may infer: an edge exists **iff** same `file` **AND** `abs(line delta) <= 3` **AND** canonical-order `SequenceMatcher` ratio `> 0.6`. Read without them, this prescription merges findings in *unrelated files* — a strictly larger over-merge than any ordering defect, and one this row's same-file same-line fixtures cannot detect. Measured 2026-09-08 by executing both readings: two findings at canonical ratio `0.972`, one in `app/a.py` and one in `app/b.py`, are collapsed to a single survivor by the description-only reading and kept separate by the full predicate. **Add a negative control to the acceptance matrix: near-identical descriptions in two different files, asserted NOT merged.**

**One large scanner-output benchmark is also part of this acceptance matrix — added 2026-09-08 (round 8, M23); its OPENING IS RESTATED 2026-09-10 in the terms of the algorithm this row actually adopts (round-17 finding [11]; Photon technical decision, UNREVIEWED), because it described a population and a cost model the row had already replaced.** Under the adopted **(α)** the work is **sorted NON-DETERMINISTIC candidates compared against the KEPT REPRESENTATIVES** — there is no separate all-pairs graph to construct — and it is **not** bounded by the LLM reviewer's 10-issue budget, because scanner findings enter the merger as well — **and that is consistent with the exclusion clause, not a contradiction of it (CORRECTED 2026-09-10, round-16 finding [0]; Photon technical decision, UNREVIEWED)**: the merger's deterministic-source exclusion is by **source prefix ALONE** — **every** `sast:`/`lint:` finding, at **every severity**, is kept out of the similarity graph, is never a collapse candidate and is never collapsed into. **The round-10 (finding [19]) paraphrase that scoped this exclusion to blocking severities, together with its "two different populations" gloss, is DELETED rather than amended.** It stated a rule this row never approved, and it described the DEFECT rather than the target state: `_is_deterministic_blocker` (`lib/merge-findings.py:45-49`) is a severity ∧ source **conjunction** today, and it STAYS one: item 2i(A)'s source-prefix-alone rule replaces only the dedup-exclusion test (:322), never this blocking predicate, so a valid low `sast:`/`lint:` finding still does not block (clarified 2026-09-24; plan-only, UNREVIEWED). Scanner findings still reach the merger, so the bound below is real. **THE FIGURES THAT FOLLOW ARE INCUMBENT ALL-PAIRS MEASUREMENTS, NOT (α)'s COST — LABELLED 2026-09-10 (round-17 finding [11]; UNREVIEWED).** They characterize the election this row REPLACES and are kept as the honest upper bound; (α)'s cost is bounded by candidates × kept representatives and **has not been measured**, which is what the benchmark is for. Measured at the checkpoint on the incumbent, **499 comparisons ≈ 10 ms** against **124,750 ≈ 2.6 s** for 500 findings, with `SequenceMatcher` itself quadratic per comparison. The benchmark's population, the source-prefix exclusion and its acceptance scope are unchanged by this restatement. **The rationale first given for calling it non-blocking is WITHDRAWN — it was refuted by execution 2026-09-09 (round 9, finding [23]).** That rationale said the two guards run before any `SequenceMatcher` call "which is what keeps the real cost near the 10 ms figure". Measured through the guards: 500 findings at the SAME file and line pass BOTH guards for all **124,750** pairs — **124,750 `SequenceMatcher` calls, 4.81 s**; 500 across distinct files, **0 calls / 5 ms**; 500 in one file with spread lines, **0 calls / 8 ms**. The guards bound the cost only when findings are SPREAD and bound it **not at all** in a dense bucket — which is both this row's own #844 fixture shape (same file, same line) and the normal shape of scanner output at one location. The benchmark is **retained**, and it is still recorded low and non-blocking, but on the honest ground: the quadratic cost is real and unbounded in the dense case, and this row accepts it rather than explains it away. A separate correction from the same finding: the benchmark must use **non-deterministic** findings, since the **source-prefix exclusion stated once above** — cited here, not restated, and no longer paraphrased with a severity scope it does not have (**CORRECTED 2026-09-10, round-15 finding [2]; UNREVIEWED**) — keeps **every** `sast:`/`lint:` finding out of the graph regardless of severity, so a 500-SAST-finding benchmark would exercise nothing. **Add one passthrough control alongside it:** a valid `low` `sast:` finding duplicated in shape, asserted to survive with **no similarity comparison performed on it at all** — that is what distinguishes true exclusion from a rank that merely happens to win.

**Then EXCLUDE deterministic blockers from dedup entirely — do not merely rank them. Corrected 2026-09-08 (round 8), because the election key as previously written reintroduced the very #844 false-PASS this row exists to close.** Verified at the checkpoint: `_is_deterministic_blocker` (`:45-49`) returns True for any `sast:`/`lint:` finding at high or medium, and `determine_status` returns FAIL on it **first** (`:70-71`), *before* the iteration filter at `:72-73`. The previous key placed `deterministic_source` **fourth**, behind `normalized_confidence`, so a matching `sast:` medium at confidence `0.4` and an `llm:` medium at `0.99` tie on the first two elements and the LLM finding wins on confidence — the deterministic element never runs. Measured 2026-09-08 over all input permutations at `LITMUS_ITERATION=3`: the undeduped truth is **FAIL** (the SAST medium blocks regardless of iteration); the incumbent returns **FAIL or PASS by input order**; and the previously prescribed key returns **PASS deterministically, in every order** — converting today's order-dependent coin-flip into a reliable false PASS, which is worse than the defect it replaces. So: deterministic findings — **identified by SOURCE PREFIX (`sast:` / `lint:` / `internal:`) ALONE, independently of the incumbent severity membership test** — are **never candidates for collapse and never collapsed into**; they always survive to `issues[]`. **`internal:` IS A MEMBER OF THIS SET, AND THIS ROW IS ITS ONE OWNER — ADDED 2026-09-13 (native 79ad7ef4 finding [15] MEDIUM; UNREVIEWED).** Item 8 previously prescribed extending this exemption with `internal:` while this row defined the set CLOSED at two members, and the two rows are not schedulable together: this row is startable today with no predecessor, item 8's mutating half waits on `baseline captured`. A worker implementing algorithm (alpha) from the canonical definition would therefore ship it WITHOUT `internal:`, every multi-rejection diagnostic would still collapse (measured 0.984 similarity, one survivor), and item 8's X1-X4 multi-input assertions would fail on the algorithm this plan told the earlier worker to build — with neither surface owning the final exclusion set and no edge connecting them. The set is now stated once, HERE, with all three members; item 8 CITES it and prescribes no extension of its own. Scope is unchanged otherwise: this is the one-line prefix membership, not item 8's gated policy work, which stays behind its own gates. **RE-SCOPED 2026-09-10 (`全部批准`, round-14 finding [14]; UNREVIEWED).** The rule previously inherited its scope from `_is_deterministic_blocker`, which is a **CONJUNCTION** — verified at HEAD, `lib/merge-findings.py:45-49`: `severity in ("high", "medium") and (source.startswith("sast:") or source.startswith("lint:"))`. A `sast:` or `lint:` finding whose severity is ABSENT, `null` or merely miscased `"HIGH"` therefore failed the first conjunct, was never excluded, and remained an ordinary collapse candidate — so the rule protected strictly fewer findings than it appeared to, and the ones it silently dropped are exactly the malformed records item 8 requires to block. Keying the exclusion on the source prefix closes that gap and touches no blocking predicate. **TWO THINGS THIS DOES NOT DO, named because each would be new policy and neither is authorized here:** it does **not** make a VALID `low` `sast:` finding blocking — such a finding becomes protected from collapse and still does not block, leaving the incumbent severity policy exactly as it stands, iteration-3 mediums included; and it does **not** move, re-time or re-place item 8's validation, which stays precisely where its row puts it. A malformed `sast:`/`lint:` record is handled there, as a blocking validation failure on the same footing as any other malformed record, and never by a severity test at this layer. Only then: build the similarity graph over the **remaining (non-deterministic) findings**, cluster it, and elect **one representative per cluster** by a **total order stated here rather than by reference to "blocking power"**, since that phrase is undefined where neither twin blocks and would otherwise make the surviving set iteration-dependent. Three properties of that construction are specified below because each was found defective by execution rather than by reading; **all three corrected 2026-09-08 (round 8, findings M6/M7/M8), and none of the three is optional.**

**(α) MEMBERSHIP — collapse only DIRECT duplicates of the elected representative, never everything reachable from it.** Connected components perform **transitive closure** over a predicate that is not transitive, so a finding that matches *nothing in the surviving set* is dropped because some third finding shared wording with both it and the representative. A member is collapsed **iff it satisfies the duplicate predicate against the ELECTED REPRESENTATIVE itself**; a reachable non-duplicate survives as its own representative. **RESTATED 2026-09-09 (round 9, finding [17]) as ONE order-independent loop, because "cluster, then elect one representative per cluster" is inconsistent with this property for any cluster of diameter ≥ 2 and left leftovers unspecified — on a four-node chain A-B-C-D, C and D both survive AND are direct duplicates of each other, so the output held an uncollapsed duplicate pair while the row claimed one representative per cluster. THE ALGORITHM: sort all non-deterministic candidates DESCENDING by the total key; take the head as a representative; drop any later candidate matching an already-kept representative under the full edge predicate (same `file`, `abs(line delta) <= 3`, canonical-order `SequenceMatcher > 0.6`); repeat until none remain. No separate clustering step, no leftovers, and (α)'s direct-match property is preserved exactly. Fixture: the four-node chain, asserting no two survivors are direct duplicates, over all 24 permutations.** Measured 2026-09-08 on this row's own chain fixture (`A~B = 0.608`, `B~C = 0.800`, `A~C = 0.397`, with the election landing on the **endpoint** A): components return `{A}` and **erase C**, while representative-checked membership returns `{A, C}`. **The trade is stated here rather than left to the implementer:** the incumbent `deduplicate()` (`:20-42`) compares each candidate only against kept survivors and so already drops on a direct match alone — this rule preserves that property while removing the ordering defect, and it deliberately accepts a **larger** surviving set than components would produce. **Silent loss is the specific hazard:** on the measured fixture the status is FAIL either way, so nothing in the verdict signals that a finding disappeared.

**(β) THE KEY MUST BE TOTAL, on both grounds it was not.** The order is `(blocks_at_iteration_1, severity_rank, normalized_confidence, deterministic_source, source, description, file, line, category, suggestion)`, compared in that order, **and TERMINATED by a canonical serialization of the complete validated record** — `json.dumps(finding, sort_keys=True, separators=(',',':'))` — so the key is total by construction rather than by enumeration. **THAT CLAIM WAS FALSE FOR EVERY COMPONENT BUT ONE, and is corrected here 2026-09-09 (round 10, finding [8]) — refuted by EXECUTION, not by reading.** The terminator makes the key total only among records whose earlier components all *compare*; it is never reached if an earlier comparison raises. β-1 below supplies a sentinel for `normalized_confidence` alone, while `source`, `description`, `file`, `line`, `category` and `suggestion` are read straight off the finding dicts, where an optional key is simply absent. Running the key exactly as specified: two records identical through `category`, one carrying `suggestion` and one not, raise `TypeError: '<' not supported between instances of 'NoneType' and 'str'`; the same with `line` absent raises `NoneType` vs `int`. **The precise trigger is ASYMMETRIC PRESENCE** — one record has the field and the other does not, with every earlier component equal; when both omit it the tuple compares fine, which is why reading the spec does not reveal it and why a fixture where both sides are well-formed will never catch it. This is the ordinary two-reviewer case, not an exotic one: reviewers differ in which optional fields they emit. **REQUIRED: every component passes through a typed accessor with an explicit per-component sentinel before it enters the tuple — empty string for `source`, `description`, `file`, `category` and `suggestion`; a numeric floor for `line` — **whose value is **`-1`** and which is the SORT SENTINEL ONLY, never the dedup edge predicate — VALUE FIXED HERE 2026-09-09 (`全部批准`, round-14 finding [15]; UNREVIEWED)**, replacing "whose VALUE and semantics are settled below": no concrete value was settled below, or anywhere else, so the one surface that promised it deferred to nothing and every downstream fixture was unwritable. **`-1` is chosen for two reasons and both are checkable:** it sorts strictly below every `line` a reviewer can emit, a file-level `0` included; and it is deliberately NOT the edge predicate's `:27` default of `0`, so the two constants can never be read as one shared value — which is the confusion (β) exists to prevent. It is this list's entry and nothing derives from it; the β-1 sentinel for `normalized_confidence`; `False` for the two booleans; and **`severity_rank`** — under the rank map stated once here, which is the INCUMBENT map, reused rather than reinvented: **`high` 3, `medium` 2, `low` 1, and anything unrecognized (including an absent or `None` severity) 0**, which is also its sentinel. Never a bare `None` in any position**

**THE `line` FLOOR AND THE DEDUP EDGE ARE TWO SEPARATE THINGS — CHRIS-APPROVED 2026-09-09 (`全部批准`, round-13 finding [11]); PLAN TEXT ONLY, UNREVIEWED, and BEHAVIOUR-CHANGING, so it lands behind this row's existing gate alongside the rest of the merger work.** Conflating them is what made the floor's value load-bearing in two incompatible ways. **(a) SORT SENTINEL — ordering only.** `line` absent or `None` sorts as a fixed numeric floor of **`-1`** — the value fixed once beside the other sentinels in the typed-accessor list above, cited here and not restated. It is safe to fix precisely because it never reaches the edge predicate, and it is distinct from that predicate's `0` default so the two are never conflated. **(b) EDGE PREDICATE — dedup only, and NOT a numeric comparison.** The incumbent edge is `abs(f.get("line", 0) - s.get("line", 0)) <= 3` (`lib/merge-findings.py:27`), so under a `0` floor an UNLOCATED finding (`line` absent or `None`) matches **every** finding at lines 0–3 in the same file — collapsing distinct defects and letting the election at `:36-38` discard the loser. **The rule: an UNLOCATED finding is a candidate ONLY against another UNLOCATED finding in the same file; a located finding is a candidate only against another LOCATED finding within 3 lines. Located and unlocated NEVER match each other, at any floor value.** Locatedness is therefore part of the predicate, not something a default smuggles in. **PIN BOTH DIRECTIONS** so neither half can silently invert: (i) two UNLOCATED findings in the same file with descriptions over the `0.6` ratio **DO** collapse, and the election still elects by `severity_rank`; (ii) one UNLOCATED and one LOCATED finding in the same file, descriptions identical, **DO NOT** collapse and both are retained — asserted in both input orders, since the incumbent predicate is order-independent and any replacement must stay so. (the enumeration previously covered nine of the ten components and omitted `severity_rank` while the same sentence demanded one per component; **corrected 2026-09-09, round 11**). **POLARITY CORRECTED 2026-09-09 (round 12) — this is the third round on this prescription, and the two earlier ones were wrong in the same direction.** Round 11 wrote `high` 0, `medium` 1, `low` 2 with the sentinel as "the rank of the LOWEST severity". Under the DESCENDING sort this algorithm specifies, that is inverted twice over: `2 > 1 > 0` elects `low` over `medium` over `high`, and a sentinel of 2 sorts an unknown severity FIRST rather than last. **The map above is the one the incumbent already uses** — `skills/litmus/scripts/lib/merge-findings.py:35` builds `{"high": 3, "medium": 2, "low": 1}` and compares with `sev_rank.get(...) > sev_rank.get(...)` at `:36`, whose `.get(..., 0)` default places an unrecognized severity BELOW `low`. Descending order and severity now agree, and the sentinel sorts last. **Note for the implementer: that map is FUNCTION-LOCAL to `deduplicate()` and is not an importable module symbol** — the diagnostic below therefore asserts on observable behaviour, and any implementation that wants to share the map must first hoist it, in the same reviewed diff. **The repair is NOT confined to the ordering key — corrected 2026-09-09 (round 11).** `lib/merge-findings.py:27` computes the duplicate predicate `abs(f.get("line", 0) - s.get("line", 0)) <= 3` on RAW fields, and `.get("line", 0)` returns its default only when the key is ABSENT: a record carrying `line: null` yields `None`, `None - 0` raises `TypeError`, and the sort-key repair never touches it. **So the ORDERING AND DEDUPLICATION consumers take the same typed accessor — and ONLY those. SCOPED 2026-09-09 (round 12).** Round 11 stated this universally over "every raw-field consumer in the merger", which inverts the merger's fail-closed authorization semantics: `determine_status` is also a raw-field consumer, and there a missing or `None` field must keep failing CLOSED rather than being coerced to a permissive sentinel. **A sentinel is a TOTALITY device for a sort key, never a defaulting device for an authorization decision** — applying it to the status path would turn an unreadable finding into a passing one, which is the exact class of defect the two-stage completeness work exists to remove. The mandate therefore covers the sort key and the duplicate predicate at `:27`; `determine_status` and every other authorization-bearing reader are **explicitly excluded from the mandate, which may not weaken them**. **QUALIFIED 2026-09-09 (`全部批准`, round-14 finding [2]; UNREVIEWED):** this clause read "excluded and **stay** fail-closed", which is false about the incumbent — item 8's row measures **four reachable fail-OPENs** in `determine_status` at HEAD (severity absent, `null`, unrecognized-including-miscased, and a NON-FINITE confidence: `NaN`, and `-Infinity` as well, added 2026-09-23 (native run `98787112`, arbiter-reproduced; Chris-approved plan-text fix; UNREVIEWED)), plus the input-level fail-opens item 8 lists after them (those two relocated to `2a-env` 2026-09-23 (native run `6e0afdc0` HIGH, arbiter-confirmed; Photon correction decision; plan-only, UNREVIEWED)), one of which is an envelope stating FAIL that merges to PASS; closing them is an approved deliverable of the `2a-env` scope, before `baseline captured` (relocated from item 8 on 2026-09-23; this sentence aligned 2026-09-24; plan-only, UNREVIEWED), cited here and not restated. So the exclusion means the mandate leaves those readers untouched, **not** that they are already fail-closed. **SCHEDULE RECONCILIATION, stated once because the two halves are separately gated:** 2i(A) has no predecessor and is startable now, while the fail-closed validation lands in `2a-env`, which waits on the whole 2i NODE and precedes `baseline captured`, so there is a real interval in which this mandate has landed and the authorization-bearing readers are still fail-open. The mandate is not authority to treat them as sound during that interval, and the `2a-env` validation is not a prerequisite of 2i(A) — neither row may be declared complete on the other's progress. **THAT EXCLUSION IS OVER READERS AND DOES NOT LICENSE DEDUP TO ACT AS AN ELIMINATOR — CORRECTED 2026-09-09 (`批准840`, round-14 finding [0]; UNREVIEWED).** `deduplicate()` is not an authorization-bearing reader, so the clause as written did not reach it — but at `:36-38` it *removes* records, and a record removed there is unobservable to every fail-closed reader downstream (`main()` runs `:185` before `:186`). A permissive sentinel in the election can therefore retire a malformed authorization-bearing record that the excluded readers would have blocked, which is the same fail-open the exclusion exists to prevent, reached one frame earlier. **The gap is closed in `2a-env`, not here:** the fail-closed validation `2a-env` carries (its text stays in item 8's row) rejects such a record at the merger's ingestion interface, BEFORE `:185`, so no unvalidated record ever reaches this election. This row's sentinel therefore stays exactly what the preceding sentence makes it — a TOTALITY device that keeps the sort key from raising — and never becomes the thing that decides a malformed record's fate. With that scope, the fixture set gains a case the prescribed β-2 cannot reach — β-2 keys on PRESENCE asymmetry, which is precisely what does NOT reproduce this: **β-3, a pair whose `line` is PRESENT and `null` on one side** — β-1 already made exactly this decision for one component, and it is applied to the rest rather than restated as a new rule. **Fixture (β-2): a pair identical through `category` differing only in the PRESENCE of `suggestion`, asserted to elect without raising and to return the same survivor set in both input orders; and the same for an absent `line`.** **EXECUTABLE DIAGNOSTIC — REQUIRED, ADDED 2026-09-09 (round 12). Prose has now failed three consecutive rounds to keep this prescription correct, so the polarity becomes a runnable assertion or it is not settled.** Four checks, all asserting on OBSERVABLE BEHAVIOUR of the merger's public entry points — **never by importing the rank map, which is function-local to `deduplicate()` and not a module symbol**:
  **(D-1) POLARITY, through `deduplicate()`.** Two findings with the same `file`, `line` within 3, and descriptions similar enough to trip the `0.6` `SequenceMatcher` ratio at `:32`, one `high` and one `low`, **both carrying the same `confidence: 0.5`** so `blocks_at_iteration_1` ties and `severity_rank` alone decides (**PINNED 2026-09-10, round-17 finding [6]; UNREVIEWED** — the same isolation as (D-2b) and (D-3), applied here because the same masking defect applies). Assert the survivor is the **`high`**, in BOTH input orders. This is the check that would have caught the round-10 and round-11 maps.
  **(D-2) SENTINEL PLACEMENT — RESTATED 2026-09-09 (round-13 finding [10]; UNREVIEWED).** It previously read: *"Sort a list containing one finding whose `severity` key is ABSENT and one `low`. Assert the absent-severity entry sorts after the `low` under the descending total key."* **That check could not be run under this diagnostic's own preamble.** There is no sort site in the repo, and the preamble forbids importing the rank map, so "the descending total key" names nothing observable; worse, the pair it describes is a DUPLICATE pair, so `deduplicate()` returns one element and no order exists to assert. **Two fixtures replace it, both through the public interface, neither introducing a module-level abstraction:**
  **(D-2a) RETENTION, non-duplicate pair.** One finding with `severity` ABSENT and one `low`, in **different `file`s** — so the `f.get("file") == s.get("file")` conjunct at `:26` fails (**anchor corrected 2026-09-10, round-17 finding [12]; `:25` is `for s in seen:`, verified identical at HEAD and at the checkpoint `727d4652`**) and neither can eliminate the other. Assert `deduplicate()` returns **both**, in both input orders. **RESTATED AS RETENTION ONLY 2026-09-09 (`全部批准`, round-14 finding [3]; UNREVIEWED):** the assertion read "returns both, **in input order**", carried by the rationale that "`deduplicate()` appends non-duplicates in insertion order and **never sorts**" — which contradicts algorithm (α), the prescription this same row adopts, in which the pass **does** sort. Both the ordering assertion and its rationale are withdrawn. The property this fixture owns is RETENTION: neither record is eliminated. It asserts nothing about output order and so constrains (α) not at all. This proves the sentinel does not cause a silent drop. **It does NOT exercise the rank map**, and it is recorded as a control precisely so no one mistakes it for the polarity check.
  **(D-2b) RANK ORDER, through the election, OVER VALID RECORDS ONLY — CORRECTED 2026-09-10 (`全部批准`, round-14 finding [14]; UNREVIEWED).** The rank map is observable at exactly one place: the replacement inside the duplicate branch at `:36-38`. So take a genuine DUPLICATE pair — same `file`, `line` within 3, descriptions over the `0.6` ratio — one **`low`** and one **`medium`**, **both VALID under item 8's contract**, **both carrying the same `confidence: 0.5`** — below item 8's `0.7` blocking threshold, so `blocks_at_iteration_1` **TIES** and `severity_rank` is the ONLY discriminator under the adopted key (**PINNED 2026-09-10, round-17 finding [6]; Photon technical decision, UNREVIEWED**). Unpinned, the leading conjunct decides the outcome whenever confidence reaches `0.7`, and a broken rank map — `{high:3, medium:1, low:2}` — would still pass this fixture. Assert the survivor is the **`medium`**, in BOTH input orders; measured against the real merger, it is. **Isolation is by INPUT CONSTRUCTION only:** the D-* preamble forbids importing or mutating the rank map, so no "swap the map" control is added here. **Blocking power remains (γ)'s fixture** — `{medium, 0.9}` against `{high, 0.5}` at natural confidence — and is not duplicated into this one. **WHAT THIS REPLACES AND WHY.** The pair was previously one `low` and one whose `severity` key was **ABSENT**, asserting that the `low` survives. Under the fail-closed validation approved in item 8's row — the ONE validation-before-dedup rule, which runs over all ingested records ahead of `:185` and is cited here, not restated — an absent-severity record is a **validation failure that must BLOCK**. Requiring it to be *elected away* by a non-blocking `low` would have written a green regression test around exactly the elimination item 8 exists to close, and pinned the fail-open into the acceptance matrix. **THE INCUMBENT BEHAVIOUR IS RECORDED AS A MEASURED DEFECT, NEVER AS ACCEPTANCE:** against the real merger the ABSENT record is dropped in BOTH input orders. That is the negative baseline this correction exists to close; no fixture may require it. **What the corrected pair still pins, stated without overclaiming:** the rank map's ordering over the records that actually reach the election once malformed input is blocked upstream. The old pair's discriminating power came from its **sentinel** rung, and that rung is no longer observable through an election at all — see (D-3), where the consequence is faced rather than papered over. **A fixture that cannot fail is worse than none**, which is why (D-2a) is labelled a control and (D-2b) carries the assertion.
  **(D-3) MONOTONICITY, OVER VALID RECORDS — CORRECTED 2026-09-10 (`全部批准`, round-14 finding [14]; UNREVIEWED).** Assert `rank(high) > rank(medium) > rank(low)` **as observed through election outcomes**, one pairwise `deduplicate()` call per comparison — **each pair carrying `confidence: 0.5` on BOTH candidates and asserted in BOTH input orders, the same severity isolation (D-2b) uses and for the same reason (PINNED 2026-09-10, round-17 finding [6]; UNREVIEWED)** — so the assertion holds against behaviour rather than against a restated constant, and against `severity_rank` rather than against the blocking conjunct that would otherwise mask it. **The `> sentinel` rung is REMOVED from this election-observed chain, and the reason is a consequence rather than a concession:** the only record that could exercise it is one whose severity is absent or unrecognized, and under item 8's approved validation such a record is blocked at ingestion and never reaches `:36-38`. A rung that requires a record the pipeline must reject cannot be observed through an election without mandating the rejection be skipped — the same defect (D-2b) carried. **The sentinel entry therefore stays what it is: a stated element of the rank map, pinned where the map is stated, and pinned behaviourally at the VALIDATION stage** by the malformed-record controls below, which assert it BLOCKS rather than that it loses an election.
  **(D-4) THE NULL CASE (β-3).** A pair whose `line` is PRESENT and `null` on one side, asserting `deduplicate()` returns without raising `TypeError` from `abs(f.get("line", 0) - s.get("line", 0))` at `:27`.
  **(D-5) MALFORMED RECORDS — BLOCKING IN `2a-env`, BEFORE `baseline captured` (relocated from item 8 2026-09-23; header aligned 2026-09-24; plan-only, UNREVIEWED), NOTHING ASSERTED HERE. ADDED 2026-09-10 (`全部批准`, round-14 finding [14]), HALF (a) WITHDRAWN 2026-09-10 (round-15 finding [0]); UNREVIEWED.** It replaced the assertions (D-2b) and (D-3) used to carry. Its header once read "RETENTION HERE, BLOCKING THERE"; only the blocking half survives, and it lives in the fail-closed validation `2a-env` carries (its text stays in item 8's row), not in this one. **(a) WITHDRAWN 2026-09-10 (round-15 finding [0]; Photon technical decision under delegation — NOT a Chris approval and NOT a retention policy).** It read: "for a duplicate pair of one VALID record and one whose `severity` is ABSENT, `null` or miscased `"HIGH"`, assert through a direct `deduplicate()` call that the malformed record is **RETAINED, in BOTH input orders** — never elected away." **That assertion is unsatisfiable and is deleted rather than repaired.** Measured against the real merger and against this row's own prescription: the malformed twin is DROPPED in **12 of 12** cases — incumbent election and simulated (α)+(β), all three malformed shapes, both input orders. Under (β) the malformed record ranks `0`, the valid record wins the first discriminating key element, and (α) drops the later duplicate deterministically. **The only way to satisfy it would be a second, severity-shaped non-collapse class inside (α), symmetric with the source-prefix exclusion — that is NEW policy this plan never approved, so it is REJECTED, and this note is its provenance rather than a silent removal.** **WHAT REPLACES IT: nothing at this layer.** A malformed record's fate is decided **solely** by the fail-closed validation at ingestion, which lands in `2a-env` before `baseline captured` and not behind item 8's gate. **CURRENT INTERVAL LIMITATION, stated as a limitation and not as desired correctness:** 2i(A) is startable now and the fail-closed validation lands later, in `2a-env`, so in that interval the election — incumbent and (α) alike — still silently drops malformed records. That is the defect that validation exists to close, **not** a property this row endorses, and no fixture may assert it as an expected outcome. **It is also load-bearing for the ordering proof:** because the election drops the malformed record, item 8's seventh fixture FAILs under the approved placement and PASSes when validation is misplaced after `:185`, which is exactly what makes it discriminate. Had retention been prescribed, that fixture would have passed under both placements and stopped proving anything. **(b) THE BLOCKING PROPERTY, owned by `2a-env`'s acceptance and CITED not duplicated:** that each of those malformed shapes is a blocking validation failure is asserted by the fixtures stated in item 8's contract file (R1–R6, N1–N2 and S1–S8), which are `2a-env`'s pre-baseline acceptance. **This row does not assert it, does not schedule it, and does not move that validation** — the split exists precisely so that 2i(A) stays startable while the blocking half lands in `2a-env`.
**SCOPE OF (D-2a), (D-2b) AND (D-3) AGAINST ITEM 8'S VALIDATION — ADDED 2026-09-09 (`批准840`, round-14 finding [0]; UNREVIEWED).** All three call `deduplicate()` **directly**, so they probe the election in isolation and remain falsifiable as written. **AMENDED 2026-09-10 (`全部批准`, round-14 finding [14]; UNREVIEWED) — this note was NECESSARY BUT NOT SUFFICIENT, and saying so is the point:** establishing that a fixture stays *runnable and falsifiable* says nothing about whether the outcome it ASSERTS is consistent with item 8's contract, and (D-2b)'s was not — it required a validation failure to be elected away. Direct invocation preserved the probe and did not preserve the doctrine. (D-2b) and (D-3) are corrected above; the sentence this replaces also claimed "(D-2b) still fails under the round-11 map", which described the retired absent-severity pair and no longer describes the fixture. They are **not** statements about the pipeline. Item 8's fail-closed validation rejects a record with an absent or unrecognized `severity` at ingestion, ahead of `:185`, so once that lands no such record reaches this election through `main()` and the `severity_rank` sentinel's own branch is unobservable end-to-end even though the sort key still needs it to stay total. **Read these three as rank-map regression probes over a function, never as authority that a malformed authorization-bearing record may legitimately be elected away** — item 8 decides that record's fate, and it decides it by blocking. **D-1 and D-2 together make the polarity falsifiable**, which is the property this prescription has lacked since it was first written. **A future edit to the rank map that does not keep all four green is a regression by definition.** **Corrected 2026-09-09 (round 9, finding [11]): two ties survived the earlier key over records the merger genuinely emits.** (1) `deterministic_source` is boolean-shaped (`lib/merge-findings.py:45-49` tests only a `sast:`/`lint:` prefix at blocking severity), so it does not separate `llm:codex` from `llm:grok`, and `source` was **not in the key at all** — two findings differing only in `source` tied on every element and elected by input order, which is routine in a three-reviewer gate with a fixed finding schema. (2) `normalized_confidence` maps `0.9` and `90` to the same value through the 0-100 branch the merger already carries at `:79-80`, while the emitted records differ. **Non-finite confidences (`nan`, `inf`) take the same sentinel as (β-1)'s missing/non-numeric case** — `nan` in particular is never order-comparable and must not reach the tuple. **Fixtures: a source-only pair and a `0.9`-vs-`90` pair, each asserted over BOTH input orders.** *(β-1)* **`normalized_confidence` is defined for missing and non-numeric values as a sentinel that sorts BELOW every real confidence — never a bare `None` in a comparison tuple.** A missing `confidence` is the normal case for a finding the merger nonetheless blocks on (`determine_status` `:76-78` treats absence as "deterministic source, always block"), and phase 5's exclusion of `sast:`/`lint:` sources does **not** cover it: an `llm:`-sourced finding with no confidence field is still a candidate. Measured 2026-09-08 — such a finding paired with a matching `{high, 0.9}` twin makes the key as previously written raise **`TypeError: '<' not supported between instances of 'float' and 'NoneType'`**, a hard runtime failure of the merger on a pair whose undeduped truth is FAIL. *(β-2)* **The key is terminated with `file`, `line`, `category` and `suggestion`**, because the edge predicate permits `abs(line delta) <= 3`: two findings three lines apart can tie on blocking status, severity, confidence, source and description while differing in line, category and suggestion, and the order then falls back to **input position** — the exact property this key exists to remove. Measured — that pair yields **two** distinct survivor sets across input orders under the previous key and **one** under the terminated key. Same-file same-line fixtures cannot see this.

**(γ) `blocks_at_iteration_1` IS A COUNTERFACTUAL EVALUATED AT A HARDCODED ITERATION OF 1, independent of `LITMUS_ITERATION`.** Anchoring the first key on iteration 1 is deliberate — it keeps the surviving SET iteration-invariant, leaving the iteration-3 *status* to item 8 rather than letting the merger's own output change under it — but the row previously stated the anchor without stating its computation, and the obvious implementation defeats it. The only blocking predicate in the file reads the ambient environment (`determine_status` does `int(os.environ.get("LITMUS_ITERATION", "1"))` at `:64` and derives `blocking_severities` at `:67`), so an implementer reusing it while the process runs at iteration 3 gets the iteration-3 answer, the first key collapses to a constant across all candidates, and election falls through to severity and confidence. **Factor the predicate so the iteration is a PARAMETER rather than an environment read.** Measured 2026-09-08 on a `{medium, 0.9}` vs `{high, 0.5}` pair: the hardcoded counterfactual elects the medium at **both** iterations, while the ambient computation elects the medium at iteration 1 and the **high** at iteration 3 — the survivor set becomes iteration-dependent again, invisibly, and every acceptance fixture that runs at a single iteration still passes.

On this route `deterministic_source` (the fourth element) is **inert by construction** — no deterministic finding is ever a candidate — and it is retained only because the demoted alternative below still needs it. Deterministic-source preservation is the exclusion rule above, stated as a construction rather than as an invariant to be checked afterwards. Election is never by list position. **Add three acceptance requirements to the matrix, one per property.** *(α)* a chain fixture whose elected representative is an **endpoint**, asserting the below-threshold member **survives** and is not erased. *(β)* a fixture whose finding carries **no `confidence` field**, asserting the election **completes** (no exception) and the survivor set is identical in both input orders; plus a **nearby-line tie** (`abs(line delta) <= 3`, every named key equal, differing `category`/`suggestion`) asserted over both orders. *(γ)* the **identical fixture executed at `LITMUS_ITERATION=1` and at `=3`**, asserting a **byte-identical surviving set** — this is the check that catches an ambient-iteration implementation, and a matrix that runs at one iteration only cannot. **Add the codex fixture to the acceptance matrix: a `sast:` medium at `0.4` and an `llm:` medium at `0.99`, same file and line, asserted over all input orders at `LITMUS_ITERATION=1` and `=3`, on BOTH the survivor set and the status.** **Components alone were verified INSUFFICIENT, by executing this prescription rather than reasoning about it:** a reference implementation of exactly the clause above, run over all six permutations of the chain fixture with the merger's own directed predicate, still produced **two** distinct surviving sets; with the canonical-order predicate it produced **one**. That is why the symmetrization paragraph above is a requirement and not a note. The earlier primary ("compute authorization over ALL validated findings *before* deduplication") is **demoted to an alternative**, because `lib/merge-findings.py` computes both from one list (`deduped = deduplicate(...)` at `:185`, `status = determine_status(deduped)` at `:186`, `result = {status, issues: deduped}` at `:188`): computing status pre-dedup while still emitting the deduped list yields **FAIL verdicts whose `issues[]` contains no blocking finding** — precisely the #844 case, since the `medium/0.9` that justified the FAIL is the one dedup evicted. If that alternative is taken anyway, it must additionally require that **any finding contributing to a FAIL is retained in `issues[]` regardless of dedup**. Either way, add the invariant in **BOTH directions — corrected 2026-09-08 (round 8), because only the FAIL direction was stated and #844's harm is a false PASS, so the invariant was vacuous in exactly the case it was added for**: (i) for every **FAIL** verdict, at least one emitted finding satisfies the blocking predicate; and (ii) for every **PASS** verdict, **no finding removed by dedup may satisfy the blocking predicate at the evaluated iteration**. Direction (ii) is the one that catches a dedup-induced FAIL→PASS conversion, and it must be evaluated at the iteration the verdict is issued under, not at iteration 1 — the survivor SET is iteration-invariant by the key above, but the blocking predicate is not. **(B) completeness PROPAGATION and refusal — scoped down 2026-09-08, because "detection" as written was not startable and the status overstated readiness exactly as 7a-ii's did.** Two verified facts forced the rescope. (i) The extractor-silence laundering (B) was said to close is **already closed**: `parse-narrative.py` returns `None` on no-match (`:87-88`) and the CLI exits 2 (`:104-112`) into the loop's `infra_failure` path, whose own comment states that an unreadable review is not a passing one. (ii) The **residual** laundering is the well-formed `{status: PASS, issues: []}` produced by the **3-new drip cap** at `init-review-loop.sh:466`/`:556` — and detecting *that* requires the **producer** (prompt) change, which lives in item 2a's convergence-policy half, behind the baseline. So (B) is scoped to what is implementable without a producer change: **carry an aggregate `completeness` through the merger and the marker writers, and enforce the authorization-artifact invariant above.** **(B) is NOT a pure propagation change, and round 7 corrected the row that said it was: the field cannot be emitted at all today, so three named amendments are deliverables of (B), not assumptions of it.** Verified at the checkpoint: (i) `run-review-loop.sh:910-914` sets `ALLOWED_TOP = {"status", "model", "issues", "reviewed_diff_hash"}` and **rejects any other top-level key** with "unknown top-level field(s)" — the trusted writer refuses a verdict carrying `completeness`; (ii) `skills/litmus/schemas/pr-backstop-verdict.schema.json` declares `"additionalProperties": false` with `required: [status, model, diff_hash, ts, issues]`, so the on-disk artifact schema forbids it too; (iii) `write_codex_lead_verdict()` (`:735-746`) takes a single `$hash` argument and emits a hardcoded `printf '{"status":"PASS","model":"codex","diff_hash":"%s","ts":%s}'` — there is no parameter and no field in which completeness could travel. **Deliverables, named — corrected 2026-09-09 (round 9, finding [4]); the earlier three could not satisfy this stage's own acceptance.** `ALLOWED_TOP` at `:911` is only the INPUT allowlist for the stdin payload; the artifact actually written is built FRESH at **`run-review-loop.sh:976-983`** as `{"status","model","diff_hash","ts","issues"}` and serialized at `:983`, so a `completeness` key admitted past `ALLOWED_TOP` is **dropped before it reaches disk** — and once Stage 2 lands, the invariant would reject every normal backstop verdict. This is round 7's defect ("the field cannot be emitted by any existing writer") recurring at the next writer down. **FIVE deliverables:** amend `ALLOWED_TOP` at `:910-914` to admit `completeness`; **add the key to the artifact constructor at `:976-983`** — without this the other four are inert; add `completeness` to `pr-backstop-verdict.schema.json`'s `properties` with `additionalProperties` still `false`; give `write_codex_lead_verdict` a second parameter whose default is `unstated` without refusing (named once at Stage 1's PR-LEAD path); and **add `agents/pr-security-backstop.md` (`:51-80` still specifies fieldless verdicts) to the Stage 2 producer cutover**, since the producer side was unlisted too — and with it the backstop's paging adoption, placed once at item 2a's OVERFLOW row under BACKSTOP ADOPTION and cited here, not restated (2026-09-13, Photon assembly-closure decision; UNREVIEWED). **AND, in the same Stage 2 producer cutover (2026-09-24; plan-only, UNREVIEWED): the ONLY caller of `write_codex_lead_verdict` — `run-review-loop.sh:3568` at the checkpoint (`:3558` on this branch) — passes `completeness` EXPLICITLY**, as the value the executed codex lead review recorded. A normal, fully executed lead PASS is `complete`, not `unstated`, so the Stage 1 `unstated` default is never what a normal lead verdict carries and Stage 2 does not refuse every normal PR lead verdict. Fixture, in the same change: a normal lead PASS writes `completeness: complete` and is honoured at Stage 2, and a lead review whose recorded completeness is not `complete` writes that value and is refused. **Lock scope is the ONE global rule in *Execution order and acceptance*, cited and NOT restated here:** Stage 2 touches locked paths, Stage 1 does not (its deliverables live in `skills/litmus/scripts/` and the backstop schema, verified absent from the lock), so the `--update` regen belongs in the same reviewed diff as **Stage 2 only**. **`skills/litmus/schemas/review-output.schema.json` IS ALSO A MIGRATION SURFACE — ADDED 2026-09-09 (round-13 finding [17]; UNREVIEWED):** it was named nowhere in this plan, and it declares `"additionalProperties": false` over exactly `status` and `issues`, so a verdict carrying `completeness` is REJECTED by it. It takes the `completeness` property in the same change as the backstop schema — or, if the shipped schema is in fact unmaintained and validates nothing, this row says so explicitly and cites what proves it. **THE SECOND BRANCH IS TAKEN, WITH THE PROOF CITED — RESOLVED 2026-09-12 (native c94fb702 finding [13], agy issues[5] at reduced severity, conf 0.6; UNREVIEWED). `skills/litmus/schemas/review-output.schema.json` HAS NO RUNTIME CONSUMER AND THEREFORE REJECTS NOTHING.** The file's content is as described (`additionalProperties: false`, `required: ["status","issues"]`), but a repository-wide search across `skills/`, `scripts/`, `hooks/`, `tests/`, `__tests__/`, `.github/`, `agents/`, `commands/` and `package.json` returns exactly ONE non-documentation hit — a **comment** at `tests/test-litmus-severity-taxonomy.sh:2` — which never loads or compiles it; the repo's `ajv` devDependency is compiled only against the install/hook/state schemas. The only schema-shaped runtime check on this path is the **hand-written `validate_review_schema` at `lib/validation.sh:114-164`**, which never reads the JSON file, checks only presence of `file`/`severity`/`category`/`description` plus two enums, does NOT enforce `additionalProperties: false`, and ends `# Always return 0 — schema validation is advisory, not blocking`. The two even disagree on their own terms: the JSON Schema requires `suggestion` and forbids unknown keys; the validator requires neither. **So migrating and version-bumping the schema is a DOCUMENTATION/TOOLING change with NO GATE EFFECT, and must not be counted as enforcement.** `completeness` is added to the hand-written validator's accepted set so the two surfaces cannot drift further apart. Wiring the JSON Schema into `validation.sh` would be a **behavioural change with its own acceptance** — that validator is fail-OPEN by design, as this row already records — and is NOT adopted here. Silence was the one option foreclosed, since an enumeration that omits a fail-closed validator is how the envelope ships and is rejected at the first real verdict. **Acceptance must exercise a captured backstop response THROUGH serialization and both PR verifiers, asserting `completeness` survives to the on-disk artifact** — reading the allowlist is not evidence it is written. Until those land, (B) is not startable as its status once claimed.

**Legacy migration — the value the whole row turns on, stated rather than deferred.** An earlier draft required "a documented default for legacy inputs" and never gave one; both naive values break the founding argument (a blanket `complete` makes the refusal theatre over exactly the drip-capped PASS laundering this exists to catch, while failing closed on every missing field bricks every mint until the blocked producer change lands). **RESOLVED 2026-09-09 as a WORKER DEFAULT under *Still needs Chris* #12 — NO GRANDFATHER, with the circularity broken by ORDERING rather than by an exemption. **PROVENANCE**: the "DECIDED 2026-09-08" label was struck 2026-09-09 (round 9, finding [0]) because the approval header records that the phase which wrote it "resolved none of round 8's policy questions and edited none of them", so no approval carried it. This is a NEW disposition carrying Chris's 2026-09-09 delegation, NOT a restoration of that label; it is vetoable by row ID and authorizes no implementation. This supersedes the provenance-keyed disposition an earlier draft carried; that disposition is retired, not retained alongside this one.** The retired version read a missing `completeness` as `complete` for any artifact stamped with a build predating the field. Round 8 established that the class it grandfathered is **empty by construction** — stamping begins at the cutover build, so the first build that stamps anything is a *current* build and nothing can ever be stamped-and-older — while the same rule's "current build, missing field, fails closed" clause refused **every ordinary review** from the moment it landed, leaving no authorization artifact mintable and `baseline captured` unreachable. **The accepted arrangement is two stages, and no artifact is ever read as `complete` without carrying the field.**
**Stage 1 — startable now, and it changes NO authorization OUTCOME.** **Deliverables, each labelled with the path it serves — relabelled and EXTENDED 2026-09-08, because the first version named five deliverables that all sat on the two PR paths and left the commit path untouched: the very path that mints the drip-capped `{status: PASS, issues: []}` this row exists to catch.** **PR-BACKSTOP path:** the `ALLOWED_TOP` admission at `run-review-loop.sh:910-914`; the `pr-backstop-verdict.schema.json` property. **PRODUCER-BUILD STAMP REMOVED FROM THIS LIST 2026-09-10 (round-15 finding [8]; Photon technical decision, UNREVIEWED).** It was a deliverable with no field name, no type, no writer-derived source, no place in the artifact constructor at `:976-983` and no property in the schema — a named deliverable nothing could satisfy. It existed for a grandfathering story this row no longer tells: under the **no-grandfather** design the cutover refuses legacy shapes outright, so nothing needs to ask which build minted a verdict. **What is RETAINED and unchanged: the `completeness` property, its schema admission and its producer** — Stage 1 stays outcome-neutral and Stage 2 stays atomic; neither is re-scoped by this removal. **PR-LEAD path:** the `write_codex_lead_verdict` parameter and its default: a missing or empty second argument, and a missing key at the constructor (`:976-983`), write `completeness` `unstated` (item 2a's producer-did-not-state sentinel) WITHOUT refusing — outcome-neutral as this heading requires; `unstated` becomes non-authorizing only under Stage 2's rule. ACCEPTANCE: a one-argument call (the sole caller at `:3568`) still writes a PASS lead artifact that `verify_pr_artifact` accepts. **COMMIT path — four items, three of them new here, verified at the checkpoint.** **(c-1) `extract_review_json.py` is a PRESERVATION site and needs NO change.** `_resolve` (`:690-699`) returns the reviewer's decoded object verbatim and `:836` `json.dumps` it, so a reviewer-stated `completeness` already survives untouched. It is named rather than omitted because it is **blueprint-review's file, shared** — `run-review-loop.sh` resolves it at `:3412-3415` and invokes it at `:3423`, and the blueprint arbiter reads the same extractor. That sharing is the REASON the stamp below is placed in the litmus-owned merger and not here: a stamp in the shared extractor would add a key to the blueprint arbitration path, which neither this row nor item 2a scopes. **(c-1b) — STAGE 2, NOT STAGE 1. PLACEMENT CORRECTED 2026-09-09 (round 12).** It is listed here beside the other COMMIT-path producers for readability, but it is **not a Stage 1 deliverable and may not be started on Stage 1's authority**: the Stage 1 heading reads "startable now, and it changes NO authorization OUTCOME", while (c-1b)'s own three deliverables (A-i)/(A-ii)/(A-iii) retire the bare `BUILTIN-<64-hex>` bearer form and therefore change an authorization outcome by design. It rides the **Stage 2 producer cutover** with the rest of the envelope work, **except (A-iii)'s `dispatcher-commit-block.sh` TWO-ARM delta, which is scheduled ONCE, in `2a-env`'s pre-baseline (i-a) commit where the graph places it; the Stage 2 cutover schedules no change to that file (scheduled once 2026-09-24; plan-only, UNREVIEWED).** Reading it as Stage 1 would let the bearer-form retirement ship ahead of the readers that recognise the replacement — the fail-open this whole row exists to close. **The BUILT-IN review agent is a producer too, and was missing from this enumeration — ADDED 2026-09-09 (round 10, finding [7]).** Verified at the checkpoint: `skills/litmus/SKILL.md:478` instructs the built-in agent to output a **bare JSON array of issues** — no envelope, therefore nowhere for a reviewer-stated `completeness` to live; `:480` invokes the marker writer with the prompt path only; and `write-review-marker.sh:373` writes `BUILTIN-${HASH}` as the **entire** marker body. So this path can carry no completeness value at all today. **Its value is `unstated` UNTIL the (A) cutover below**, emitted the same way `parse-narrative.py` emits it in (c-2) — a producer that predates the field, not a producer that stated nothing suspicious. **After the cutover it states a real value**, and `unstated` survives only as the legacy-shape degradation. Stage 2's existing rule (missing or not `complete` fails closed) then covers it with no additional refusal rule, exactly as the `unstated` sentinel is defined in item 2a. **This matters for the word ATOMIC:** a cutover that changes the envelope while one live executed-review producer is unenumerated is not atomic, and the omission would have surfaced as a built-in-reviewed commit whose marker the new readers could not classify. An earlier version of this cell called a real envelope (a schema change at `SKILL.md:478` plus a marker-payload change) "a **larger scope** than this row carries" and left `unstated` as the permanent disposition. **THAT PREMISE IS WITHDRAWN AND SUPERSEDED — Chris approved OPTION (A) on 2026-09-09 (`批准840保留fallback方案`, *Still needs Chris* #14).** The scope call was made without the fact that decides it: the built-in path is a **LIVE commit-authorizing fallback today** — anchored by PREDICATE and REVISION-QUALIFIED per the provenance rule, RE-ANCHORED 2026-09-09 (round-13 finding [0]; UNREVIEWED), because this cell previously carried branch numbering while fixture 6 below cited the checkpoint for the SAME comparison: the arm whose `elif` matches `^BUILTIN-[a-f0-9]{64}$` is `727d4652:hooks/gate-scripts/pre-commit-gate.sh:943` (branch `9b4c6e3e:...:909`), the `BUILTIN_HASH=${MARKER_CONTENT#BUILTIN-}` extraction is `727d4652:...:947` (branch `:913`), and the `[ "$BUILTIN_HASH" = "$STAGED_HASH" ]` comparison is `727d4652:...:948` (branch `:914`) — the checkpoint value governs — so a permanent `unstated` was never a neutral disposition. It is a **silent retirement**, and one that re-running the built-in review cannot undo, because the same producer emits `unstated` every time. **The fallback is PRESERVED by giving it a real completeness envelope. It is NOT exempted from the invariant and NOT retired.** Three deliverables, which JOIN the Stage 2 producer cutover enumerated above and land atomically with it:
**(A-i) PRODUCER — the agent output contract** (`skills/litmus/SKILL.md`, Builtin Fallback step 3). The dispatched `code-reviewer` is today required to emit a **bare JSON ARRAY** of issues, which has nowhere for a producer statement to live — that ABSENCE, not a policy choice, is what forced `unstated`. It emits an ENVELOPE instead: `{"status", "issues", "completeness"}`, where `completeness` is exactly one of the five values declared in item 2a's enum. **A legacy bare array stays PARSEABLE and reads as `unstated`** — still accepted as INPUT, never as AUTHORIZATION — so an un-updated agent degrades fail-closed rather than into a bearer token.
**(A-ii) MARKER PAYLOAD** (`write-review-marker.sh:373`, which writes `BUILTIN-${HASH}` as the **entire** marker body). It becomes **`BUILTIN-<64-hex>-<completeness>`**, still one line. The form is unambiguous without a delimiter change: the hash is fixed-width hex and no enum token contains a hyphen.
**(A-i-b) THE TWO LINKS BETWEEN PRODUCER AND WRITER — ADDED 2026-09-09 (round 12).** Changing the agent's output shape and the marker payload while leaving the path between them untouched fails closed at both links, so the cutover would ship a fallback that can never mint. Verified verbatim: **`skills/litmus/SKILL.md:479` is step 4 — "If parsing fails or the output is not a JSON array of issues, treat as FAIL"** — which REFUSES the envelope shape (A-i) introduces; and **`:480` is step 5**, which invokes the writer with the prompt path and no completeness argument, so the writer has no value to stamp. **Both are deliverables of this cutover:** step 4's parser accepts an envelope (and still accepts a bare array, reading it as `unstated`), and step 5 carries the completeness value to the writer through an explicit argument. **On the builtin path, Step 4 and Step 5 do NOT mint when the envelope's `status` is `FAIL`, even if `issues` is empty: that envelope is a FAIL and goes to Step 6 and Step 7's `--discard`** (a deliverable of this cutover, stated 2026-09-24; plan-only, UNREVIEWED; `skills/litmus/SKILL.md` is not edited by this plan text). A cutover that changes (A-i) and (A-ii) without these two is not atomic.
**(A-iii) CONSUMERS — plural, and branch-qualified. CORRECTED 2026-09-09 (round 12): the enumeration named one consumer and there are two.**
  **1. `hooks/gate-scripts/pre-commit-gate.sh` (this branch and `main`)** — REVISION-QUALIFIED and PREDICATE-PAIRED 2026-09-09 (round-13 finding [0]; UNREVIEWED): the `^BUILTIN-[a-f0-9]{64}$` recognizer at `727d4652:...:943` (branch `9b4c6e3e:...:909`), the `${MARKER_CONTENT#BUILTIN-}` extraction at `727d4652:...:947` (branch `:913`), the staged-hash comparison at `727d4652:...:948` (branch `:914`). The pattern extends to `^BUILTIN-([a-f0-9]{64})-(complete|truncated|errored|cap_exhausted|unstated)$`. **The EXTRACTION (`727d4652:...:947`) is its own deliverable and is NOT unchanged:** `BUILTIN_HASH=${MARKER_CONTENT#BUILTIN-}` strips only the prefix, so a `BUILTIN-<hash>-complete` body yields `<hash>-complete` and the comparison (`727d4652:...:948`) fails for every well-formed new marker. Extraction must take the captured hash group, not a prefix strip. **Only the comparison SEMANTICS (`727d4652:...:948`) are unchanged** — an earlier draft said "the staged-hash comparison is UNCHANGED" and named no extraction deliverable at all, which would have shipped a fallback that refuses its own correct markers.
  **2. `hooks/gate-scripts/lib/validate-staged-litmus-marker.sh` (branch `fix/issue-622-merge-commit-gate` ONLY — this file does not exist on `main`)** — `:366` accepts `^BUILTIN-[a-f0-9]{64}$` and `:367` does `local builtin_hash=${marker_content#BUILTIN-}`, comparing it against the staged hash to authorize MERGE transactions. It carries the identical recognizer-plus-prefix-strip pair and needs the identical change. **This is the branch item 0 already tracks, so the two must land together or the merge path keeps honouring a form the commit path has retired.** **The order is: that branch merges FIRST**, so this consumer is in the same tree and the cutover covers both in one diff. The graph carries it as `2a-env (i-a)/(i-b) commit ──► "#622 merged"`, a gate on that ONE commit and not on the whole `2a-env` node (one order, stated and narrowed 2026-09-24; plan-only, UNREVIEWED).
  With both consumers changed, Stage 2's existing rule then covers it with **no additional refusal rule** — missing or not `complete` fails closed. **THERE IS A THIRD READER, AND IT IS A CONTINUING REJECTOR — ADDED 2026-09-12 (native c94fb702 finding [11], grok issues[6] `architecture`, conf 0.85; UNREVIEWED).** `scripts/dispatcher-commit-block.sh:946-948` rejects `SKIPPED-NONE*`, `DEGRADED*` and `BUILTIN-*` **by prefix** (`emit_bail "external review marker rejected … pr-grind requires real external-CLI review-PASS"`), and it reads the `litmus-passed.local` commit-marker artifact — which makes it a member of item 2a's honouring set under that cell's own rule, **"every site that READS OR CLASSIFIES MARKER CONTENT, by whatever channel."** Two complete-sets therefore disagreed, and left silent a `2a-env` worker applying the honouring-set rule literally would "fix" `:947` to honour the new envelope. **It must not be taught to accept `BUILTIN-<hash>-complete`: for this site, learning the new form means CONTINUING TO REFUSE IT.** **ITS DELTA IS TWO-ARM, DEFINED ONCE HERE AND CITED ELSEWHERE (2026-09-24; plan-only, UNREVIEWED):** (1) the bare-64-hex arm (`:1076-1084`) learns item 2a bound (i-a)'s executed-review form only, the same form Stage 2's writers mint (:365); this one delta lands in `2a-env`'s (i-a) commit and nothing looser is recognized. The Stage 2 cutover changes no dispatcher code: Stage 2's writers mint this same (i-a) executed-review form and no Stage-2 FORM token, so this arm's full-string match accepts a Stage 2 executed-review mint of the staged diff, and a pr-grind commit after Stage 2 is not refused for its marker's form (producer side aligned 2026-09-24; plan-only, UNREVIEWED); (2) the `BUILTIN-*` prefix reject (`:946-948`) stays, `BUILTIN-<64-hex>-complete` included. One fixture per arm: an item 2a bound (i-a) executed-review mint of the staged diff is ACCEPTED, and a `BUILTIN-<64-hex>-complete` marker is REJECTED. pr-grind's external-CLI requirement is **unchanged by this plan** — loosening it is a product decision this plan does not own and does not take. **The bare `BUILTIN-<64-hex>` form is RETIRED at the cutover and falls to the unrecognized arm**, for exactly the reason the epoch-only `PASS-EXCLUDED` form was retired: it is precisely the bearer token this change removes. **Migration consequence, stated rather than discovered:** a marker minted before the cutover stops being honoured after it, so a commit in flight re-runs `/litmus` once. That is one re-run, not a lost capability.
**(A-iv) SCANNER DISPOSITION ON THE BUILT-IN PATH — ADDED 2026-09-23 (native run `0994c559` HIGH, codex `completeness`, arbiter-confirmed; plan-only, UNREVIEWED).** The completeness envelope above binds the REVIEWER's outcome and the staged hash only; nothing in (A-i)–(A-iii) reads the SCANNERS. That left bound (i-c)'s scanner-failure refusal incomplete: its (2-ORD) reader is the merger/short-circuit reader, but the scanners run at `run-review-loop.sh:2889` (`run_sast_scan`) and the exit-3 `BUILTIN_FALLBACK` handoff (`:3260-3367`, exit at `:3377`) leaves the loop BEFORE `lib/merge-findings.py` runs — the same fact bound (2-A)'s THIRD GAP records for capture. So a failed scanner, no available external CLI and a clean built-in envelope minted `BUILTIN-<hash>-complete` with the failure read by nobody. **THE RULE: the exit-3 handoff is a (2-ORD) READER of (i-c)'s failure token. When any scanner in the run produced that token, the loop does NOT arm the handoff** — it writes neither `builtin-review-prompt-path.local` nor `builtin-review-marker-baseline.local` and does not emit `BUILTIN_FALLBACK`; it ends through its EXISTING non-authorizing `infra_failure` exit, whose diagnostic names the failed scanner. The writer already refuses a Step-5 call with no armed handoff, so no marker can be minted on this path — **no new mechanism, no new token form, no new exit code and no writer change.** (2-ORD) binds it exactly as it binds the merger reader: it lands in `2a-env` in the same change as, or before, the first producer of the token. A CONFIGURED SKIP and a VALID-EMPTY scan are not failures and arm the handoff as today. **A FAILURE TOKEN IS NOT THE WHOLE CLASS: A SUCCESSFUL SCAN'S BLOCKING FINDINGS ARE ALSO READ HERE. EXTENDED 2026-09-23 (native run `b344739e` HIGH, codex `bugs`, arbiter-confirmed; plan-only, UNREVIEWED).** On the merger path a `sast:`/`lint:` finding at `high` or `medium` FAILs deterministically: `_is_deterministic_blocker` (`lib/merge-findings.py:45-49`), invoked as the first branch of `determine_status` (`:70-71`), with no confidence and no iteration filter. On the built-in path the same findings reach the agent only as prompt text (`run-review-loop.sh:3190-3199`, "Include them in your output as-is"), and Builtin Fallback steps 3-5 decide from the agent's own output. So a clean scan run that returned a `sast:semgrep` high, followed by an agent that leaves that finding out, minted `BUILTIN-<hash>-complete`. That is the deterministic-blocker class item 2i(A)/#844 guarantees on the merger path, reopened one path over, and a prompt instruction is not enforcement. **THE SECOND RULE: before arming the handoff, the loop applies that SAME predicate — the one function, called, never a copy of its condition — to every scanner finding retained from this run.** The bash loop does not call `_is_deterministic_blocker` itself, and no bash copy of its condition is written: the bash step invokes the existing Python check, `_is_deterministic_blocker` in `skills/litmus/scripts/lib/merge-findings.py:45-49`, by running Python that loads that file by path (its hyphenated name rules out a plain import) and calls the function on each retained scanner finding. **OUTPUT CONTRACT (2026-09-24; plan-only, UNREVIEWED):** on success the helper always prints exactly one JSON object, `{"blockers": [...]}`, listing every retained scanner finding the predicate matched, and `{"blockers": []}` when none did. An empty `blockers` array arms the handoff as today (fixtures 8 and 10). A non-empty array is the review **FAIL** described below, and the run reports those findings (fixture 9). Only a non-zero exit, a raised exception, empty stdout, or stdout that is not that JSON object is `infra_failure`, and then the handoff is NOT armed. It is not the merger's `determine_status`, which also weighs non-deterministic findings (invocation stated 2026-09-24; plan-only, UNREVIEWED). ** When any finding satisfies it, the loop does NOT arm the handoff** and does not emit `BUILTIN_FALLBACK`. It ends the run as a review **FAIL** that reports those findings, the outcome the merger path reaches for the same input. It is not `infra_failure`, because the scan succeeded. As with the first rule, no marker can be minted, and there is **no new mechanism, no new predicate, no new exit code and no writer change.** Because the predicate is shared, a later change to which scanner severities block (item 8's gated policy half) reaches this path without a second edit. Scanner findings below the predicate (`low`) arm the handoff as today and stay in the prompt. This rule reads only successful scanner output, so (2-ORD) places no ordering on it: it is `2a-env` work with the first rule. Acceptance is fixtures 7 through 10 below. **THOSE FOUR FIXTURES ARE WRITTEN AGAINST TODAY'S BUILT-IN PATH, AND STAGE 2 RE-RUNS THE TWO POSITIVE CONTROLS. CORRECTED 2026-09-23 (native run `e74fa2bc` HIGH, codex, arbiter-confirmed at 0.9; plan-only, UNREVIEWED).** As first written, fixtures 8 and 10 were `2a-env` acceptance, which is pre-baseline, and they required minting fixture 1's `BUILTIN-<hash>-complete` marker. That marker's producer is (c-1b) at Stage 2, which is downstream of `baseline captured`, so the graph held a cycle through them. The fix leaves (c-1b) at Stage 2 and does not move any Stage 2 work onto Stage 1 authority. Instead, **in `2a-env` every one of fixtures 7-10 runs against the built-in path as it exists before the cutover**: the agent's bare JSON array, and the writer's current bare `BUILTIN-<64-hex>` marker. Nothing in them depends on (A-i)-(A-iii). Fixtures 7 and 9 assert that the handoff is never armed and that no marker of ANY form exists afterwards. That absence does not depend on the marker's form, so the two need no re-run. Fixtures 8 and 10 assert that the handoff IS armed and that today's bare marker is minted. **At Stage 2, the (c-1b) cutover re-runs fixtures 8 and 10 with a `complete` envelope and asserts the `BUILTIN-<hash>-complete` form. That re-run is Stage 2 acceptance and is not `2a-env`'s.** It adds no graph edge: each capability dependency a fixture names now sits at or upstream of the node whose acceptance holds it.
**FAIL-CLOSED INCOMPLETE HANDLING:** a parse failure or a non-enum value NEVER mints — step 4's existing "treat as FAIL" governs and the writer is never invoked, so there is no path on which an unparseable review yields a marker at all.
**ACCEPTANCE FIXTURES — enumerated here, count derived from the list:**
  1. Envelope stating `complete`, no blocking issues ⇒ marker `BUILTIN-<hash>-complete`, gate honours it, commit authorized. **This is the fixture that proves the fallback SURVIVES**; its absence is what would make this an accepted retirement wearing a specification's clothes.
  2. Legacy bare array ⇒ `unstated` ⇒ **NO marker is minted at all**, and the block message names the recovery. **CORRECTED 2026-09-09 (round 12):** this fixture previously asserted that a `BUILTIN-<hash>-unstated` marker is WRITTEN and then refused by Stage 2, which contradicts item 2a's invariant in this same plan — "no review-derived authorization artifact … may be minted or honoured unless every executed review carries `completeness == complete`", and, in the same row, "an incomplete result must prevent marker creation". Both cannot hold. **The invariant wins, because it is the stronger and earlier-decided rule:** an incomplete built-in review does not reach the writer, exactly as fixture 4 already requires for unparseable output. The refusal is at MINT time, not at honour time; Stage 2's honour-side rule remains as the second line of defence for any marker that predates the cutover.
  3. Envelope carrying a `completeness` outside the enum ⇒ fails closed, no marker.
  4. Truncated/unparseable agent output ⇒ FAIL at step 4, writer never invoked, **no marker written at all**.
  5. An old-form `BUILTIN-<64-hex>` marker present after the cutover ⇒ **NOT honoured** — pins the bearer-form retirement as deliberate rather than accidental.
  6. A `complete` marker whose hash does NOT equal the staged hash ⇒ still blocked — a regression over the existing `:948` binding, so the new suffix cannot weaken the old check.
  7. **(A-iv), end to end through the real loop:** one scanner produces (i-c)'s failure token, no external review CLI is available, and the built-in reviewer would return a clean review (today's bare array; a `complete` envelope after the Stage 2 cutover) ⇒ **no handoff is armed, no `BUILTIN_FALLBACK` is emitted, the loop exits `infra_failure` naming the scanner, and NO marker of any form exists afterwards** — including when Step 5 is invoked anyway.
  8. The positive control for 7: the same run with every scanner clean (and, separately, with a configured skip) ⇒ the handoff is armed and a clean review mints today's bare `BUILTIN-<64-hex>` marker in `2a-env`. Stage 2 re-runs it for fixture 1's `BUILTIN-<hash>-complete` (see (A-iv)). Without it, fixture 7 passes by never arming anything.
  9. **(A-iv) second rule, end to end through the real loop:** every scanner succeeds, one returns a `sast:` finding at `high`, no external review CLI is available, and the built-in reviewer would return a clean review that omits that finding (today's bare array; a `complete` envelope after the Stage 2 cutover) ⇒ **no handoff is armed, no `BUILTIN_FALLBACK` is emitted, the run ends as a FAIL reporting the finding, and NO marker of any form exists afterwards**, even if Step 5 is invoked anyway. Repeated with a `sast:` `medium` and with a `lint:` `high`, each with the same outcome.
  10. The positive control for 9: the same run where the scanners return only `low` findings ⇒ the handoff is armed, and a clean review mints today's bare `BUILTIN-<64-hex>` marker in `2a-env`. Stage 2 re-runs it for fixture 1's `BUILTIN-<hash>-complete` (see (A-iv)). Without it, fixture 9 passes by treating every scanner finding as a blocker.
**(c-2) `parse-narrative.py` EMITS the sentinel, unconditionally.** Invoked as the fallback at `:3441`; its return is exactly `{"status", "issues"}` (`:96-99`) and it reconstructs a verdict from prose, so there is no producer statement to carry through. It emits `completeness: unstated` on every return and **may never emit `complete`** — the honest reading of a narrative parse is that the producer did not state completeness, never that it stated it. **(c-3) `lib/merge-findings.py` needs BOTH of its sites, not the one previously named.** `_ingest` (`:91-96`) must stop discarding the key on the way in, **and** the result assembly at `:188` must emit the aggregate on the way out — it is a fresh `{"status", "issues"}` dict today, so an ingested value would still be dropped at the exit. **(c-4) The aggregate FOLD — SCOPED 2026-09-09 (round 9, finding [5]); as first written it refused every review containing scanner output.** **FOLD PARTICIPANTS are REVIEW-ENVELOPE inputs only** — parsed objects carrying `status`/`issues`. **Bare finding ARRAYS are NOT participants and never yield `unstated`.** Verified at the checkpoint: `run-review-loop.sh:3480` pipes THREE inputs — `printf '%s\n%s\n%s\n' "$SAST_FINDINGS" "$MARKDOWN_FINDINGS" "$JSON_OUTPUT"` — and `_ingest` (`lib/merge-findings.py:91-96`) accepts a bare list via `sink.extend(parsed)` as well as an envelope. `$SAST_FINDINGS` and `$MARKDOWN_FINDINGS` are bare arrays that can never carry a `completeness` key, so an unscoped fold makes EVERY merge containing scanner output `unstated`, which Stage 2's "missing or not `complete` fails closed" then turns into a refusal of every such review — `baseline captured` unreachable, and the exact failure the two-stage arrangement exists to remove, re-entered one level down. This row already excludes deterministic scanner findings from dedup; it must exclude them here for the same reason. **The fold over participants, stated as a TOTAL PRECEDENCE** — added 2026-09-09 (round 9, finding [12]), because "any `truncated`, `errored` or `cap_exhausted` dominates a `complete`" never ordered the three against each other, so a merge returning both `cap_exhausted` and `errored` had two admissible aggregates and emitted a **different recovery instruction per reviewer order**: item 2a makes `cap_exhausted` non-authorizing and requiring SCOPE REDUCTION, while `errored` is a retry condition. That re-introduced order-dependence at the diagnostic layer in the very row that removes it from the survivor set. **Precedence, highest first: `errored` > `cap_exhausted` > `truncated` > `unstated` > `complete`.** The aggregate is the highest-precedence value present; `complete` therefore requires EVERY participant to state `complete`, and any absent or `unstated` participant yields at least `unstated`. **Per-slot reasons are retained alongside the aggregate** so the recovery instruction is derivable from the slot, not only from the fold. **Fixture: a mixed `cap_exhausted` + `errored` merge asserted over both input orders, on both the aggregate AND the recovery instruction.** **A merge with NO participants is `unstated`, never `complete`.** **`_ingest` needs a SECOND per-input channel**: its signature is `(parsed, sink)` where `sink` is a flat findings list, so "stop discarding the key on the way in" has nowhere to put it — name the channel a per-input completeness list populated alongside the sink, envelope inputs only. **Fixture: SAST findings plus one `complete` LLM review must fold to `complete`, not `unstated`.** **Where stage 2 READS it is deliberately not a stage-1 deliverable:** the executed-review marker writer at `:3585` is the commit-path consumer, and the short-circuit mint at `:3115` is a no-review mint already governed by item 2a bound (i) rather than by this invariant. Naming a reader here would violate stage 1's own rule immediately below. **Stated as a rule, not an aspiration: while stage 1 stands alone, no reader consults `completeness`, no mint is refused because of it, and no verdict changes.** A stage-1-only tree must behave identically to today's, and the acceptance below requires that to be demonstrated rather than asserted.
**Stage 2 — the refusal rule, enabled ATOMICALLY with the FIRST AUTHORIZING READER of `completeness`, in the same commit.** (RE-KEYED 2026-09-13, native b5bddf50 HIGH; UNREVIEWED: this read "with item 2a's first complete producer", but bound (2-CAP) makes `2a-env` the first EMITTER, ahead of `baseline captured`, and that emitter refuses nothing.) From that commit a REVIEW-DERIVED artifact whose `completeness` is missing or not `complete` **fails closed, with no exemption class among review-derived artifacts other than item 2a bound (i-a)'s executed-review form, which carries no `completeness` field: it is not refused at its reader for lacking the field, because it is gated at its MINT (`:3585`), and any (i-a) marker present when Stage 2 activates is retired (below); the `PASS-FAST` marker, review-derived and not that form, is refused at its reader by FORM (the parent's #12; 2026-09-24; UNREVIEWED) (narrowed 2026-09-24; plan-only, UNREVIEWED)** (SCOPED 2026-09-23 (native run `e90373b7` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED)) — and there is nothing to grandfather: every artifact minted before that commit, including one that already carries a `complete` value no reader checked, is re-reviewed, never honoured; a bare (i-a) executed-review mint is included, by retirement at activation (below). **HOW STAGE 2 TELLS A PRE-STAGE-2 `complete` APART — RESOLVED 2026-09-13 (native 2a9a6900 finding [2]; Photon remaining-contract decision; UNREVIEWED), by REUSING item 2a bound (i-b)'s format-cutover provenance, not by a build stamp (that stamp was removed from Stage 1 on 2026-09-10):** the Stage 2 commit's writers mint the EXECUTED-REVIEW authorization artifact in the honouring set in item 2a bound (i-a)'s executed-review form, the one full-string form the unchanged `dispatcher-commit-block.sh` accepts, and mint NO Stage-2 FORM token, so (i-a)'s short-circuit versus executed-review distinction is kept as it is (producer side aligned 2026-09-24; plan-only, UNREVIEWED) — and the readers this cutover changes honour two review-derived forms: that (i-a) executed-review form, which no reader this plan changes refuses; and the built-in review mint in its (A-ii) form with the value `complete`, `BUILTIN-<64-hex>-complete`, which Stage 2's existing rule already covers with no additional refusal rule (:350) (the (i-a) form added as honoured 2026-09-24; plan-only, UNREVIEWED). **That (i-a) executed-review form has NO `completeness` field, and no gate-side reader is required to read `completeness` off that marker: completeness stays on the envelope and the (2-CAP) path, not on the form `dispatcher-commit-block.sh` matches, and no field the dispatcher would refuse is added (2026-09-24; plan-only, UNREVIEWED).** `dispatcher-commit-block.sh` is not changed by this cutover: its one change, the TWO-ARM delta defined at (A-iii) (:350), is scheduled in `2a-env`'s (i-a) commit. The short-circuit mint at `:3115` is NOT review-derived: it is a no-review mint, as (c-4), (B-stage-2) and the Content Lock bullet already classify it, and it is governed by bound (i) (forms and fixtures aligned 2026-09-24; plan-only, UNREVIEWED). So an artifact minted before the commit in a review-derived form OTHER than the (i-a) executed-review form is refused by form and re-reviewed, whatever `completeness` it carries; nothing reads a date, a build or a stamp. A bare (i-a) executed-review mint cannot be told apart by form, because Stage 2's writers mint the same form, so it is handled by INSTANCE, not by form: **the Stage 2 activation step retires every `litmus-passed.local` (except one whose content is, in full, a `SKIPPED-NONE` or `PASS-EXCLUDED` no-review form, which stays and is honoured as below (2026-09-26; plan-only, UNREVIEWED)), `pr-review-passed.local` (its `PASS-FAST` form included), `pr-codex-lead.local.json` and `pr-backstop-verdict.local.json` present when Stage 2 activates, in every worktree whose state directory the Stage 2 build reads, through the existing audited drain (`scripts/design-clear.sh --skip <name>`, whose drainable list already names all four)**, and each such diff needs a fresh review (2026-09-24; plan-only, UNREVIEWED). The drain runs only after the Stage 2 build is installed AND no review process started before that install is still running in those worktrees; a live pre-install review blocks activation until it exits, and any marker it wrote is drained with the rest (2026-09-26; plan-only, UNREVIEWED). After activation every honoured (i-a) marker was minted by the completeness-gated `:3585` writer, and no `PASS-FAST` marker is honoured, since Stage 2 refuses that form at its reader (the parent's #12). No reader refuses the form, so the unchanged dispatcher stays compatible (2026-09-24; plan-only, UNREVIEWED). ACCEPTANCE: a fresh Stage-2 mint of the staged diff, which is in the (i-a) executed-review form, is ACCEPTED by every reader this cutover changes and by the unchanged `dispatcher-commit-block.sh`, and a pre-Stage-2 bare (i-a) executed-review mint of the SAME diff is RETIRED at activation and NOT honoured afterwards; that diff needs a fresh executed review. Likewise a pre-Stage-2 `PASS-FAST` marker for the same diff, still within `MAX_AGE`, is RETIRED at activation and NOT honoured afterwards (2026-09-24; plan-only, UNREVIEWED). **NEGATIVE FIXTURE (2026-09-24; plan-only, UNREVIEWED): an executed review whose `completeness` is not `complete` mints NO executed-review marker, and a Codex lead review whose `completeness` is not `complete`, run with `LITMUS_PR_FAST=1`, mints NO `PASS-FAST` marker** — the `:3585` and `:3563` writers' refusal, one rule for both forms, stated here as acceptance; this plan text does not edit either writer script. No fixture may delete an artifact to pass. **THE NO-REVIEW MINTS ARE OUTSIDE THIS REFUSAL AND THIS FORM — SCOPED 2026-09-23 (native run `e90373b7` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED).** The refusal and the FORM requirement bind review-derived artifacts only. The short-circuit mint at `:3115` keeps its bound (i-a) short-circuit form and, under bound (i-b), never authorizes a commit on its own. The two configured no-review writers carry their own no-review forms, unchanged, with no `completeness` field: `SKIPPED-NONE` carries exactly `SKIPPED-NONE-<epoch>` (`run-review-loop.sh:1852`), and `PASS-EXCLUDED` carries exactly `PASS-EXCLUDED-<reviewed-diff-hash>-<epoch>` (`:2735`), each matched in full by its reader and governed by bound (i)'s separate no-review rule. **FIXTURE OBLIGATION OF STAGE 2, plan text only:** at Stage 2 activation, (a) a `SKIPPED-NONE-<epoch>` marker is honoured exactly as before Stage 2; (b) a `PASS-EXCLUDED-<hash>-<epoch>` marker bound to the staged diff is honoured exactly as before Stage 2; and (c) a marker that carries either no-review prefix with any other content — a `completeness` field or a trailing line appended — is REFUSED, so the scoping opens no mixed form that the refusal cannot see. The acceptance pair above runs in the same change. **THE FIRST AUTHORIZING READER, defined once here:** a gate-side reader whose honouring of an authorization artifact depends on that artifact's RECORDED `completeness`. The `2a-env` continuation driver is NOT one — its in-run chain control only withholds authorization from an unfinished run and never grants on a value (item 2a bound (2-CAP), (2-ORD)). **The cost is accepted and stated here rather than discovered later: (B) delivers no detection value until 2a lands**, and truncation DETECTION remains unavailable for exactly that interval.
**Why this is cheap, verified rather than assumed.** The authorization markers are **diff-bound and consumed**, not durable: `pre-commit-gate.sh` matches the marker against the staged diff hash, and `post-commit-consume-marker.sh:154` unlinks it on a confirmed commit. So at the stage-2 cutover the population of already-minted, fieldless artifacts is stale markers that fail closed and are re-reviewed — which is why **no migration exemption needs to exist for them.** **Truncation DETECTION is explicitly unavailable until the prompt half lands** — stated here rather than implied, so no one schedules a detector that cannot exist yet.

**Class:** hard

**Status 2026-09-08:** **SPLIT** — **(A) #844: UPSTREAM-CLOSED, OBLIGATION OPEN — STATUS DEFINED ONCE HERE, every other surface CITES this cell and none restates it (CORRECTED 2026-09-16 root-A; source-bound, queried 2026-09-16; UNREVIEWED)**, the only integrity work with no predecessor. **The facts, date- and commit-qualified:** GitHub #844 is **CLOSED/COMPLETED `2026-09-15T21:07:11Z`**, closed by **PR #856** (`d4d3afece07ef2d40134138c607f4431a755561a`, merged to `main` `2026-09-15T21:07:10Z`); the sibling **PR #855** merged to `main` `2026-09-14T17:22:16Z` (`3bb16144679f61ab9090ab00751f31739891fbc9`). **NEITHER MERGE COMMIT IS CONTAINED BY THIS BRANCH** at HEAD `34887cb7` — verified by `git branch --contains` on 2026-09-16, which lists `main` and `fix/issue-838-worktree-diagnostic` and **not** `docs/0827-plan-configured-routing-20260906`. **A LANDED CHILD DOES NOT SATISFY ITS PARENT NODE:** #856's diff implements cross-class retention ONLY, so item 2i(A) is **neither unstarted nor satisfied**, this node's acceptance is unchanged and still owed, and no milestone is cleared by the closure. The item therefore remains schedulable here **on the stated ground that the change is absent from this branch and the parent obligation is wider than the landed child** — never on the ground that the issue is still open, which it is not. **(B): TODO for STAGE 1 only** (stamp, schema admission, `_ingest` carry-through — and it changes no authorization outcome); **STAGE 2 (the refusal rule) and detection — QUALIFIED 2026-09-16 post-16 (native run `5e995822` MEDIUM; Photon manual-resume brief `manual-802-838-840-847-20260916.md` #840 section, on the `840-post16-readonly-diagnosis-result.md` roots; source-read at HEAD `34887cb7`, NOTHING EXECUTED; UNREVIEWED): "detection" here means STAGE 2, THE REFUSING READER, and NOT the first emitter, which the 2026-09-13 re-key moved PRE-baseline; read unqualified this sentence recreates the N ──► detection ──► baseline cycle that re-key removed. Pre-baseline statement and capture are bound (2-CAP)'s, cited. — are BLOCKED on `baseline captured` and land atomically with the FIRST AUTHORIZING READER of `completeness` (re-keyed 2026-09-13; this cell propagated the same day, native 2a9a6900 finding [1]; UNREVIEWED — it previously said "on item 2a's producer change").** Neither member may be started on the other's authority, and stage 2 may not be started on stage 1's authority. Live gates, in one line: (A) startable; (B) Stage 1 startable; (B) Stage 2 BLOCKED on `baseline captured` (sentence completed 2026-09-24; plan-only, UNREVIEWED).

**Evidence / settling check:** **(A)** Regression matrix, **both input orders**: SAST-vs-LLM; LLM `{high, 0.6}` vs LLM `{medium, 0.9}`; equal-severity differing-confidence; plus the negative control at similarity < 0.6. **And TWO distinct three-finding fixtures, each asserted over all SIX permutations — corrected 2026-09-08 after the first version of this clause was EXECUTED and found non-discriminating.** They test different properties and must not be conflated.

**(A-i) BLOCKING-POWER fixture — ONE edge.** `A~B > 0.6`, `A~C < 0.6`, `B~C < 0.6`, same file and line, with A `{high, 0.6}` and B `{medium, 0.9}`. This is what an earlier draft of this row mislabelled as the non-transitive case. Measured at `A~B=0.965, B~C=0.143, A~C=0.127`, it yields **one** surviving set across all six permutations, so it cannot detect order-dependence at all — recorded as a finding rather than quietly swapped out. It is RETAINED because it does discriminate on the other half of #844: at `LITMUS_ITERATION=1` HEAD elects the non-blocking `{high, 0.6}`, evicts the blocking `{medium, 0.9}` and returns **PASS**, while blocking-power election retains the medium and returns **FAIL**. Assert that status difference here; do **not** assert order-variance here.

**(A-ii) TRUE NON-TRANSITIVE CHAIN — TWO edges.** `A~B > 0.6` **AND** `B~C > 0.6`, with `A~C < 0.6`. **The round-6 reproduction's numbers are this shape with the hub at A, not at B**: `A~B = 0.921` and `A~C = 0.653` are both above threshold while `B~C = 0.587` is below. An earlier draft printed those three numbers beside the inequalities `A~B > 0.6, B~C < 0.6, A~C < 0.6`, which contradict them and describe a single edge — the labels are corrected here, and the numbers are the ones that were always right. Measured on a chain at `A~B=0.608, B~C=0.800, A~C=0.429`: HEAD yields **three** distinct surviving sets across the six permutations and **flips its decision** — `FAIL` on one input order, `PASS` on the other five, at iteration 1.

**Both fixtures assert SURVIVOR-SET *and* DECISION-STATUS invariance across all six permutations.** The surviving set alone is insufficient: the status is the authorization-bearing output, and it was measured flipping. A pairs-in-both-orders matrix never presents three mutually-overlapping findings and so cannot see any of this. **Assert ORDER-INDEPENDENCE and finding-survival, NOT a fixed status at every iteration** — an earlier draft required this matrix to assert FAIL at iterations 1 *and* 3, which was wrong and would have decided a question this plan reserves for Chris. Reproduced at the checkpoint: at `LITMUS_ITERATION=3` the `{high, 0.6}` vs `{medium, 0.9}` pair returns PASS in **both** orders, and would still do so after this repair, because from iteration 3 the blocking set is `{high}` (`:67`) and a medium is advisory by design. Requiring FAIL there implements **item 8's** medium-blocking decision ahead of its approval. So: at iteration 1, assert **FAIL and that the blocking finding is present in `issues[]`**; at iteration 3, assert only that the outcome is **the same in both input orders** and that the surviving finding set is identical — the iteration-3 *status* is item 8's to change, not 2i's. Pin at the merger's **stdin/argv interface**, not at `determine_status`. **(B), split by stage to match the accepted two-stage arrangement. (B-stage-1)** `completeness` is *carried* end to end — emitted, admitted by `ALLOWED_TOP`, surviving `_ingest`, present in the PR pair — **and an end-to-end cutover test proves an ordinary review still authorizes on the day stage 1 merges**: same fixtures, same verdicts, same mints as the pre-stage-1 tree, with **no** refusal attributable to the new field. A stage-1 change that alters any authorization outcome fails this criterion. **(B-stage-2)** `completeness` reaches every **review-derived honouring reader** — the set item 2a bound (i) defines once, which by that definition EXCLUDES the three no-review mint sites (`run-review-loop.sh:1852`, `:2649`/`:2745`, `:3115`); they owe the envelope change under bound (i-a), not a completeness fail-closed —
**SHORT-CIRCUIT REACHABILITY — the bound stated at round 10 (finding [6]) is now LIFTED, because its stated precondition was met: #11 was REOPENED and answered (A) by Chris 2026-09-09.** The round-10 wording said this criterion could not make Stage 2 refuse a short-circuited run, because at the checkpoint `:3115` and `:3585` write byte-identical markers so no reader can tell them apart — and it deferred the stronger claim, in terms, until #11 was reopened. **That has happened, so the stronger claim is now made rather than deferred:** Stage 2 asserts that `completeness` reaches every review-derived honouring reader that reads it — which excludes item 2a bound (i-a)'s executed-review form, which carries no `completeness` field, while the `PASS-FAST` reader refuses that form outright (the parent's #12) (scoped 2026-09-24; plan-only, UNREVIEWED) — **and** that **Stage-2 authorization is NOT reachable from a run that performed no executed review** — the short-circuit mint is distinguishable under bound (i-a) and non-authorizing on its own under bound (i-b). **SCOPED 2026-09-09 (round 11): this assertion is over the COMPLETENESS-BEARING, review-derived set only.** Bound (i-b) expressly exempts the configured no-review paths from the completeness invariant — they are MORE constrained, not less, in what they authorize — and `SKIPPED-NONE` and `PASS-EXCLUDED` are live marker writers (`run-review-loop.sh:1852` and `:2735`) that Chris #11 did not decide. Read as universal, the sentence retires two paths no decision here touches; read as scoped, it says exactly what #11 answered. **The scoped reading governs.** **This is a strengthening, and it is honest only because the discriminator is now an in-scope deliverable of the same cell**; it would have been an unchecked assertion under the previous answer, which is exactly why round 10 refused to write it. **Sequencing, so the criterion is never satisfiable early:** the assertion is testable only once (i-a)'s producer-and-readers commit has landed, so Stage 2's settling check includes a fixture in which a **short-circuit mint alone fails to authorize a commit** while an executed-review mint of the same diff succeeds. **The residual recorded at bound (i-b) is closed by this answer inside the plan — it is not carried to a successor owner**, an alternative that was considered during diagnosis and NOT approved. and each fails closed on a missing or dropped field, with the two honest exceptions recorded there (the merger must first *carry* a field before it can check one; `validation.sh` is fail-open by design, so closing it is a behavioural change with its own acceptance) — **and the refusal rule is demonstrated to land in the same commit as the FIRST AUTHORIZING READER of the field (bound (2-CAP)'s pre-baseline emitter refuses nothing; re-keyed 2026-09-13), with a fixture proving a review minted by the immediately preceding commit — including one already carrying `complete` — is re-reviewed rather than grandfathered, a bare item 2a bound (i-a) executed-review mint included, which is retired at Stage 2 activation rather than refused by form (item 2i(B) Stage 2; 2026-09-24; plan-only, UNREVIEWED).** **The authorization-artifact invariant covers the PR pair, not only `litmus-passed.local`. The review-derived `PASS-FAST` marker is refused by FORM:** Stage 2 refuses it at `pre-pr-gate.sh:449-468` because it is not the (i-a) form (the parent's #12), not for the `completeness` field `PASS-FAST-<hash>-<epoch>` lacks (2026-09-24; UNREVIEWED); its mint is gated as well (`:3563`, stated at the invariant below), while a `PASS-EXCLUDED` marker (no review ran, `:2649`) keeps its bound (i) treatment unchanged (added 2026-09-13, native b5bddf50 HIGH; UNREVIEWED). **Neither member may be declared complete by writing a document; both require a passing regression.**

---

## Moved from the parent 2026-09-24: § *Item 8*

> **MOVED VERBATIM (2026-09-24; Chris-approved shrink; plan-only, UNREVIEWED).** The text below was the parent's § *Item 8*, at parent spec `8bfd530e…`; the parent keeps a one-line pointer to this file at its place. It binds as part of this file and is reviewed with this file's spec hash; a verdict on the parent does not cover it. Inside it, "this file", "this document", "above", "below" and line anchors `:NNN` into the parent refer to the parent as it stood before this move.

Litmus commit mode: keep `medium` blocking. Stratified sample of 20 medium-only FAILs with predeclared thresholds, dispositioned against the item-2 ledger, **AND a separately adjudicated stratum of iteration-3-and-later medium-only LLM PASSes — ACCEPTED 2026-09-09 (Chris, round-13 finding [13]); UNREVIEWED.** A FAIL-only frame excludes the outcomes the decision would change: `blocking_severities` is `{high, medium}` for iterations 1–2 and `{high}` from iteration 3 (`lib/merge-findings.py:64-67`, byte-identical at HEAD and the checkpoint), so a valid medium-only LLM review FAILs early and PASSes from iteration 3 — sampling FAILs alone therefore draws almost entirely from iterations 1–2 plus deterministic scanner findings, and omits precisely the advisory-treated cases whose authorization behaviour "keep `medium` blocking" would actually alter. **How each stratum bears on the decision is PREDECLARED**, alongside the thresholds, not chosen once the counts are visible. Within both strata two separations are required: **deterministic (`sast:`/`lint:`) findings are counted apart from LLM findings** — they block at every iteration via `_is_deterministic_blocker` (`:45-49`, invoked at `:70-71`) and so cannot discriminate this policy — and **repeated observations of ONE defect are counted apart from distinct defects**, so frequency is not mistaken for impact. Then decide. CodeScene: one final advisory state in the ack ledger, drop per-event acks — re-opens ADR 0012 (`:37`) explicitly; ONE umbrella CodeScene-debt issue absorbing #759 #733 #725 #716 #700 #637 #590 #564 (no blanket close). **[#840 adopted]** Pin the **whole** `determine_status` contract in tests first, not just the iteration rule: SAST/lint high+medium always block — cite this precisely as **`lib/merge-findings.py:45-49` (`_is_deterministic_blocker`), invoked as the first branch of `determine_status` at `:70-71`**. **That "always" holds on the built-in exit-3 path only through item 2i(B) (A-iv)'s second rule, which calls the same predicate before arming the handoff; this row's merger-side tests do not cover that path, and (A-iv)'s fixtures 9 and 10 do** (added 2026-09-23, native `b344739e` HIGH; UNREVIEWED). (`:49` is the helper's `return`, not a branch inside `determine_status`, which begins at `:52`; a test author following the old anchor looks in the wrong function.) **Pin it at the merger's stdin/argv interface, not at `determine_status`** — the helper is correct in isolation and is defeated upstream at `:185`. Beyond that: an LLM finding blocks only when its severity is in the iteration set **and** its normalized confidence is ≥ 0.7; the iteration set is `{high, medium}` for iterations 1–2 and `{high}` from iteration 3 (`LITMUS_ITERATION`, `:64`, `:67`). **Correction — the "missing or non-numeric confidence blocks unconditionally" clause in an earlier draft was false, and implementing it would have re-blocked iteration-3 mediums through the missing-confidence path (exactly the item-8 / item-2a collision this row exists to avoid).** The code's actual predicate: `:70-71` returns FAIL for deterministic blockers; `:72-73` is `if f.get("severity","") not in blocking_severities: continue`; **only then**, at `:75-82`, is confidence inspected. So a missing or non-numeric confidence FAILs **only for findings whose severity already survived the iteration filter**. Pin both directions: a **high** LLM finding with `confidence: null` FAILs at every iteration; a **medium** LLM finding with `confidence: null` remains **PASS at iteration 3**.

**EXPLICIT FAIL-CLOSED VALIDATION OF THE AUTHORIZATION-BEARING FIELDS — CHRIS-APPROVED 2026-09-09 (`全部批准`, round-13 finding [5]); PLAN TEXT ONLY, UNREVIEWED, and BEHAVIOUR-CHANGING. RELOCATED TO `2a-env`, PRE-BASELINE, 2026-09-23 (native run `d036cd94` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED): it no longer lands behind item 8's gate, it lands with `2a-env`'s other pre-baseline behaviour changes and not earlier, and it does NOT move into any outcome-neutral surface.** Measured against the checkpoint before writing (`determine_status`, `:52-88`, byte-identical at HEAD): the guard at `:72-73` is `if f.get("severity","") not in blocking_severities: continue`, so a finding the gate cannot classify is **skipped rather than blocked**. Four **RECORD-LEVEL** fail-opens are reachable today, each verified by execution at `LITMUS_ITERATION=1`: a finding with **severity ABSENT** returns PASS; **`severity: null`** returns PASS; an **unrecognized** severity returns PASS — including the mere miscasing **`"HIGH"`**, which no schema forbids; and a `high` finding with a **non-finite `confidence` of `NaN`** returns PASS, because `nan >= 0.7` is False and `json.loads` accepts the bare `NaN` literal. **`-Infinity` is the same fail-open and is named here beside `NaN` (added 2026-09-23 (native run `98787112`, arbiter-reproduced; Chris-approved plan-text fix; UNREVIEWED)):** a `high` finding with `confidence: -Infinity`, a bare literal `json.loads` also accepts, returns PASS because `-inf >= 0.7` is False. **The check is `math.isfinite` on the coerced value**, which rejects `NaN`, `-Infinity` and `+Infinity` alike. `+Infinity` happens to block today, but it is rejected too, because a non-finite confidence is not a classifiable value. Fixtures N1 and N2 in item 8's contract file pin the two infinities. **The deliverable:** each of those four is a **validation failure that BLOCKS**, not a finding to skip — the gate must never authorize over input it could not classify. **Its acceptance is `2a-env`'s, PRE-BASELINE (2026-09-23 (native run `d036cd94` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED)):** fixtures R1–R6 and N1–N2, stated in item 8's contract file for the merger interface, each merge to FAIL: absent, `null` and unrecognized severity (miscased `"HIGH"` included), `NaN`, `-Infinity` and `+Infinity` confidence, and the `bool` and negative confidence folded into R1–R6. A reproduced fail-open may not stay open while `baseline captured` is being collected, and a gate that authorizes over a record it could not classify would be measured as the baseline.

**RELOCATED TO `2a-env`, PRE-BASELINE — 2026-09-23 (native run `6e0afdc0` HIGH, arbiter-confirmed; Photon correction decision; plan-only, UNREVIEWED).** This input-level rule no longer lands behind item 8's gate: a reproduced fail-open may not stay open while `baseline captured` is being collected. Its home is the graph's `2a-env` SCOPE line. The record-level severity and confidence validation followed it there 2026-09-23 (native run `d036cd94` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED), and container and member shape validation (S1–S8, its synthetic rejection diagnostic and the S5/S6 valid-empty controls) followed 2026-09-23 (native run `e90373b7` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED); item 8 keeps X1's retention and indexing checks. Its `2a-env` acceptance fixture is **P1**: one unparseable input beside one clean review, in both input orders, on both stdin and argv, merges to FAIL, and the all-clean run still authorizes. **A FIFTH FAIL-OPEN SITS AT THE INPUT LEVEL, NOT THE RECORD LEVEL — ADDED 2026-09-10 (round-16 finding [2]; Photon technical decision, UNREVIEWED). The "four" above enumerates RECORD-level fail-opens and is NOT an exhaustiveness claim about the merger; read as one it was false.** Verified by execution at HEAD: `printf '%s\n%s\n' '{bad json' '{"status":"PASS","issues":[]}' | python3 skills/litmus/scripts/lib/merge-findings.py` returns `{"status": "PASS", "issues": []}`. The malformed input increments `parse_errors` and is then skipped by the `continue` at `:160`, and the fail-closed branch fires **only** when `parse_errors == total_inputs` — so one unparseable input beside one clean review still authorizes. **Neither approved check can observe it:** this row's validation is explicitly record-level (it runs over ingested RECORDS), and item 2i(B)(c-4)'s fold is explicitly scoped to **parsed review envelopes**. An input that never became a record, and never became an envelope, is invisible to both. **The deliverable:** a parse failure is **independently NON-AUTHORIZING at the ingestion boundary** — any input that fails to parse is a blocking validation failure in its own right, never a counter weighed against a total — while successfully parsed findings are still retained and reported for diagnosis. **SCANNER-ARRAY SEMANTICS ARE UNCHANGED by this correction:** a bare scanner ARRAY stays outside the (c-4) review-completeness fold, because it is not a review envelope and must not be made to carry one; what changes is only that a scanner input which fails to PARSE stops authorizing.

**A FURTHER INPUT-LEVEL FAIL-OPEN: AN ENVELOPE THAT STATES FAIL MERGES TO PASS — ADDED 2026-09-23 (native run `98787112`, arbiter-reproduced; Chris-approved plan-text fix; UNREVIEWED), codex HIGH.** Reproduced at HEAD: `python3 skills/litmus/scripts/lib/merge-findings.py '{"status":"FAIL","issues":[]}'` prints `{"status": "PASS", "issues": []}`. `_ingest` keeps only `issues`, `main()` recomputes status from the findings (`merge-findings.py:185-189`), and `run-review-loop.sh:3473` then overwrites `REVIEW_STATUS` with the merged status, so a reviewer FAIL whose reasons stayed in prose becomes an authorizing PASS. **The rule:** **A merged verdict is never PASS when an ingested review envelope stated `status: "FAIL"` and carried NO finding (the empty `issues` list).** Such an envelope is a BLOCKING INGESTION INCONSISTENCY; a FAIL envelope that carries findings is decided by `determine_status` over them, so the iteration relaxation is unchanged and a valid medium-only FAIL envelope still merges to PASS at iteration 3 (2026-09-26; plan-only, UNREVIEWED): the merger emits the existing `internal:merge-findings` HIGH synthetic diagnostic naming that input, so the merged status is FAIL. This uses the same shape and channel as the other ingestion rejections, so there is no new mechanism. It does not weaken the reverse direction: a PASS envelope still merges to FAIL whenever a finding blocks. Scanner arrays carry no `status` and are unaffected. The synthetic finding is itself blocking, so item 2i(A)'s invariant that every FAIL verdict emits at least one blocking finding still holds. It is behaviour-changing, and it lands in `2a-env`, PRE-BASELINE, not behind item 8's gate (relocated 2026-09-23 (native run `6e0afdc0` HIGH, arbiter-confirmed; Photon correction decision; plan-only, UNREVIEWED)). A reproduced fail-open may not stay open while `baseline captured` is being collected. Its fixture E1 is stated in item 8's contract file for the merger interface, but it is `2a-env`'s acceptance, not item 8's.

**A PARSE FAILURE IS NOT THE WHOLE CLASS — SHAPE VALIDATION ADDED 2026-09-10 (round-17 finding [1]; Photon technical decision, UNREVIEWED). RELOCATED TO `2a-env`, PRE-BASELINE, 2026-09-23 (native run `e90373b7` HIGH, arbiter-confirmed; plan-text fix; plan-only, UNREVIEWED), with the rejection-finding rule below and fixtures S1–S8; it does not land behind item 8's gate.** Measured at HEAD against `_ingest` (`lib/merge-findings.py:91-96`), which extends the accumulator only for a LIST or for a DICT carrying `issues`: `[]`, `{}`, JSON `""` and a valid empty envelope `{"status":"PASS","issues":[]}` **all yield ZERO records**, so the accumulator's COUNT cannot separate a valid empty input from an invalid one — **only its SHAPE can**. **Exactly two shapes are accepted at ingestion:** a findings **LIST** (empty permitted), or a **REVIEW ENVELOPE** (empty `issues` permitted). **ONE ENVELOPE PREDICATE, DEFINED HERE AND CITED EVERYWHERE — CORRECTED 2026-09-12 (native c94fb702 finding [2], agy issues[0] `bugs`, conf 0.85; UNREVIEWED). THE PREDICATE: a dict input is a REVIEW ENVELOPE iff it carries a RECOGNIZED `status` AND a list-shaped `issues`.** **THE RECOGNIZED `status` SET, ENUMERATED ONCE HERE AND CITED BY S7/S8 AND 2i(B)(c-4): exactly `PASS` and `FAIL`** — the enum of `skills/litmus/schemas/review-output.schema.json:10` and the check at `run-review-loop.sh:923` (both at `727d4652`); any other value, `ERROR` and a non-string included, is UNRECOGNIZED and BLOCKING (enumerated 2026-09-13, native 2a9a6900 finding [12]; UNREVIEWED). The earlier wording accepted "a DICT whose `issues` is a list" with **no `status` requirement**, while item 2i(B)(c-4) defines the fold's participants as envelopes carrying `status`/`issues`. The two contracts admitted the same input and disagreed about whether it participates in the gate — and the disagreement resolved **fail-open**: `{"issues": [], "completeness": "errored"}` was a valid ingested shape under this rule, contributed zero findings, was excluded from the fold by (c-4), and the fold over the one remaining well-formed participant returned `complete` and AUTHORIZED. An `errored` review authorizing a commit, through the one predicate mismatch neither row owned. Verified in source: `_ingest` (`lib/merge-findings.py:91-96`) is `isinstance(parsed, list)` or `isinstance(parsed, dict) and "issues" in parsed`, **with no status test anywhere**. **A dict carrying `issues` but NO `status` is therefore a BLOCKING invalid shape in the same class as `{"issues": "lots"}` (S4), and emits an ingestion-rejection finding rather than contributing silently.** The bare scanner-ARRAY exemption is unchanged — scanners legitimately emit naked lists and always have. (c-4) and bound (ii) CITE this predicate; neither restates it. **Any other parsed shape — `{}`, a JSON string, a number, `{"issues": "lots"}` — is a BLOCKING, NON-AUTHORIZING input of the same class as a parse failure**, while every validly-shaped input in the same run is still ingested and reported. This is the third door in the same class as round-15 [16] and round-16 [2]: an input that becomes **neither a record** for the validation above, **nor an envelope** for 2i(B)(c-4)'s fold, **nor a parse error** for the rule above it — invisible to all three at once. **SHAPE VALIDATION IS NOT ONLY THE CONTAINER'S — THE MEMBERS ARE VALIDATED TOO. ADDED 2026-09-12 (native ca145c70 finding C4, codex issues[4] `completeness`, conf 0.95; UNREVIEWED).** The two accepted shapes above constrain the CONTAINER and say nothing about what it holds, and `_ingest` (`lib/merge-findings.py:91-96`) extends the accumulator with whatever the list contains — read from source 2026-09-12: `isinstance(parsed, list)` ⇒ `sink.extend(parsed)`, with no per-member test. A **non-dict member** (`[null, <validFinding>]`) and a dict member whose **`source` is `null`** each reach an attribute access on the wrong type. **REPRODUCED BY EXECUTION 2026-09-12 against `lib/merge-findings.py` sha256 `511be928…` — the outcome is a CRASH, and the earlier "not reproduced, could be either" hedge in this clause is WITHDRAWN as the measurement settles it.** Both cases exit **rc 1 with EMPTY stdout and no result JSON at all**, at two DIFFERENT sites: `[null, <validFinding>]` dies in **`deduplicate()` at `:26`** — `AttributeError: 'NoneType' object has no attribute 'get'`, reached from `main()` `:185`, i.e. BEFORE `determine_status` ever runs, via argv and via stdin alike; `source: null` dies in **`_is_deterministic_blocker` at `:49`** — `AttributeError: 'NoneType' object has no attribute 'startswith'`, reached from `determine_status` `:70`. **STATE THE CONSEQUENCE PRECISELY, BECAUSE IT IS NOT THE ONE THIS ROW'S OTHER FAIL-OPENS HAVE: this is EVIDENCE LOSS, NOT a fail-open PASS.** No verdict is emitted, so nothing is authorized by it — and the **validly-shaped sibling in the same run is LOST with it**, which is precisely the mixed-run promise stated one paragraph above failing in the one direction a container-only validation cannot see. It must not be recorded as a PASS, and closing it must not be described as removing an authorization path; what it removes is a silent loss of a review's own findings. **So the ingestion boundary validates each MEMBER as well: a member that is not a dict, or that carries a `null`/non-string `source` where the deterministic-exclusion predicate will read one, is a BLOCKING, NON-AUTHORIZING input of the same class as an invalid container — named by its index and observed shape through the existing `internal:merge-findings` synthetic finding, and its validly-shaped siblings in the same run are still ingested and reported.** This adds no new rejection MECHANISM: it is the same ingestion rule, the same synthetic-finding reporting shape, and the same class, applied one level down from the container it already covers. **The `sast:`/`lint:` deterministic exclusion is unchanged**, and no member that is a well-formed finding becomes ineligible. **THE VALID-EMPTY NEGATIVE CONTROLS ARE PART OF ACCEPTANCE, NOT AN AFTERTHOUGHT: `[]` and `{"status":"PASS","issues":[]}` MUST BOTH REMAIN VALID and authorizing-eligible** — `[]` is the honest clean zero the fast path exists for, and an empty `issues` list is a valid envelope still subject to the existing completeness and status rules. A fixture asserting either as a failure would delete the fast path and MUST NOT be written. **2i(B)(c-4)'s scanner-array exclusion is untouched by all of this.**

**THE REJECTION MUST EMIT A FINDING, NOT ONLY A VERDICT — ADDED 2026-09-10 (round-18 finding [3]; Photon technical decision, UNREVIEWED).** Item 2i(A)'s invariant (i) requires that **every FAIL verdict emit at least one finding satisfying the blocking predicate**. A parse failure or an invalid shape produces a FAIL with an empty `issues` array, which would violate that invariant in exactly the case this row adds — and the invariant is NOT weakened to accommodate it. **Reuse the pattern the merger already ships:** the all-inputs-failed branch at `lib/merge-findings.py:168-180` emits a synthetic `high` / `category: "internal"` finding with `source: "internal:merge-findings"`. Ingestion rejection emits the same shape, **naming the rejected input** (its index and the shape observed — `{}`, JSON `""`, a bare number, `{"issues": <non-list>}`, or unparseable). Because that finding is `high`, it satisfies the blocking predicate at every iteration set, so invariant (i) holds **even for a mixed run** of one malformed input beside valid or valid-empty inputs — the case where a count-based rule would otherwise report FAIL with nothing enumerated. Validly-shaped findings from the same run are still ingested and reported alongside it. **No blanket ALLOW, and no weakening of either invariant direction.**

**THE `NaN` CLAUSE ABOVE IS CORRECT AS WRITTEN AND MUST NOT BE ACTIONED AWAY — round-16 reviewer finding grok[6] REFUTED BY EXECUTION 2026-09-10 (UNREVIEWED).** grok argued the `NaN` fixture is unconstructible because `json.loads` would reject a bare `NaN`. It does not: Python's `json` accepts `NaN` by default through `parse_constant` (`json.loads('{"a": NaN}')` ⇒ `{'a': nan}`, and `raw_decode` likewise), and a `high` finding carrying `confidence: NaN` driven through the merger returns PASS. Acting on that suggestion would delete a valid pinned fail-open fixture and replace it with a weaker string-shaped one. The refutation, with its exact commands and outputs, is recorded in `metadata.refutations` of the round-16 `claude.json`.

**THE VALIDATION RUNS OVER ALL INGESTED RECORDS, BEFORE `deduplicate()` — CHRIS-APPROVED 2026-09-09 (`批准840`, round-14 finding [0]); PLAN TEXT ONLY, UNREVIEWED.** Binding contract moved verbatim to `docs/plans/2026-08-27-pipeline-final-plan.d/2026-08-27-pipeline-final-plan--item-8-validation-before-dedup.md` (2026-09-23 split; UNREVIEWED — the review loop does not read it). It remains part of this plan's requirements.

**Class:** needs Chris after data

**Status 2026-09-08:** **SPLIT** — analysis after 2-B; the mutating work on 2-B AND `baseline captured` AND the recorded Chris decision at *Still needs Chris* #18, which is OPEN (#18 cited in this cell 2026-09-24; plan-only, UNREVIEWED; round-14 finding [6]; the node is authoritative and is not restated here. Token corrected from `BLOCKED on 2` 2026-09-10, round-16 finding [5]; Photon technical decision, UNREVIEWED — the status legend requires a SPLIT row to name both halves and both gates in its status cell, and item 8's Progress row already records **SPLIT**, so the two had drifted)

**Evidence / settling check:** Ledger numbers. Explicit decision on impact, reachable trigger, evidence and repair cost — not finding frequency or iteration count. Disputed blockers stay unresolved until the established process settles them; no retry-count auto-approval, no blanket issue closure.
