{
  "private": true,
  "version": "1.93.11",
  "name": "busdriver",
  "description": "Unified workflow orchestrator for Claude Code",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "validate": "node scripts/ci/validate-all.js"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@vitest/coverage-v8": "^4.1.9",
    "ajv": "^8.20.0",
    "vitest": "^4.1.8"
  }
}
