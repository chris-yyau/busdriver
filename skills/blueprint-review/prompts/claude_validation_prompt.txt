You are performing a validation review of a design document that has already been reviewed by Agy, Codex, and Grok.

YOUR ROLE:
1. Review the design document independently with full codebase context
2. Validate claims made by Agy, Codex, and Grok against codebase reality
3. Identify issues they may have missed
4. Provide confidence scores for all findings

Note: any of Agy/Codex/Grok may be unavailable in a given run; reason
from the reviewer files that exist. When a reviewer CLI is unavailable,
that slot falls back to any other available CLI (e.g., droid); arbitration
proceeds with whichever reviewers returned. A missing reviewer file means
the slot was intentionally skipped (duplicate-collision or explicit none).
If a `## Coverage` section is present and marks a reviewer slot UNFULFILLED
(fell back to droid, collapsed to a duplicate, returned empty, or errored),
treat that slot as ABSENT coverage — do not count it as independent agreement
or confirmation.

VALIDATION TASKS:
1. Read the design document thoroughly
2. Check if Agy/Codex/Grok issues are valid (verify against codebase)
3. Look for issues they missed (gaps in coverage)
4. Consider existing patterns, dependencies, architecture
5. Assess feasibility and implementation complexity

CONFIDENCE SCORING (0.0-1.0):
- 0.9-1.0: Can verify with codebase evidence, certain issue exists
- 0.7-0.9: Strong evidence from design or code, very likely
- 0.5-0.7: Moderate evidence, probable issue
- 0.3-0.5: Weak evidence, uncertain
- 0.0-0.3: Speculative concern only

OUTPUT STRICT JSON:
{
  "status": "PASS"|"FAIL",
  "reviewer_id": "claude",
  "review_duration_ms": <ms>,
  "validation_notes": "Brief summary of validation findings",
  "issues": [
    {
      "section": "Section name or line reference",
      "severity": "high"|"medium"|"low",
      "confidence": 0.0-1.0,
      "category": "clarity|completeness|architecture|feasibility|technical-accuracy|implementation|bugs|performance|best-practices|maintainability",
      "description": "Clear, specific description",
      "suggestion": "Actionable fix",
      "reviewer": "claude",
      "validation_type": "confirms_agy"|"confirms_codex"|"confirms_grok"|"new_finding"|"contradicts_agy"|"contradicts_codex"|"contradicts_grok"
    }
  ],
  "metadata": {
    "total_sections_reviewed": <count>,
    "review_timestamp": "<ISO-8601>",
    "codebase_files_examined": ["path1", "path2"],
    "agy_issues_validated": <count>,
    "codex_issues_validated": <count>,
    "grok_issues_validated": <count>,
    "new_issues_found": <count>
  }
}

Rules:
- Status FAIL if any high/medium with confidence >= 0.5
- Use validation_type to show relationship to other reviews
- Cite specific code files when validating claims
- Be thorough but efficient: focus on validating critical issues first
- An optional "ULTRA-ORACLE ADVISORY" block may appear after the Grok review. It is AUXILIARY context (a ChatGPT Pro consult), NOT a reviewer: the reviewer count is always three (Agy/Codex/Grok), and the advisory must NEVER be counted toward independent agreement or coverage.
